@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeneratedGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("accusamus porro non")"
                        Opacity="@(0.7663)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Show)"
                        BlendMode="@(BlendMode.Saturation)"
                        Effect="@(new Effect("quia",
                            9728449.5536))"
                        FullExtent="@(new Extent(114.1488,
                                -102.0125,
                                10.7460,
                                -62.9382,
                                835.3524,
                                133.0089,
                                478.8996,
                                300.0407,
                            new SpatialReference(1,
                                    "et",
                                    "vitae",
                                    "maxime"),
                                true,
                                true))"
                        LegendEnabled="@(true)"
                        MaxScale="@(45777794.1135)"
                        MinScale="@(44276.7700)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(500.5736)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2009, 3, 19, 11, 54, 1))"
                            End="@(new DateTime(2017, 9, 7, 0, 56, 9))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(191, 68, 222),
                                new Dimension(935.2376),
                                SimpleLineSymbolStyle.LongDashDotDot,
                                Cap.Round,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(225, 214, 118),
                                    LinePlacement.Begin,
                                    LineSymbolMarkerStyle.Arrow),
                                813.5028))"
                            Color="@(new MapColor(203, 243, 120))"
                            Style="@(SimpleFillSymbolStyle.Cross)">
                        </SimpleFillSymbol>
                        <PictureMarkerSymbol 
                            Angle="@(858.9458)"
                            Color="@(new MapColor(217, 240, 177))"
                            Xoffset="@(new Dimension(596.4203))"
                            Yoffset="@(new Dimension(820.9674))">
                        </PictureMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(17, 17, 147))"
                            Width="@(new Dimension(298.3613))"
                            Style="@(SimpleLineSymbolStyle.LongDash)"
                            Cap="@(Cap.Butt)"
                            Join="@(Join.Miter)"
                            Marker="@(new LineSymbolMarker(new MapColor(14, 177, 172),
                                LinePlacement.End,
                                LineSymbolMarkerStyle.Square))"
                            MiterLimit="@(631.6524)">
                        </SimpleLineSymbol>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.Exclusion;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(-150.2920,
                    165.3835,
                    -84.9463,
                    16.1092,
                    758.8887,
                    731.7292,
                    752.3158,
                    213.0371,
                new SpatialReference(2,
                        "et",
                        "eos",
                        "sit"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(219, 68, 69),
                    new Dimension(941.0519),
                SimpleLineSymbolStyle.LongDashDot,
                Cap.Butt,
                Join.Round,
                new LineSymbolMarker(new MapColor(81, 66, 39),
                    LinePlacement.BeginEnd,
                    LineSymbolMarkerStyle.X),
                    157.3038);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(117, 12, 184),
                    new Dimension(265.5285),
                    SimpleLineSymbolStyle.ShortDashDot,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(185, 90, 90),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Diamond),
                    420.3719),
                new MapColor(49, 22, 177),
                    new Dimension(598.1922),
                SimpleMarkerSymbolStyle.Triangle,
                    535.9469,
                    new Dimension(9.2517),
                    new Dimension(338.5916),
                    "voluptatem");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(3, 183, 253),
                    new Dimension(147.9828),
                    SimpleLineSymbolStyle.ShortDash,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(222, 92, 157),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Circle),
                    785.7606),
                new MapColor(226, 91, 159),
                SimpleFillSymbolStyle.Cross);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2005, 3, 24, 5, 58, 40),
                    new DateTime(2010, 8, 12, 2, 4, 42));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

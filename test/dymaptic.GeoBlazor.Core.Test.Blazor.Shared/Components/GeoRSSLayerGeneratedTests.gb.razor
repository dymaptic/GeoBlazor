@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeneratedGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("ipsa corrupti vel")"
                        Opacity="@(0.5010)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Hide)"
                        BlendMode="@(BlendMode.Color)"
                        Effect="@(new Effect("ullam",
                            24182591.9071))"
                        LegendEnabled="@(true)"
                        MaxScale="@(24691299.6639)"
                        MinScale="@(44668.9014)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(561.8860)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2002, 3, 5, 21, 34, 33))"
                            End="@(new DateTime(2000, 4, 25, 3, 34, 41))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(91, 112, 4),
                                new Dimension(227.5983),
                                SimpleLineSymbolStyle.ShortDash,
                                Cap.Round,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(245, 10, 16),
                                    LinePlacement.Begin,
                                    LineSymbolMarkerStyle.Square),
                                316.9502))"
                            Color="@(new MapColor(25, 28, 238))"
                            Style="@(SimpleFillSymbolStyle.DiagonalCross)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(953.8419)"
                            Color="@(new MapColor(248, 94, 145))"
                            Xoffset="@(new Dimension(566.7783))"
                            Yoffset="@(new Dimension(486.7431))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(122, 8, 165))"
                            Width="@(new Dimension(196.2813))"
                            Style="@(SimpleLineSymbolStyle.None)"
                            Cap="@(Cap.Round)"
                            Join="@(Join.Round)"
                            Marker="@(new LineSymbolMarker(new MapColor(92, 215, 218),
                                LinePlacement.BeginEnd,
                                LineSymbolMarkerStyle.X))"
                            MiterLimit="@(966.8439)">
                        </SimpleLineSymbol>
                        <Extent 
                            Xmax="@(-24.6902)"
                            Xmin="@(-133.3545)"
                            Ymax="@(-84.8376)"
                            Ymin="@(-64.7607)"
                            Zmax="@(265.2106)"
                            Zmin="@(183.7500)"
                            Mmax="@(344.3811)"
                            Mmin="@(666.0804)"
                            SpatialReference="@(new SpatialReference(9,
                                    "et",
                                    "vero",
                                    "ea"))"
                            HasM="@(true)"
                            HasZ="@(true)">
                        </Extent>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.Xor;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(54.1178,
                    -151.9417,
                    13.5085,
                    68.1327,
                    129.1877,
                    985.8366,
                    199.2548,
                    705.1625,
                new SpatialReference(9,
                        "sed",
                        "ea",
                        "quia"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(118, 79, 125),
                    new Dimension(74.1863),
                SimpleLineSymbolStyle.Dash,
                Cap.Square,
                Join.Miter,
                new LineSymbolMarker(new MapColor(160, 50, 184),
                    LinePlacement.Begin,
                    LineSymbolMarkerStyle.Diamond),
                    875.9656);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(6, 48, 210),
                    new Dimension(446.6091),
                    SimpleLineSymbolStyle.DashDot,
                    Cap.Round,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(109, 54, 160),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Square),
                    775.8044),
                new MapColor(77, 93, 178),
                    new Dimension(360.1387),
                SimpleMarkerSymbolStyle.Square,
                    912.5765,
                    new Dimension(214.3404),
                    new Dimension(14.4539),
                    "hic");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(130, 226, 175),
                    new Dimension(851.3919),
                    SimpleLineSymbolStyle.DashDot,
                    Cap.Butt,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(147, 222, 0),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Arrow),
                    662.7800),
                new MapColor(168, 202, 198),
                SimpleFillSymbolStyle.Horizontal);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2016, 11, 14, 18, 12, 41),
                    new DateTime(2004, 5, 15, 3, 56, 0));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

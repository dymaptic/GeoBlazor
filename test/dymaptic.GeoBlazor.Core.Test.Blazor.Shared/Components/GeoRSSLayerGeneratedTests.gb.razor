@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeneratedGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("illo eligendi iste")"
                        Opacity="@(0.0517)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Show)"
                        BlendMode="@(BlendMode.Screen)"
                        Effect="@(new Effect("voluptatum",
                            440805.7134))"
                        LegendEnabled="@(true)"
                        MaxScale="@(37950467.6463)"
                        MinScale="@(27244.6667)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(136.0872)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2015, 1, 4, 3, 38, 46))"
                            End="@(new DateTime(2014, 6, 24, 10, 3, 51))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(142, 150, 38),
                                new Dimension(958.0604),
                                SimpleLineSymbolStyle.DashDot,
                                Cap.Round,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(69, 129, 252),
                                    LinePlacement.Begin,
                                    LineSymbolMarkerStyle.Square),
                                120.9118))"
                            Color="@(new MapColor(237, 20, 190))"
                            Style="@(SimpleFillSymbolStyle.Vertical)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(173.7013)"
                            Color="@(new MapColor(190, 250, 187))"
                            Xoffset="@(new Dimension(823.1394))"
                            Yoffset="@(new Dimension(641.8502))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(34, 187, 66))"
                            Width="@(new Dimension(690.9975))"
                            Style="@(SimpleLineSymbolStyle.ShortDot)"
                            Cap="@(Cap.Round)"
                            Join="@(Join.Round)"
                            Marker="@(new LineSymbolMarker(new MapColor(120, 59, 32),
                                LinePlacement.Begin,
                                LineSymbolMarkerStyle.Square))"
                            MiterLimit="@(441.0336)">
                        </SimpleLineSymbol>
                        <Extent 
                            Xmax="@(4.3068)"
                            Xmin="@(-99.4488)"
                            Ymax="@(7.4416)"
                            Ymin="@(-68.7672)"
                            Zmax="@(90.3094)"
                            Zmin="@(99.9248)"
                            Mmax="@(64.0545)"
                            Mmin="@(331.6714)"
                            SpatialReference="@(new SpatialReference(2,
                                    "voluptas",
                                    "mollitia",
                                    "repudiandae"))"
                            HasM="@(true)"
                            HasZ="@(true)">
                        </Extent>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Show;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.Plus;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(46.9898,
                    -129.1943,
                    20.3536,
                    -3.4769,
                    692.3410,
                    926.7110,
                    958.1582,
                    164.4458,
                new SpatialReference(2,
                        "ducimus",
                        "quaerat",
                        "quibusdam"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(64, 191, 71),
                    new Dimension(945.2723),
                SimpleLineSymbolStyle.ShortDot,
                Cap.Round,
                Join.Round,
                new LineSymbolMarker(new MapColor(126, 131, 124),
                    LinePlacement.BeginEnd,
                    LineSymbolMarkerStyle.X),
                    115.1563);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(207, 142, 162),
                    new Dimension(609.1080),
                    SimpleLineSymbolStyle.LongDashDot,
                    Cap.Round,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(204, 187, 216),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Square),
                    373.6669),
                new MapColor(171, 27, 104),
                    new Dimension(15.0512),
                SimpleMarkerSymbolStyle.Square,
                    994.7368,
                    new Dimension(869.6679),
                    new Dimension(559.2575),
                    "repudiandae");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(131, 23, 2),
                    new Dimension(853.4277),
                    SimpleLineSymbolStyle.ShortDashDotDot,
                    Cap.Butt,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(60, 32, 65),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Diamond),
                    54.1105),
                new MapColor(200, 125, 109),
                SimpleFillSymbolStyle.BackwardDiagonal);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2008, 3, 7, 4, 9, 1),
                    new DateTime(2013, 6, 14, 0, 48, 36));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

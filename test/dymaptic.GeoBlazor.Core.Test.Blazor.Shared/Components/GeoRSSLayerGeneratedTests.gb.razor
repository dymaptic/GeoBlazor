@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeneratedGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("aut sit consequuntur")"
                        Opacity="@(0.8969)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Show)"
                        BlendMode="@(BlendMode.DestinationOver)"
                        Effect="@(new Effect("voluptates",
                            45095551.7209))"
                        LegendEnabled="@(true)"
                        MaxScale="@(15277340.8278)"
                        MinScale="@(32156.8185)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(686.4461)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2015, 2, 9, 9, 29, 55))"
                            End="@(new DateTime(2021, 4, 26, 11, 37, 25))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(100, 15, 165),
                                new Dimension(776.1350),
                                SimpleLineSymbolStyle.Dash,
                                Cap.Round,
                                Join.Round,
                                new LineSymbolMarker(new MapColor(126, 150, 25),
                                    LinePlacement.Begin,
                                    LineSymbolMarkerStyle.Cross),
                                386.5046))"
                            Color="@(new MapColor(139, 144, 170))"
                            Style="@(SimpleFillSymbolStyle.DiagonalCross)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(124.1819)"
                            Color="@(new MapColor(246, 132, 137))"
                            Xoffset="@(new Dimension(195.9115))"
                            Yoffset="@(new Dimension(165.8853))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(84, 236, 156))"
                            Width="@(new Dimension(218.7633))"
                            Style="@(SimpleLineSymbolStyle.LongDashDotDot)"
                            Cap="@(Cap.Round)"
                            Join="@(Join.Bevel)"
                            Marker="@(new LineSymbolMarker(new MapColor(119, 77, 68),
                                LinePlacement.BeginEnd,
                                LineSymbolMarkerStyle.Square))"
                            MiterLimit="@(2.4495)">
                        </SimpleLineSymbol>
                        <Extent 
                            Xmax="@(50.5962)"
                            Xmin="@(144.0270)"
                            Ymax="@(87.7707)"
                            Ymin="@(-44.1366)"
                            Zmax="@(839.4871)"
                            Zmin="@(14.0585)"
                            Mmax="@(39.8983)"
                            Mmin="@(350.5573)"
                            SpatialReference="@(new SpatialReference(3,
                                    "reprehenderit",
                                    "ea",
                                    "velit"))"
                            HasM="@(true)"
                            HasZ="@(true)">
                        </Extent>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.DestinationIn;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(35.5450,
                    166.2011,
                    40.4647,
                    -22.7917,
                    190.6469,
                    245.9116,
                    322.6748,
                    176.7185,
                new SpatialReference(8,
                        "ea",
                        "ut",
                        "ut"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(39, 95, 0),
                    new Dimension(51.2657),
                SimpleLineSymbolStyle.LongDashDotDot,
                Cap.Butt,
                Join.Miter,
                new LineSymbolMarker(new MapColor(58, 183, 35),
                    LinePlacement.BeginEnd,
                    LineSymbolMarkerStyle.Cross),
                    551.1718);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(98, 35, 151),
                    new Dimension(236.9553),
                    SimpleLineSymbolStyle.Dot,
                    Cap.Round,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(223, 18, 253),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Diamond),
                    514.6080),
                new MapColor(103, 224, 28),
                    new Dimension(957.4348),
                SimpleMarkerSymbolStyle.X,
                    194.6459,
                    new Dimension(158.8874),
                    new Dimension(850.8609),
                    "rerum");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(221, 155, 219),
                    new Dimension(926.2823),
                    SimpleLineSymbolStyle.ShortDashDotDot,
                    Cap.Round,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(96, 246, 172),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Diamond),
                    151.8155),
                new MapColor(87, 252, 35),
                SimpleFillSymbolStyle.Solid);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2001, 5, 26, 16, 44, 43),
                    new DateTime(2000, 2, 10, 19, 48, 3));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

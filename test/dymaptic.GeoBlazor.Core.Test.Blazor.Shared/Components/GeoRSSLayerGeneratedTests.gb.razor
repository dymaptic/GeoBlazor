@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("accusamus eum voluptatem")"
                        Opacity="@(0.2731)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Show)"
                        BlendMode="@(BlendMode.Xor)"
                        Effect="@(new Effect("fugiat",
                            32151759.4788))"
                        FullExtent="@(new Extent(-167.4123,
                                -107.3156,
                                -23.5921,
                                1.7759,
                                27.3503,
                                58.7793,
                                154.9391,
                                64.6619,
                            new SpatialReference(7,
                                    "qui",
                                    "dicta",
                                    "repudiandae"),
                                true,
                                true))"
                        LegendEnabled="@(true)"
                        MaxScale="@(7055998.9373)"
                        MinScale="@(49297.2930)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(276.8758)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2017, 6, 25, 21, 33, 6))"
                            End="@(new DateTime(2013, 3, 21, 5, 27, 9))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(209, 164, 122),
                                new Dimension(105.3940),
                                SimpleLineSymbolStyle.ShortDashDot,
                                Cap.Butt,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(197, 186, 43),
                                    LinePlacement.End,
                                    LineSymbolMarkerStyle.Cross),
                                0.5508))"
                            Color="@(new MapColor(32, 17, 28))"
                            Style="@(SimpleFillSymbolStyle.BackwardDiagonal)">
                        </SimpleFillSymbol>
                        <PictureMarkerSymbol 
                            Angle="@(474.9795)"
                            Color="@(new MapColor(20, 198, 138))"
                            Xoffset="@(new Dimension(766.9588))"
                            Yoffset="@(new Dimension(953.3166))">
                        </PictureMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(178, 116, 2))"
                            Width="@(new Dimension(100.1897))"
                            Style="@(SimpleLineSymbolStyle.Solid)"
                            Cap="@(Cap.Butt)"
                            Join="@(Join.Miter)"
                            Marker="@(new LineSymbolMarker(new MapColor(215, 174, 237),
                                LinePlacement.Begin,
                                LineSymbolMarkerStyle.Diamond))"
                            MiterLimit="@(434.6999)">
                        </SimpleLineSymbol>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Show;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.Lighter;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(5.4934,
                    27.4867,
                    -89.5805,
                    30.4021,
                    482.4365,
                    498.7124,
                    37.8566,
                    13.4560,
                new SpatialReference(3,
                        "nemo",
                        "quia",
                        "et"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(242, 220, 97),
                    new Dimension(353.6493),
                SimpleLineSymbolStyle.None,
                Cap.Butt,
                Join.Miter,
                new LineSymbolMarker(new MapColor(82, 164, 211),
                    LinePlacement.Begin,
                    LineSymbolMarkerStyle.Arrow),
                    637.1451);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(182, 161, 132),
                    new Dimension(227.2009),
                    SimpleLineSymbolStyle.LongDashDotDot,
                    Cap.Round,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(153, 53, 209),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Diamond),
                    383.5865),
                new MapColor(59, 154, 34),
                    new Dimension(575.6367),
                SimpleMarkerSymbolStyle.Circle,
                    451.3902,
                    new Dimension(115.4340),
                    new Dimension(926.2405),
                    "qui");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(232, 130, 170),
                    new Dimension(187.7013),
                    SimpleLineSymbolStyle.ShortDashDot,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(1, 129, 195),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Cross),
                    648.4076),
                new MapColor(183, 205, 149),
                SimpleFillSymbolStyle.BackwardDiagonal);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2009, 1, 22, 16, 21, 1),
                    new DateTime(2010, 11, 24, 20, 33, 38));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

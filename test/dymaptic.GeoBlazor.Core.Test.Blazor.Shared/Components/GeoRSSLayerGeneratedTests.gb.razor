@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("officiis provident exercitationem")"
                        Opacity="@(0.8270)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Hide)"
                        BlendMode="@(BlendMode.Difference)"
                        Effect="@(new Effect("quae",
                            31714450.0159))"
                        FullExtent="@(new Extent(103.9049,
                                -22.3440,
                                -85.5843,
                                21.7914,
                                298.7823,
                                490.9996,
                                717.5126,
                                935.8958,
                            new SpatialReference(2,
                                    "praesentium",
                                    "quos",
                                    "perferendis"),
                                true,
                                true))"
                        LegendEnabled="@(true)"
                        MaxScale="@(6045940.6971)"
                        MinScale="@(18734.2380)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(330.8898)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2005, 6, 6, 19, 36, 41))"
                            End="@(new DateTime(2017, 10, 23, 18, 31, 45))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(171, 221, 173),
                                new Dimension(795.4547),
                                SimpleLineSymbolStyle.LongDashDotDot,
                                Cap.Butt,
                                Join.Round,
                                new LineSymbolMarker(new MapColor(84, 50, 142),
                                    LinePlacement.End,
                                    LineSymbolMarkerStyle.Arrow),
                                372.5652))"
                            Color="@(new MapColor(176, 45, 38))"
                            Style="@(SimpleFillSymbolStyle.ForwardDiagonal)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(894.4202)"
                            Color="@(new MapColor(222, 76, 185))"
                            Xoffset="@(new Dimension(151.8010))"
                            Yoffset="@(new Dimension(606.1768))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(97, 220, 101))"
                            Width="@(new Dimension(294.7544))"
                            Style="@(SimpleLineSymbolStyle.LongDash)"
                            Cap="@(Cap.Butt)"
                            Join="@(Join.Bevel)"
                            Marker="@(new LineSymbolMarker(new MapColor(131, 118, 78),
                                LinePlacement.Begin,
                                LineSymbolMarkerStyle.Arrow))"
                            MiterLimit="@(508.9946)">
                        </SimpleLineSymbol>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.Multiply;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(30.6122,
                    4.0006,
                    63.1725,
                    49.0649,
                    687.4804,
                    270.9540,
                    892.1218,
                    691.3503,
                new SpatialReference(3,
                        "dolor",
                        "in",
                        "aut"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(68, 151, 146),
                    new Dimension(741.9931),
                SimpleLineSymbolStyle.None,
                Cap.Round,
                Join.Miter,
                new LineSymbolMarker(new MapColor(148, 29, 97),
                    LinePlacement.Begin,
                    LineSymbolMarkerStyle.Square),
                    777.6988);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(65, 215, 44),
                    new Dimension(274.6224),
                    SimpleLineSymbolStyle.ShortDot,
                    Cap.Butt,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(37, 17, 232),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Diamond),
                    233.0534),
                new MapColor(15, 8, 228),
                    new Dimension(600.8705),
                SimpleMarkerSymbolStyle.Path,
                    268.5858,
                    new Dimension(781.4571),
                    new Dimension(205.3854),
                    "recusandae");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(143, 198, 52),
                    new Dimension(127.7862),
                    SimpleLineSymbolStyle.Dash,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(54, 117, 122),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Cross),
                    943.6223),
                new MapColor(81, 183, 53),
                SimpleFillSymbolStyle.Horizontal);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2013, 8, 12, 20, 22, 27),
                    new DateTime(2018, 4, 11, 21, 25, 56));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

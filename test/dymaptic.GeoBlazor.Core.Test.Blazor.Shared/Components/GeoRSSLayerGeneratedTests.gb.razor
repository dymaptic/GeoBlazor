@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeneratedGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("rem nesciunt dolorum")"
                        Opacity="@(0.3246)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Hide)"
                        BlendMode="@(BlendMode.Minus)"
                        Effect="@(new Effect("esse",
                            43637607.6830))"
                        FullExtent="@(new Extent(-101.0635,
                                -134.3997,
                                -38.9987,
                                -3.6687,
                                263.5024,
                                275.1322,
                                264.5855,
                                947.0134,
                            new SpatialReference(9,
                                    "blanditiis",
                                    "rerum",
                                    "et"),
                                true,
                                true))"
                        LegendEnabled="@(true)"
                        MaxScale="@(1100194.3450)"
                        MinScale="@(19792.6207)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(199.3738)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2009, 6, 10, 21, 27, 7))"
                            End="@(new DateTime(2012, 8, 22, 12, 53, 21))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(110, 67, 232),
                                new Dimension(746.4103),
                                SimpleLineSymbolStyle.ShortDashDot,
                                Cap.Round,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(126, 151, 2),
                                    LinePlacement.Begin,
                                    LineSymbolMarkerStyle.Diamond),
                                768.9830))"
                            Color="@(new MapColor(6, 205, 208))"
                            Style="@(SimpleFillSymbolStyle.BackwardDiagonal)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(788.2304)"
                            Color="@(new MapColor(181, 227, 190))"
                            Xoffset="@(new Dimension(473.2883))"
                            Yoffset="@(new Dimension(134.9052))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(75, 28, 62))"
                            Width="@(new Dimension(97.3748))"
                            Style="@(SimpleLineSymbolStyle.ShortDash)"
                            Cap="@(Cap.Butt)"
                            Join="@(Join.Bevel)"
                            Marker="@(new LineSymbolMarker(new MapColor(80, 202, 215),
                                LinePlacement.Begin,
                                LineSymbolMarkerStyle.Diamond))"
                            MiterLimit="@(45.7584)">
                        </SimpleLineSymbol>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.VividLight;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(-135.5565,
                    -147.9994,
                    59.9276,
                    -30.2772,
                    408.1491,
                    270.2218,
                    85.2810,
                    137.1593,
                new SpatialReference(4,
                        "qui",
                        "ex",
                        "quisquam"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(78, 254, 171),
                    new Dimension(787.9436),
                SimpleLineSymbolStyle.Dash,
                Cap.Butt,
                Join.Round,
                new LineSymbolMarker(new MapColor(24, 134, 50),
                    LinePlacement.End,
                    LineSymbolMarkerStyle.Diamond),
                    0.9887);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(135, 215, 48),
                    new Dimension(99.2005),
                    SimpleLineSymbolStyle.Dot,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(227, 129, 63),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Cross),
                    670.9706),
                new MapColor(204, 120, 154),
                    new Dimension(87.5350),
                SimpleMarkerSymbolStyle.Circle,
                    115.5091,
                    new Dimension(568.6667),
                    new Dimension(946.9261),
                    "quos");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(99, 235, 121),
                    new Dimension(735.9785),
                    SimpleLineSymbolStyle.ShortDot,
                    Cap.Butt,
                    Join.Miter,
                    new LineSymbolMarker(new MapColor(55, 181, 176),
                        LinePlacement.End,
                        LineSymbolMarkerStyle.Diamond),
                    867.9891),
                new MapColor(249, 158, 253),
                SimpleFillSymbolStyle.Cross);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2018, 9, 17, 5, 55, 8),
                    new DateTime(2011, 6, 15, 14, 12, 17));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

@inherits TestRunnerBase

@{
    base.BuildRenderTree(__builder);
}

@code {

    [TestMethod]
    public async Task TestCanRenderGeoRSSLayer(Action renderHandler)
    {
        GeoRSSLayer? geoRSSLayer = null;

        RefreshEvent? refreshResult = null;
        void OnRefresh(RefreshEvent result)
        {
            refreshResult = result;
        }
        AddMapRenderFragment(
            @<MapView class="map-view" OnViewRendered="renderHandler">
                <Map>
                    <Basemap>
                        <BasemapStyle Name="BasemapStyleName.ArcgisTopographic" />
                    </Basemap>
                    <GeoRSSLayer 
                        Url="@("https://arcgis.github.io/arcgis-samples-javascript/sample-data/layers-georss/sample-georss.xml")"
                        Title="@("quo vitae est")"
                        Opacity="@(0.9238)"
                        Visible="@(true)"
                        ListMode="@(ListMode.Hide)"
                        BlendMode="@(BlendMode.SourceOut)"
                        Effect="@(new Effect("ea",
                            44936940.5452))"
                        FullExtent="@(new Extent(-157.7836,
                                -152.7866,
                                65.4302,
                                -63.3540,
                                626.2712,
                                425.1675,
                                932.5493,
                                668.3009,
                            new SpatialReference(5,
                                    "quia",
                                    "blanditiis",
                                    "neque"),
                                true,
                                true))"
                        LegendEnabled="@(true)"
                        MaxScale="@(17627800.0250)"
                        MinScale="@(13440.8807)"
                        PersistenceEnabled="@(true)"
                        RefreshInterval="@(369.5282)"
                        OnRefresh="OnRefresh">
                        <TimeExtent 
                            Start="@(new DateTime(2011, 4, 15, 14, 53, 39))"
                            End="@(new DateTime(2013, 10, 21, 5, 57, 38))">
                        </TimeExtent>
                        <SimpleFillSymbol 
                            Outline="@(new Outline(new MapColor(108, 173, 54),
                                new Dimension(200.6539),
                                SimpleLineSymbolStyle.Dot,
                                Cap.Butt,
                                Join.Miter,
                                new LineSymbolMarker(new MapColor(214, 141, 18),
                                    LinePlacement.End,
                                    LineSymbolMarkerStyle.Arrow),
                                785.1394))"
                            Color="@(new MapColor(100, 86, 251))"
                            Style="@(SimpleFillSymbolStyle.DiagonalCross)">
                        </SimpleFillSymbol>
                        <SimpleMarkerSymbol 
                            Angle="@(762.9859)"
                            Color="@(new MapColor(118, 144, 78))"
                            Xoffset="@(new Dimension(735.4795))"
                            Yoffset="@(new Dimension(962.7770))">
                        </SimpleMarkerSymbol>
                        <SimpleLineSymbol 
                            Color="@(new MapColor(106, 213, 35))"
                            Width="@(new Dimension(169.0508))"
                            Style="@(SimpleLineSymbolStyle.ShortDashDot)"
                            Cap="@(Cap.Round)"
                            Join="@(Join.Bevel)"
                            Marker="@(new LineSymbolMarker(new MapColor(130, 75, 192),
                                LinePlacement.Begin,
                                LineSymbolMarkerStyle.Arrow))"
                            MiterLimit="@(293.2567)">
                        </SimpleLineSymbol>
                    </GeoRSSLayer>
                </Map>
            </MapView>);
        await WaitForMapToRender();
        Assert.IsNotNull(geoRSSLayer!);
        await AssertJavaScript("dispatchEvent", args: [geoRSSLayer!.Id, "refresh"]);
        int tries = 100;
        while (tries > 0 &&
            (refreshResult is null))
        {
            await Task.Delay(100);
            tries--;
        }
        Assert.IsInstanceOfType<RefreshEvent>(refreshResult);
        await AssertJavaScript("assertLayerExists", args: "geo-rss");
        await geoRSSLayer.Refresh();
        ListMode testListMode = ListMode.Hide;
        await geoRSSLayer.SetListMode(testListMode);
        var listMode = await geoRSSLayer.GetListMode();
        Assert.AreEqual(testListMode, listMode);
        BlendMode testBlendMode = BlendMode.DestinationAtop;
        await geoRSSLayer.SetBlendMode(testBlendMode);
        var blendMode = await geoRSSLayer.GetBlendMode();
        Assert.AreEqual(testBlendMode, blendMode);
        Extent testFullExtent = new Extent(-5.6084,
                    -33.8492,
                    -22.1209,
                    -25.1816,
                    496.3453,
                    883.2781,
                    773.0871,
                    867.7949,
                new SpatialReference(1,
                        "voluptatem",
                        "quia",
                        "aut"),
                    true,
                    true);
        await geoRSSLayer.SetFullExtent(testFullExtent);
        var fullExtent = await geoRSSLayer.GetFullExtent();
        Assert.AreEqual(testFullExtent, fullExtent);
        SimpleLineSymbol testLineSymbol = new SimpleLineSymbol(new MapColor(112, 11, 250),
                    new Dimension(660.7737),
                SimpleLineSymbolStyle.LongDashDotDot,
                Cap.Round,
                Join.Bevel,
                new LineSymbolMarker(new MapColor(103, 36, 24),
                    LinePlacement.Begin,
                    LineSymbolMarkerStyle.Arrow),
                    374.3440);
        await geoRSSLayer.SetLineSymbol(testLineSymbol);
        var lineSymbol = await geoRSSLayer.GetLineSymbol();
        Assert.AreEqual(testLineSymbol, lineSymbol);
        MarkerSymbol testPointSymbol = new SimpleMarkerSymbol(new Outline(new MapColor(130, 119, 91),
                    new Dimension(323.3362),
                    SimpleLineSymbolStyle.DashDot,
                    Cap.Butt,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(235, 7, 151),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Cross),
                    185.3882),
                new MapColor(193, 150, 90),
                    new Dimension(710.7766),
                SimpleMarkerSymbolStyle.X,
                    855.7054,
                    new Dimension(749.6407),
                    new Dimension(671.1537),
                    "minus");
        await geoRSSLayer.SetPointSymbol(testPointSymbol);
        var pointSymbol = await geoRSSLayer.GetPointSymbol();
        Assert.AreEqual(testPointSymbol, pointSymbol);
        SimpleFillSymbol testPolygonSymbol = new SimpleFillSymbol(new Outline(new MapColor(37, 239, 173),
                    new Dimension(857.3112),
                    SimpleLineSymbolStyle.LongDash,
                    Cap.Butt,
                    Join.Round,
                    new LineSymbolMarker(new MapColor(176, 197, 75),
                        LinePlacement.Begin,
                        LineSymbolMarkerStyle.Diamond),
                    691.1545),
                new MapColor(200, 58, 240),
                SimpleFillSymbolStyle.None);
        await geoRSSLayer.SetPolygonSymbol(testPolygonSymbol);
        var polygonSymbol = await geoRSSLayer.GetPolygonSymbol();
        Assert.AreEqual(testPolygonSymbol, polygonSymbol);
        TimeExtent testVisibilityTimeExtent = new TimeExtent(new DateTime(2016, 3, 24, 6, 5, 25),
                    new DateTime(2022, 5, 16, 2, 32, 4));
        await geoRSSLayer.SetVisibilityTimeExtent(testVisibilityTimeExtent);
        var visibilityTimeExtent = await geoRSSLayer.GetVisibilityTimeExtent();
        Assert.AreEqual(testVisibilityTimeExtent, visibilityTimeExtent);
    }
}

// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetIRouteSymbolsDirectionLines } from './iRouteSymbolsDirectionLines';

export async function buildJsIRouteSymbolsDirectionLinesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsRouteSymbolsDirectionLines: any = {};

    
    let jsObjectRef = DotNet.createJSObjectReference(jsRouteSymbolsDirectionLines);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsRouteSymbolsDirectionLines;
    
    let { buildDotNetIRouteSymbolsDirectionLines } = await import('./iRouteSymbolsDirectionLines');
    let dnInstantiatedObject = await buildDotNetIRouteSymbolsDirectionLines(jsRouteSymbolsDirectionLines, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type IRouteSymbolsDirectionLines detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for IRouteSymbolsDirectionLines', e);
    }
    
    return jsRouteSymbolsDirectionLines;
}


export async function buildDotNetIRouteSymbolsDirectionLinesGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsIRouteSymbolsDirectionLines } = await import('./iRouteSymbolsDirectionLines');
        jsComponentRef = await buildJsIRouteSymbolsDirectionLines(jsObject, layerId, viewId);
    }
    
    let dotNetIRouteSymbolsDirectionLines: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };


    if (hasValue(geoBlazorId)) {
        dotNetIRouteSymbolsDirectionLines.id = geoBlazorId;
    }

    return dotNetIRouteSymbolsDirectionLines;
}


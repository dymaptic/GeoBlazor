// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import Ground from '@arcgis/core/Ground';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';
import { buildDotNetGround } from './ground';
import { buildDotNetMapColor } from './mapColor';
import { buildDotNetGroundNavigationConstraint } from './groundNavigationConstraint';
import { buildDotNetElevationQueryResult } from './elevationQueryResult';
import { buildJsMapColor } from './mapColor';
import { buildJsGroundNavigationConstraint } from './groundNavigationConstraint';
import BaseComponent from "./baseComponent";

export default class GroundGenerated extends BaseComponent {
    public component: Ground;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: Ground) {
        super(component);
        this.component = component;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.layers) && dotNetObject.layers!.length > 0) {
            let { buildJsLayer } = await import('./layer');

            this.component.layers = await Promise.all(dotNetObject.layers?.map(i => buildJsLayer(i, this.layerId, this.viewId)));
        }

        if (hasValue(dotNetObject.navigationConstraint)) {
            this.component.navigationConstraint = await buildJsGroundNavigationConstraint(dotNetObject.navigationConstraint, this.layerId, this.viewId);
        }

        if (hasValue(dotNetObject.surfaceColor)) {
            this.component.surfaceColor = buildJsMapColor(dotNetObject.surfaceColor);
        }


        if (hasValue(dotNetObject.opacity)) {
            this.component.opacity = dotNetObject.opacity;
        }
    }
    
    // region methods
    async cancelLoad(): Promise<void> {
        this.component.cancelLoad();
    }

    async createElevationSampler(extent: any,
        options?: any | null,
        signal?: AbortSignal): Promise<any> {
        options.signal = signal;
        let paramList: any[] = [];
        let { buildJsExtent } = await import('./extent');

        let jsExtent: any = buildJsExtent(extent);
        paramList.push(jsExtent);
        paramList.push(options);
        let result = await this.component.createElevationSampler(...paramList as [any, any]) as any;
        if (hasValue(result)) {
            let { buildDotNetIElevationSampler } = await import('./iElevationSampler');

            return await buildDotNetIElevationSampler(result);
        }
                               
        return null;
    }

    async isFulfilled(): Promise<any> {
        let result = this.component.isFulfilled() as any;
        return result;
    }

    async isRejected(): Promise<any> {
        let result = this.component.isRejected() as any;
        return result;
    }

    async isResolved(): Promise<any> {
        let result = this.component.isResolved() as any;
        return result;
    }

    async load(signal?: AbortSignal): Promise<any> {
        let options = { signal: signal };
        let paramList: any[] = [];
        paramList.push(options);
        let result = await this.component.load(...paramList as [any]) as any;
        if (hasValue(result)) {
            return result;
        }
                               
        return null;
    }

    async loadAll(): Promise<any> {
        let result = await this.component.loadAll() as any;
        if (hasValue(result)) {
            let { buildDotNetGround } = await import('./ground');

            return await buildDotNetGround(result, this.layerId, this.viewId);
        }
                               
        return null;
    }

    async queryElevation(geometry: any,
        options?: any | null,
        signal?: AbortSignal): Promise<any> {
        options.signal = signal;
        let paramList: any[] = [];
        let { buildJsPoint } = await import('./point');

        let jsGeometry: any = buildJsPoint(geometry);
        paramList.push(jsGeometry);
        paramList.push(options);
        let result = await this.component.queryElevation(...paramList as [any, any]) as any;
        if (hasValue(result)) {
            return buildDotNetElevationQueryResult(result, this.viewId);
        }
                               
        return null;
    }

    async when(hasCallback: boolean,
        callbackFuncId: string,
        hasErrback: boolean,
        errbackFuncId: string): Promise<any> {
        let paramList: any[] = [];
        let skippedLastParam = false;
        let dotNetObject = dotNetRefs[this.geoBlazorId!];
        if (hasValue(hasCallback) && hasCallback) {
            let jsCallback = async () => {
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsCallback', callbackFuncId, );
            };

                if (skippedLastParam) {
                    paramList.push(undefined);
                }

                paramList.push(jsCallback);
                skippedLastParam = false;
            } else {
                skippedLastParam = true;
            }
        if (hasValue(hasErrback) && hasErrback) {
            let jsErrback = async () => {
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsErrback', errbackFuncId, );
            };

                paramList.push(jsErrback);
            }
        let result = await this.component.when(...paramList as [any, any]) as any;
        if (hasValue(result)) {
            return result;
        }
                               
        return null;
    }

    // region properties
    
    async getLayers(): Promise<any> {
        if (this.component.loadStatus === 'not-loaded') {
            await this.component.load();
        }
        
        
        if (hasValue(this.component.layers) && this.component.layers!.length > 0) {
            let { buildDotNetLayer } = await import('./layer');

            return await Promise.all(this.component.layers?.map(i => buildDotNetLayer(i, this.layerId, this.viewId)));
        }
                               
        return null;
    }

    async setLayers(value: any): Promise<void> {
        if (hasValue(value) && value!.length > 0) {
            let { buildJsLayer } = await import('./layer');

            this.component.layers = await Promise.all(value?.map(i => buildJsLayer(i, this.layerId, this.viewId)));
        } else {
            this.component.layers?.removeAll();
        }
    }

    async getNavigationConstraint(): Promise<any> {
        if (this.component.loadStatus === 'not-loaded') {
            await this.component.load();
        }
        
        
        if (hasValue(this.component.navigationConstraint)) {
            return await buildDotNetGroundNavigationConstraint(this.component.navigationConstraint, this.viewId);
        }
                               
        return null;
    }

    async setNavigationConstraint(value: any): Promise<void> {
        if (hasValue(value)) {
            this.component.navigationConstraint = await buildJsGroundNavigationConstraint(value, this.layerId, this.viewId);
        }

    }

    async getSurfaceColor(): Promise<any> {
        if (this.component.loadStatus === 'not-loaded') {
            await this.component.load();
        }
        
        
        if (hasValue(this.component.surfaceColor)) {
            return buildDotNetMapColor(this.component.surfaceColor);
        }
                               
        return null;
    }

    async setSurfaceColor(value: any): Promise<void> {
        if (hasValue(value)) {
            this.component.surfaceColor = buildJsMapColor(value);
        }

    }

}


export async function buildJsGroundGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.layers) && dotNetObject.layers!.length > 0) {
        let { buildJsLayer } = await import('./layer');

        properties.layers = await Promise.all(dotNetObject.layers?.map(i => buildJsLayer(i, layerId, viewId)));
    }

    if (hasValue(dotNetObject.navigationConstraint)) {
        properties.navigationConstraint = await buildJsGroundNavigationConstraint(dotNetObject.navigationConstraint, layerId, viewId);
    }

    if (hasValue(dotNetObject.surfaceColor)) {
        properties.surfaceColor = buildJsMapColor(dotNetObject.surfaceColor);
    }


    if (hasValue(dotNetObject.opacity)) {
        properties.opacity = dotNetObject.opacity;
    }

    let jsGround = new Ground(properties);

    let { default: GroundWrapper } = await import('./ground');

    let groundWrapper = new GroundWrapper(jsGround);
    groundWrapper.geoBlazorId = dotNetObject.id;
    groundWrapper.viewId = viewId;
    groundWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = groundWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsGround;
    
    return jsGround;
}


export async function buildDotNetGroundGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetGround: any = {};
    
        if (hasValue(jsObject.navigationConstraint)) {
            dotNetGround.navigationConstraint = await buildDotNetGroundNavigationConstraint(jsObject.navigationConstraint, viewId);
        }

        if (hasValue(jsObject.surfaceColor)) {
            dotNetGround.surfaceColor = buildDotNetMapColor(jsObject.surfaceColor);
        }

    if (hasValue(jsObject.loaded)) {
        dotNetGround.loaded = jsObject.loaded;
    }

    if (hasValue(jsObject.opacity)) {
        dotNetGround.opacity = jsObject.opacity;
    }


    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetGround.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetGround.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for Ground', e);
            }
        }
    }

    if (hasValue(dotNetGround.id)) {
        if (!jsObjectRefs.hasOwnProperty(dotNetGround.id)) {
            let { default: GroundWrapper } = await import('./ground');
            let groundWrapper = new GroundWrapper(jsObject);
            groundWrapper.geoBlazorId = dotNetGround.id;
            groundWrapper.viewId = viewId;
            groundWrapper.layerId = layerId;
            jsObjectRefs[dotNetGround.id] = groundWrapper;
        }
        arcGisObjectRefs[dotNetGround.id] ??= jsObject;
    }
    return dotNetGround;
}


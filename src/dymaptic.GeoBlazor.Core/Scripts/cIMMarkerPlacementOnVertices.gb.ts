// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMMarkerPlacementOnVertices } from './cIMMarkerPlacementOnVertices';

export async function buildJsCIMMarkerPlacementOnVerticesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMMarkerPlacementOnVertices: any = {};

    if (hasValue(dotNetObject.angleToLine)) {
        jsCIMMarkerPlacementOnVertices.angleToLine = dotNetObject.angleToLine;
    }
    if (hasValue(dotNetObject.offset)) {
        jsCIMMarkerPlacementOnVertices.offset = dotNetObject.offset;
    }
    if (hasValue(dotNetObject.placeOnControlPoints)) {
        jsCIMMarkerPlacementOnVertices.placeOnControlPoints = dotNetObject.placeOnControlPoints;
    }
    if (hasValue(dotNetObject.placeOnEndPoints)) {
        jsCIMMarkerPlacementOnVertices.placeOnEndPoints = dotNetObject.placeOnEndPoints;
    }
    if (hasValue(dotNetObject.placeOnRegularVertices)) {
        jsCIMMarkerPlacementOnVertices.placeOnRegularVertices = dotNetObject.placeOnRegularVertices;
    }
    if (hasValue(dotNetObject.placePerPart)) {
        jsCIMMarkerPlacementOnVertices.placePerPart = dotNetObject.placePerPart;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMMarkerPlacementOnVertices.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMMarkerPlacementOnVertices);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMMarkerPlacementOnVertices;
    
    let { buildDotNetCIMMarkerPlacementOnVertices } = await import('./cIMMarkerPlacementOnVertices');
    let dnInstantiatedObject = await buildDotNetCIMMarkerPlacementOnVertices(jsCIMMarkerPlacementOnVertices);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMMarkerPlacementOnVertices detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMMarkerPlacementOnVertices', e);
    }
    
    return jsCIMMarkerPlacementOnVertices;
}


export async function buildDotNetCIMMarkerPlacementOnVerticesGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMMarkerPlacementOnVertices: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.angleToLine)) {
        dotNetCIMMarkerPlacementOnVertices.angleToLine = jsObject.angleToLine;
    }
    if (hasValue(jsObject.offset)) {
        dotNetCIMMarkerPlacementOnVertices.offset = jsObject.offset;
    }
    if (hasValue(jsObject.placeOnControlPoints)) {
        dotNetCIMMarkerPlacementOnVertices.placeOnControlPoints = jsObject.placeOnControlPoints;
    }
    if (hasValue(jsObject.placeOnEndPoints)) {
        dotNetCIMMarkerPlacementOnVertices.placeOnEndPoints = jsObject.placeOnEndPoints;
    }
    if (hasValue(jsObject.placeOnRegularVertices)) {
        dotNetCIMMarkerPlacementOnVertices.placeOnRegularVertices = jsObject.placeOnRegularVertices;
    }
    if (hasValue(jsObject.placePerPart)) {
        dotNetCIMMarkerPlacementOnVertices.placePerPart = jsObject.placePerPart;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMMarkerPlacementOnVertices.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMMarkerPlacementOnVertices.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMMarkerPlacementOnVertices.id = geoBlazorId;
    }

    return dotNetCIMMarkerPlacementOnVertices;
}


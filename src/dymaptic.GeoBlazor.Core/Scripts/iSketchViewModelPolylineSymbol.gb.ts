// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetISketchViewModelPolylineSymbol } from './iSketchViewModelPolylineSymbol';

export async function buildJsISketchViewModelPolylineSymbolGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsSketchViewModelPolylineSymbol: any = {};

    
    let jsObjectRef = DotNet.createJSObjectReference(jsSketchViewModelPolylineSymbol);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsSketchViewModelPolylineSymbol;
    
    let { buildDotNetISketchViewModelPolylineSymbol } = await import('./iSketchViewModelPolylineSymbol');
    let dnInstantiatedObject = await buildDotNetISketchViewModelPolylineSymbol(jsSketchViewModelPolylineSymbol, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type ISketchViewModelPolylineSymbol detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ISketchViewModelPolylineSymbol', e);
    }
    
    return jsSketchViewModelPolylineSymbol;
}


export async function buildDotNetISketchViewModelPolylineSymbolGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsISketchViewModelPolylineSymbol } = await import('./iSketchViewModelPolylineSymbol');
        jsComponentRef = await buildJsISketchViewModelPolylineSymbol(jsObject, layerId, viewId);
    }
    
    let dotNetISketchViewModelPolylineSymbol: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };


    if (hasValue(geoBlazorId)) {
        dotNetISketchViewModelPolylineSymbol.id = geoBlazorId;
    }

    return dotNetISketchViewModelPolylineSymbol;
}


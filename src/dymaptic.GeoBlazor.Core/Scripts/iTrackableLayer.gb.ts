// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import TrackableLayer = __esri.TrackableLayer;
import { hasValue, sanitize } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class ITrackableLayerGenerated implements IPropertyWrapper {
    public layer: TrackableLayer;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(layer: TrackableLayer) {
        this.layer = layer;
    }
    
    // region methods
   
    unwrap() {
        return this.layer;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.trackInfo)) {
            let { buildJsTrackInfo } = await import('./trackInfo');
            this.layer.trackInfo = await buildJsTrackInfo(dotNetObject.trackInfo, this.layerId, this.viewId) as any;
        }

    }
    
    // region properties
    
    async getTrackInfo(): Promise<any> {
        if (!hasValue(this.layer.trackInfo)) {
            return null;
        }
        
        let { buildDotNetTrackInfo } = await import('./trackInfo');
        return await buildDotNetTrackInfo(this.layer.trackInfo);
    }
    
    async setTrackInfo(value: any): Promise<void> {
        let { buildJsTrackInfo } = await import('./trackInfo');
        this.layer.trackInfo = await  buildJsTrackInfo(value, this.layerId, this.viewId);
    }
    
    getProperty(prop: string): any {
        return this.layer[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.layer[prop] = value;
    }
}

export function buildJsITrackableLayerGenerated(dotNetObject: any): any {
    return sanitize(dotNetObject);
}

export function buildDotNetITrackableLayerGenerated(jsObject: any): any {
    return jsObject;
}


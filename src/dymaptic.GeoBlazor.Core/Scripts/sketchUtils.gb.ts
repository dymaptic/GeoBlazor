// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file

import sketchUtils = __esri.sketchUtils;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class SketchUtilsGenerated implements IPropertyWrapper {
    public component: sketchUtils;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: sketchUtils) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async hideAllVisibleElements(sketchViewModel: any): Promise<void> {
        let { buildJsSketchViewModel } = await import('./sketchViewModel');
        let jsSketchViewModel = await buildJsSketchViewModel(sketchViewModel, this.layerId, this.viewId) as any;
        this.component.hideAllVisibleElements(jsSketchViewModel);
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsSketchUtilsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jssketchUtils: any = {}


    let { default: SketchUtilsWrapper } = await import('./sketchUtils');
    let sketchUtilsWrapper = new SketchUtilsWrapper(jssketchUtils);
    sketchUtilsWrapper.geoBlazorId = dotNetObject.id;
    sketchUtilsWrapper.viewId = viewId;
    sketchUtilsWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(sketchUtilsWrapper);
    jsObjectRefs[dotNetObject.id] = sketchUtilsWrapper;
    arcGisObjectRefs[dotNetObject.id] = jssketchUtils;
    let { buildDotNetSketchUtils } = await import('./sketchUtils');
    let dnInstantiatedObject = await buildDotNetSketchUtils(jssketchUtils);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for SketchUtils', e);
    }
    
    return jssketchUtils;
}
export async function buildDotNetSketchUtilsGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSketchUtils: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetSketchUtils.id = k;
                break;
            }
        }
    }

    return dotNetSketchUtils;
}


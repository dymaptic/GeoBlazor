// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectTaperedPolygon } from './cIMGeometricEffectTaperedPolygon';

export async function buildJsCIMGeometricEffectTaperedPolygonGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectTaperedPolygon: any = {};

    if (hasValue(dotNetObject.fromWidth)) {
        jsCIMGeometricEffectTaperedPolygon.fromWidth = dotNetObject.fromWidth;
    }
    if (hasValue(dotNetObject.length)) {
        jsCIMGeometricEffectTaperedPolygon.length = dotNetObject.length;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectTaperedPolygon.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.toWidth)) {
        jsCIMGeometricEffectTaperedPolygon.toWidth = dotNetObject.toWidth;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectTaperedPolygon);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectTaperedPolygon;
    
    let { buildDotNetCIMGeometricEffectTaperedPolygon } = await import('./cIMGeometricEffectTaperedPolygon');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectTaperedPolygon(jsCIMGeometricEffectTaperedPolygon);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectTaperedPolygon detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectTaperedPolygon', e);
    }
    
    return jsCIMGeometricEffectTaperedPolygon;
}


export async function buildDotNetCIMGeometricEffectTaperedPolygonGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectTaperedPolygon: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.fromWidth)) {
        dotNetCIMGeometricEffectTaperedPolygon.fromWidth = jsObject.fromWidth;
    }
    if (hasValue(jsObject.length)) {
        dotNetCIMGeometricEffectTaperedPolygon.length = jsObject.length;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectTaperedPolygon.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.toWidth)) {
        dotNetCIMGeometricEffectTaperedPolygon.toWidth = jsObject.toWidth;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectTaperedPolygon.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectTaperedPolygon.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectTaperedPolygon;
}


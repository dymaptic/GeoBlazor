// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import DirectionLine from '@arcgis/core/rest/support/DirectionLine';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class DirectionLineGenerated implements IPropertyWrapper {
    public component: DirectionLine;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: DirectionLine) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async toGraphic(): Promise<any> {
        return this.component.toGraphic();
    }

    // region properties
    
    async getGeometry(): Promise<any> {
        if (!hasValue(this.component.geometry)) {
            return null;
        }
        
        let { buildDotNetPolyline } = await import('./polyline');
        return buildDotNetPolyline(this.component.geometry);
    }
    async setGeometry(value: any): Promise<void> {
        let { buildJsPolyline } = await import('./polyline');
        this.component.geometry =  buildJsPolyline(value);
    }
    async getPopupTemplate(): Promise<any> {
        if (!hasValue(this.component.popupTemplate)) {
            return null;
        }
        
        let { buildDotNetPopupTemplate } = await import('./popupTemplate');
        return await buildDotNetPopupTemplate(this.component.popupTemplate);
    }
    async setPopupTemplate(value: any): Promise<void> {
        let { buildJsPopupTemplate } = await import('./popupTemplate');
        this.component.popupTemplate =  buildJsPopupTemplate(value, this.layerId, this.viewId);
    }
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsDirectionLineGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};
    if (hasValue(dotNetObject.geometry)) {
        let { buildJsPolyline } = await import('./polyline');
        properties.geometry = buildJsPolyline(dotNetObject.geometry) as any;
    }
    if (hasValue(dotNetObject.popupTemplate)) {
        let { buildJsPopupTemplate } = await import('./popupTemplate');
        properties.popupTemplate = buildJsPopupTemplate(dotNetObject.popupTemplate, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.directionLineType)) {
        properties.directionLineType = dotNetObject.directionLineType;
    }
    if (hasValue(dotNetObject.distance)) {
        properties.distance = dotNetObject.distance;
    }
    if (hasValue(dotNetObject.duration)) {
        properties.duration = dotNetObject.duration;
    }
    let jsDirectionLine = new DirectionLine(properties);

    let { default: DirectionLineWrapper } = await import('./directionLine');
    let directionLineWrapper = new DirectionLineWrapper(jsDirectionLine);
    directionLineWrapper.geoBlazorId = dotNetObject.id;
    directionLineWrapper.viewId = viewId;
    directionLineWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(directionLineWrapper);
    jsObjectRefs[dotNetObject.id] = directionLineWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsDirectionLine;
    let { buildDotNetDirectionLine } = await import('./directionLine');
    let dnInstantiatedObject = await buildDotNetDirectionLine(jsDirectionLine);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for DirectionLine', e);
    }
    
    return jsDirectionLine;
}

export async function buildDotNetDirectionLineGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetDirectionLine: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.geometry)) {
            let { buildDotNetPolyline } = await import('./polyline');
            dotNetDirectionLine.geometry = buildDotNetPolyline(jsObject.geometry);
        }
        if (hasValue(jsObject.popupTemplate)) {
            let { buildDotNetPopupTemplate } = await import('./popupTemplate');
            dotNetDirectionLine.popupTemplate = await buildDotNetPopupTemplate(jsObject.popupTemplate);
        }
    if (hasValue(jsObject.directionLineType)) {
        dotNetDirectionLine.directionLineType = jsObject.directionLineType;
    }
    if (hasValue(jsObject.distance)) {
        dotNetDirectionLine.distance = jsObject.distance;
    }
    if (hasValue(jsObject.duration)) {
        dotNetDirectionLine.duration = jsObject.duration;
    }
    if (hasValue(jsObject.type)) {
        dotNetDirectionLine.type = jsObject.type;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetDirectionLine.id = k;
                break;
            }
        }
    }

    return dotNetDirectionLine;
}


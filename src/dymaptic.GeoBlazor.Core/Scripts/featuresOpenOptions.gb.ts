// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetFeaturesOpenOptions } from './featuresOpenOptions';
import FeaturesOpenOptions = __esri.FeaturesOpenOptions;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsFeaturesOpenOptionsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsFeaturesOpenOptions: any = {}
    if (hasValue(dotNetObject.features)) {
        let { buildJsGraphic } = await import('./graphic');
        jsFeaturesOpenOptions.features = dotNetObject.features.map(i => buildJsGraphic(i)) as any;
    }
    if (hasValue(dotNetObject.location)) {
        let { buildJsPoint } = await import('./point');
        jsFeaturesOpenOptions.location = buildJsPoint(dotNetObject.location) as any;
    }

    if (hasValue(dotNetObject.actions)) {
        jsFeaturesOpenOptions.actions = dotNetObject.actions;
    }
    if (hasValue(dotNetObject.collapsed)) {
        jsFeaturesOpenOptions.collapsed = dotNetObject.collapsed;
    }
    if (hasValue(dotNetObject.content)) {
        jsFeaturesOpenOptions.content = dotNetObject.content;
    }
    if (hasValue(dotNetObject.featureMenuOpen)) {
        jsFeaturesOpenOptions.featureMenuOpen = dotNetObject.featureMenuOpen;
    }
    if (hasValue(dotNetObject.fetchFeatures)) {
        jsFeaturesOpenOptions.fetchFeatures = dotNetObject.fetchFeatures;
    }
    if (hasValue(dotNetObject.promises)) {
        jsFeaturesOpenOptions.promises = dotNetObject.promises;
    }
    if (hasValue(dotNetObject.title)) {
        jsFeaturesOpenOptions.title = dotNetObject.title;
    }
    if (hasValue(dotNetObject.updateLocationEnabled)) {
        jsFeaturesOpenOptions.updateLocationEnabled = dotNetObject.updateLocationEnabled;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsFeaturesOpenOptions);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsFeaturesOpenOptions;
    
    let dnInstantiatedObject = await buildDotNetFeaturesOpenOptions(jsFeaturesOpenOptions);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for FeaturesOpenOptions', e);
    }
    
    return jsFeaturesOpenOptions;
}

export async function buildDotNetFeaturesOpenOptionsGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFeaturesOpenOptions: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.features)) {
            let { buildDotNetGraphic } = await import('./graphic');
            dotNetFeaturesOpenOptions.features = jsObject.features.map(i => buildDotNetGraphic(i, layerId, viewId));
        }
        if (hasValue(jsObject.location)) {
            let { buildDotNetPoint } = await import('./point');
            dotNetFeaturesOpenOptions.location = buildDotNetPoint(jsObject.location);
        }
        if (hasValue(jsObject.actions)) {
            dotNetFeaturesOpenOptions.actions = jsObject.actions;
        }
        if (hasValue(jsObject.collapsed)) {
            dotNetFeaturesOpenOptions.collapsed = jsObject.collapsed;
        }
        if (hasValue(jsObject.content)) {
            dotNetFeaturesOpenOptions.content = jsObject.content;
        }
        if (hasValue(jsObject.featureMenuOpen)) {
            dotNetFeaturesOpenOptions.featureMenuOpen = jsObject.featureMenuOpen;
        }
        if (hasValue(jsObject.fetchFeatures)) {
            dotNetFeaturesOpenOptions.fetchFeatures = jsObject.fetchFeatures;
        }
        if (hasValue(jsObject.promises)) {
            dotNetFeaturesOpenOptions.promises = jsObject.promises;
        }
        if (hasValue(jsObject.title)) {
            dotNetFeaturesOpenOptions.title = jsObject.title;
        }
        if (hasValue(jsObject.updateLocationEnabled)) {
            dotNetFeaturesOpenOptions.updateLocationEnabled = jsObject.updateLocationEnabled;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetFeaturesOpenOptions.id = k;
                break;
            }
        }
    }

    return dotNetFeaturesOpenOptions;
}


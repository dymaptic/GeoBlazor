import { buildDotNetCustomTemplate } from './customTemplate';
// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file
import CustomTemplate from '@arcgis/core/widgets/Print/CustomTemplate';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
export async function buildJsCustomTemplateGenerated(dotNetObject: any): Promise<any> {
    let jsCustomTemplate = new CustomTemplate();
    if (hasValue(dotNetObject.format)) {
        let { buildJsFormat } = await import('./format');
        jsCustomTemplate.format = await buildJsFormat(dotNetObject.format, layerId, viewId) as any;
    }

    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsCustomTemplate);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCustomTemplate;
    
    let dnInstantiatedObject = await buildDotNetCustomTemplate(jsCustomTemplate);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CustomTemplate', e);
    }
    
    return jsCustomTemplate;
}
export async function buildDotNetCustomTemplateGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCustomTemplate: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.format)) {
            let { buildDotNetFormat } = await import('./format');
            dotNetCustomTemplate.format = await buildDotNetFormat(jsObject.format);
        }
        if (hasValue(jsObject.description)) {
            dotNetCustomTemplate.description = jsObject.description;
        }
        if (hasValue(jsObject.label)) {
            dotNetCustomTemplate.label = jsObject.label;
        }
        if (hasValue(jsObject.layout)) {
            dotNetCustomTemplate.layout = jsObject.layout;
        }
        if (hasValue(jsObject.layoutOptions)) {
            dotNetCustomTemplate.layoutOptions = jsObject.layoutOptions;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetCustomTemplate.id = k;
                break;
            }
        }
    }

    return dotNetCustomTemplate;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetMapViewScreenPoint } from './mapViewScreenPoint';

export async function buildJsMapViewScreenPointGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsMapViewScreenPoint: any = {};

    if (hasValue(dotNetObject.x)) {
        jsMapViewScreenPoint.x = dotNetObject.x;
    }
    if (hasValue(dotNetObject.y)) {
        jsMapViewScreenPoint.y = dotNetObject.y;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsMapViewScreenPoint);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsMapViewScreenPoint;
    
    let { buildDotNetMapViewScreenPoint } = await import('./mapViewScreenPoint');
    let dnInstantiatedObject = await buildDotNetMapViewScreenPoint(jsMapViewScreenPoint, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type MapViewScreenPoint detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for MapViewScreenPoint', e);
    }
    
    return jsMapViewScreenPoint;
}


export async function buildDotNetMapViewScreenPointGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsMapViewScreenPoint } = await import('./mapViewScreenPoint');
        jsComponentRef = await buildJsMapViewScreenPoint(jsObject, layerId, viewId);
    }
    
    let dotNetMapViewScreenPoint: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.x)) {
        dotNetMapViewScreenPoint.x = jsObject.x;
    }
    if (hasValue(jsObject.y)) {
        dotNetMapViewScreenPoint.y = jsObject.y;
    }


    if (hasValue(geoBlazorId)) {
        dotNetMapViewScreenPoint.id = geoBlazorId;
    }

    return dotNetMapViewScreenPoint;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import MeshMaterial from '@arcgis/core/geometry/support/MeshMaterial';
import { hasValue, sanitize } from './geoBlazorCore';
export function buildJsMeshMaterialGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): any {
    if (!hasValue(dotNetObject)) {
        return null;
    }
    
    if (Object.hasOwn(dotNetObject, "alphaCutoff")) {
        let { buildJsMeshMaterial } = await import('./meshMaterial');
        return await buildJsMeshMaterial(dotNetObject, layerId, viewId);
    }

    if (Object.hasOwn(dotNetObject, "metallic")) {
        let { buildJsMeshMaterialMetallicRoughness } = await import('./meshMaterialMetallicRoughness');
        return await buildJsMeshMaterialMetallicRoughness(dotNetObject, layerId, viewId);
    }

    return sanitize(dotNetObject);
}
export function buildDotNetMeshMaterialGenerated(jsObject: any, viewId: string | null): any {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    if (Object.hasOwn(jsObject, "alphaCutoff")) {
        let { buildDotNetMeshMaterial } = await import('./meshMaterial');
        return await buildDotNetMeshMaterial(jsObject, viewId);
    }

    if (Object.hasOwn(jsObject, "metallic")) {
        let { buildDotNetMeshMaterialMetallicRoughness } = await import('./meshMaterialMetallicRoughness');
        return await buildDotNetMeshMaterialMetallicRoughness(jsObject, viewId);
    }

    return sanitize(jsObject);
}

// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import FillSymbol from '@arcgis/core/symbols/FillSymbol';
import { hasValue, sanitize, removeCircularReferences } from './arcGisJsInterop';
export async function buildJsFillSymbolGenerated(dotNetObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }
    
    switch (dotNetObject.type) {
        case 'simple-fill': 
            let { buildJsSimpleFillSymbol } = await import('./simpleFillSymbol');
            return await buildJsSimpleFillSymbol(dotNetObject, viewId);
        case 'picture-fill': 
            let { buildJsPictureFillSymbol } = await import('./pictureFillSymbol');
            return await buildJsPictureFillSymbol(dotNetObject, viewId);
        default: 
            return sanitize(dotNetObject);
    }
}     

export async function buildDotNetFillSymbolGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    switch (jsObject.type) {
        case 'simple-fill': 
            let { buildDotNetSimpleFillSymbol } = await import('./simpleFillSymbol');
            return await buildDotNetSimpleFillSymbol(jsObject, viewId);
        case 'picture-fill': 
            let { buildDotNetPictureFillSymbol } = await import('./pictureFillSymbol');
            return await buildDotNetPictureFillSymbol(jsObject, viewId);
        default: 
            return removeCircularReferences(jsObject);
    }
}     


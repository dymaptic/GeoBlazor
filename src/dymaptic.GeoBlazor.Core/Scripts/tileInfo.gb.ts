// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import TileInfo from '@arcgis/core/layers/support/TileInfo';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class TileInfoGenerated implements IPropertyWrapper {
    public component: TileInfo;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: TileInfo) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.lods) && dotNetObject.lods.length > 0) {
            let { buildJsLOD } = await import('./lOD');
            this.component.lods = await Promise.all(dotNetObject.lods.map(async i => await buildJsLOD(i, this.viewId))) as any;
        }
        if (hasValue(dotNetObject.origin)) {
            let { buildJsPoint } = await import('./point');
            this.component.origin = buildJsPoint(dotNetObject.origin, this.viewId) as any;
        }
        if (hasValue(dotNetObject.spatialReference)) {
            let { buildJsSpatialReference } = await import('./spatialReference');
            this.component.spatialReference = buildJsSpatialReference(dotNetObject.spatialReference, this.viewId) as any;
        }

        if (hasValue(dotNetObject.dpi)) {
            this.component.dpi = dotNetObject.dpi;
        }
        if (hasValue(dotNetObject.format)) {
            this.component.format = dotNetObject.format;
        }
        if (hasValue(dotNetObject.size) && dotNetObject.size.length > 0) {
            this.component.size = dotNetObject.size;
        }
    }
    
    async scaleToZoom(scale: any): Promise<any> {
        return this.component.scaleToZoom(scale);
    }

    async zoomToScale(zoom: any): Promise<any> {
        return this.component.zoomToScale(zoom);
    }

    // region properties
    
    async getLods(): Promise<any> {
        if (!hasValue(this.component.lods)) {
            return null;
        }
        
        let { buildDotNetLOD } = await import('./lOD');
        return await Promise.all(this.component.lods!.map(async i => await buildDotNetLOD(i, this.viewId)));
    }
    
    async setLods(value: any): Promise<void> {
        if (!hasValue(value)) {
            this.component.lods.removeAll();
        }
        let { buildJsLOD } = await import('./lOD');
        this.component.lods = await Promise.all(value.map(async i => await buildJsLOD(i, this.viewId))) as any;
    }
    
    async getOrigin(): Promise<any> {
        if (!hasValue(this.component.origin)) {
            return null;
        }
        
        let { buildDotNetPoint } = await import('./point');
        return buildDotNetPoint(this.component.origin, this.viewId);
    }
    
    async setOrigin(value: any): Promise<void> {
        let { buildJsPoint } = await import('./point');
        this.component.origin =  buildJsPoint(value, this.viewId);
    }
    
    async getSpatialReference(): Promise<any> {
        if (!hasValue(this.component.spatialReference)) {
            return null;
        }
        
        let { buildDotNetSpatialReference } = await import('./spatialReference');
        return buildDotNetSpatialReference(this.component.spatialReference, this.viewId);
    }
    
    async setSpatialReference(value: any): Promise<void> {
        let { buildJsSpatialReference } = await import('./spatialReference');
        this.component.spatialReference =  buildJsSpatialReference(value, this.viewId);
    }
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsTileInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.lods) && dotNetObject.lods.length > 0) {
        let { buildJsLOD } = await import('./lOD');
        properties.lods = await Promise.all(dotNetObject.lods.map(async i => await buildJsLOD(i, viewId))) as any;
    }
    if (hasValue(dotNetObject.origin)) {
        let { buildJsPoint } = await import('./point');
        properties.origin = buildJsPoint(dotNetObject.origin, viewId) as any;
    }
    if (hasValue(dotNetObject.spatialReference)) {
        let { buildJsSpatialReference } = await import('./spatialReference');
        properties.spatialReference = buildJsSpatialReference(dotNetObject.spatialReference, viewId) as any;
    }

    if (hasValue(dotNetObject.dpi)) {
        properties.dpi = dotNetObject.dpi;
    }
    if (hasValue(dotNetObject.format)) {
        properties.format = dotNetObject.format;
    }
    if (hasValue(dotNetObject.size) && dotNetObject.size.length > 0) {
        properties.size = dotNetObject.size;
    }
    let jsTileInfo = new TileInfo(properties);

    let { default: TileInfoWrapper } = await import('./tileInfo');
    let tileInfoWrapper = new TileInfoWrapper(jsTileInfo);
    tileInfoWrapper.geoBlazorId = dotNetObject.id;
    tileInfoWrapper.viewId = viewId;
    tileInfoWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = tileInfoWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsTileInfo;
    
    return jsTileInfo;
}


export async function buildDotNetTileInfoGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTileInfo: any = {};
    
    if (hasValue(jsObject.lods)) {
        let { buildDotNetLOD } = await import('./lOD');
        dotNetTileInfo.lods = await Promise.all(jsObject.lods.map(async i => await buildDotNetLOD(i, viewId)));
    }
    
    if (hasValue(jsObject.origin)) {
        let { buildDotNetPoint } = await import('./point');
        dotNetTileInfo.origin = buildDotNetPoint(jsObject.origin, viewId);
    }
    
    if (hasValue(jsObject.spatialReference)) {
        let { buildDotNetSpatialReference } = await import('./spatialReference');
        dotNetTileInfo.spatialReference = buildDotNetSpatialReference(jsObject.spatialReference, viewId);
    }
    
    if (hasValue(jsObject.dpi)) {
        dotNetTileInfo.dpi = jsObject.dpi;
    }
    
    if (hasValue(jsObject.format)) {
        dotNetTileInfo.format = removeCircularReferences(jsObject.format);
    }
    
    if (hasValue(jsObject.isWrappable)) {
        dotNetTileInfo.isWrappable = jsObject.isWrappable;
    }
    
    if (hasValue(jsObject.size)) {
        dotNetTileInfo.size = jsObject.size;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetTileInfo.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        dotNetTileInfo.id = await dotNetRef.invokeMethodAsync('GetId');
    }
    if (hasValue(dotNetTileInfo.id)) {
        jsObjectRefs[dotNetTileInfo.id] ??= jsObject;
        arcGisObjectRefs[dotNetTileInfo.id] ??= jsObject;
    }

    return dotNetTileInfo;
}


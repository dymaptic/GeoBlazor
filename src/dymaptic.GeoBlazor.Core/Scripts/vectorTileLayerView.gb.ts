// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import VectorTileLayerView = __esri.VectorTileLayerView;
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, generateSerializableJson } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class VectorTileLayerViewGenerated implements IPropertyWrapper {
    public component: VectorTileLayerView;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: VectorTileLayerView) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {

        if (hasValue(dotNetObject.visible)) {
            this.component.visible = dotNetObject.visible;
        }
    }
    
    async isFulfilled(): Promise<any> {
        return this.component.isFulfilled();
    }

    async isRejected(): Promise<any> {
        return this.component.isRejected();
    }

    async isResolved(): Promise<any> {
        return this.component.isResolved();
    }

    async when(callback: any,
        errback: any): Promise<any> {
        let result = await this.component.when(callback,
            errback);
        
        return generateSerializableJson(result);
    }

    // region properties
    
    async getLayer(): Promise<any> {
        if (!hasValue(this.component.layer)) {
            return null;
        }
        
        let { buildDotNetLayer } = await import('./layer');
        return await buildDotNetLayer(this.component.layer, this.viewId);
    }
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsVectorTileLayerViewGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.visible)) {
        properties.visible = dotNetObject.visible;
    }
    
    // @ts-ignore
    let jsVectorTileLayerView = new Object(properties);

    let { default: VectorTileLayerViewWrapper } = await import('./vectorTileLayerView');
    let vectorTileLayerViewWrapper = new VectorTileLayerViewWrapper(jsVectorTileLayerView);
    vectorTileLayerViewWrapper.geoBlazorId = dotNetObject.id;
    vectorTileLayerViewWrapper.viewId = viewId;
    vectorTileLayerViewWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = vectorTileLayerViewWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsVectorTileLayerView;
    
    return jsVectorTileLayerView;
}


export async function buildDotNetVectorTileLayerViewGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetVectorTileLayerView: any = {};
    
    if (hasValue(jsObject.hasVisibleFeatures)) {
        dotNetVectorTileLayerView.hasVisibleFeatures = jsObject.hasVisibleFeatures;
    }
    
    if (hasValue(jsObject.spatialReferenceSupported)) {
        dotNetVectorTileLayerView.spatialReferenceSupported = jsObject.spatialReferenceSupported;
    }
    
    if (hasValue(jsObject.suspended)) {
        dotNetVectorTileLayerView.suspended = jsObject.suspended;
    }
    
    if (hasValue(jsObject.updating)) {
        dotNetVectorTileLayerView.updating = jsObject.updating;
    }
    
    if (hasValue(jsObject.visible)) {
        dotNetVectorTileLayerView.visible = jsObject.visible;
    }
    
    if (hasValue(jsObject.visibleAtCurrentScale)) {
        dotNetVectorTileLayerView.visibleAtCurrentScale = jsObject.visibleAtCurrentScale;
    }
    
    if (hasValue(jsObject.visibleAtCurrentTimeExtent)) {
        dotNetVectorTileLayerView.visibleAtCurrentTimeExtent = jsObject.visibleAtCurrentTimeExtent;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetVectorTileLayerView.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetVectorTileLayerView.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for VectorTileLayerView', e);
            }
        }
    }
    if (hasValue(dotNetVectorTileLayerView.id)) {
        jsObjectRefs[dotNetVectorTileLayerView.id] ??= jsObject;
        arcGisObjectRefs[dotNetVectorTileLayerView.id] ??= jsObject;
    }

    return dotNetVectorTileLayerView;
}


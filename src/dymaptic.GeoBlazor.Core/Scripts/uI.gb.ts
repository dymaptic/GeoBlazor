// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import UI from '@arcgis/core/views/ui/UI';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class UIGenerated implements IPropertyWrapper {
    public component: UI;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: UI) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async add(component: any,
        position: any): Promise<void> {
        let { buildJsWidget } = await import('./widget');
        let jsComponent = await buildJsWidget(component, this.layerId, this.viewId) as any;
        let { buildJsUIAddPosition } = await import('./uIAddPosition');
        let jsPosition = await buildJsUIAddPosition(position, this.layerId, this.viewId) as any;
        this.component.add(jsComponent,
            jsPosition);
    }

    async find(id: any): Promise<any> {
        let result = this.component.find(id);
        let { buildDotNetWidget } = await import('./widget');
        return await buildDotNetWidget(result);
    }

    async getComponents(position: any): Promise<any> {
        let { buildJsUIAddPosition } = await import('./uIAddPosition');
        let jsPosition = await buildJsUIAddPosition(position, this.layerId, this.viewId) as any;
        let result = this.component.getComponents(jsPosition);
        let { buildDotNetWidget } = await import('./widget');
        return await Promise.all(result.map(async i => await buildDotNetWidget(i)));
    }

    async move(component: any,
        position: any): Promise<void> {
        let { buildJsWidget } = await import('./widget');
        let jsComponent = await buildJsWidget(component, this.layerId, this.viewId) as any;
        let { buildJsUIAddPosition } = await import('./uIAddPosition');
        let jsPosition = await buildJsUIAddPosition(position, this.layerId, this.viewId) as any;
        this.component.move(jsComponent,
            jsPosition);
    }

    async remove(component: any): Promise<void> {
        let { buildJsWidget } = await import('./widget');
        let jsComponent = await buildJsWidget(component, this.layerId, this.viewId) as any;
        this.component.remove(jsComponent);
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsUIGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};
    if (hasValue(viewId)) {
        properties.view = arcGisObjectRefs[viewId!];
    }

    if (hasValue(dotNetObject.container)) {
        properties.container = dotNetObject.container;
    }
    if (hasValue(dotNetObject.padding)) {
        properties.padding = dotNetObject.padding;
    }
    let jsUI = new UI(properties);

    let { default: UIWrapper } = await import('./uI');
    let uIWrapper = new UIWrapper(jsUI);
    uIWrapper.geoBlazorId = dotNetObject.id;
    uIWrapper.viewId = viewId;
    uIWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(uIWrapper);
    jsObjectRefs[dotNetObject.id] = uIWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsUI;
    let { buildDotNetUI } = await import('./uI');
    let dnInstantiatedObject = await buildDotNetUI(jsUI);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for UI', e);
    }
    
    return jsUI;
}

export async function buildDotNetUIGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetUI: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.container)) {
        dotNetUI.container = jsObject.container;
    }
    if (hasValue(jsObject.height)) {
        dotNetUI.height = jsObject.height;
    }
    if (hasValue(jsObject.padding)) {
        dotNetUI.padding = jsObject.padding;
    }
    if (hasValue(jsObject.width)) {
        dotNetUI.width = jsObject.width;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetUI.id = k;
                break;
            }
        }
    }

    return dotNetUI;
}


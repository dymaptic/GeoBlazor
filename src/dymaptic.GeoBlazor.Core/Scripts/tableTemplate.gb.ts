// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import TableTemplate from '@arcgis/core/widgets/FeatureTable/support/TableTemplate';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetTableTemplate } from './tableTemplate';

export async function buildJsTableTemplateGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};
    if (hasValue(dotNetObject.columnTemplates)) {
        let { buildJsColumnTemplateBase } = await import('./columnTemplateBase');
        properties.columnTemplates = await Promise.all(dotNetObject.columnTemplates.map(async i => await buildJsColumnTemplateBase(i, layerId, viewId))) as any;
    }

    let jsTableTemplate = new TableTemplate(properties);
    
    let jsObjectRef = DotNet.createJSObjectReference(jsTableTemplate);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsTableTemplate;
    
    let { buildDotNetTableTemplate } = await import('./tableTemplate');
    let dnInstantiatedObject = await buildDotNetTableTemplate(jsTableTemplate);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_')) {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type TableTemplate detected at path: ${key}, value: ${value.__proto__?.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for TableTemplate', e);
    }
    
    return jsTableTemplate;
}


export async function buildDotNetTableTemplateGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTableTemplate: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.columnTemplates)) {
        let { buildDotNetColumnTemplateBase } = await import('./columnTemplateBase');
        dotNetTableTemplate.columnTemplates = await Promise.all(jsObject.columnTemplates.map(async i => await buildDotNetColumnTemplateBase(i)));
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetTableTemplate.id = k;
                break;
            }
        }
    }

    return dotNetTableTemplate;
}


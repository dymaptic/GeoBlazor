// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import DatePickerInput from '@arcgis/core/form/elements/inputs/DatePickerInput';
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetDatePickerInput } from './datePickerInput';

export async function buildJsDatePickerInputGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};

    if (hasValue(dotNetObject.max)) {
        properties.max = dotNetObject.max;
    }
    if (hasValue(dotNetObject.min)) {
        properties.min = dotNetObject.min;
    }
    let jsDatePickerInput = new DatePickerInput(properties);
    
    let jsObjectRef = DotNet.createJSObjectReference(jsDatePickerInput);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsDatePickerInput;
    
    let { buildDotNetDatePickerInput } = await import('./datePickerInput');
    let dnInstantiatedObject = await buildDotNetDatePickerInput(jsDatePickerInput, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type DatePickerInput detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for DatePickerInput', e);
    }
    
    return jsDatePickerInput;
}


export async function buildDotNetDatePickerInputGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsDatePickerInput } = await import('./datePickerInput');
        jsComponentRef = await buildJsDatePickerInput(jsObject, layerId, viewId);
    }
    
    let dotNetDatePickerInput: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.max)) {
        dotNetDatePickerInput.max = jsObject.max;
    }
    if (hasValue(jsObject.min)) {
        dotNetDatePickerInput.min = jsObject.min;
    }
    if (hasValue(jsObject.type)) {
        dotNetDatePickerInput.type = jsObject.type;
    }


    if (hasValue(geoBlazorId)) {
        dotNetDatePickerInput.id = geoBlazorId;
    }

    return dotNetDatePickerInput;
}


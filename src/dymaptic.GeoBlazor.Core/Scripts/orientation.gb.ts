// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file


import Orientation = __esri.Orientation;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class OrientationGenerated implements IPropertyWrapper {
    public component: Orientation;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: Orientation) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsOrientationGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsOrientation: any = {}

    if (hasValue(dotNetObject.z)) {
        jsOrientation.z = dotNetObject.z;
    }
    let { default: OrientationWrapper } = await import('./orientation');
    let orientationWrapper = new OrientationWrapper(jsOrientation);
    orientationWrapper.geoBlazorId = dotNetObject.id;
    orientationWrapper.viewId = viewId;
    orientationWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(orientationWrapper);
    jsObjectRefs[dotNetObject.id] = orientationWrapper;

    arcGisObjectRefs[dotNetObject.id] = jsOrientation;
    let { buildDotNetOrientation } = await import('./orientation');
    let dnInstantiatedObject = await buildDotNetOrientation(jsOrientation, layerId, viewId);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Orientation', e);
    }
    
    return jsOrientation;
}

export async function buildDotNetOrientationGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetOrientation: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.z)) {
            dotNetOrientation.z = jsObject.z;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetOrientation.id = k;
                break;
            }
        }
    }

    return dotNetOrientation;
}


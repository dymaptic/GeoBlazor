import { buildDotNetLineCallout3DBorder } from './lineCallout3DBorder';
// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file
import LineCallout3DBorder = __esri.LineCallout3DBorder;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
export async function buildJsLineCallout3DBorderGenerated(dotNetObject: any): Promise<any> {
    let jsLineCallout3DBorder: any = {}
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsLineCallout3DBorder.color = buildJsMapColor(dotNetObject.color) as any;
    }

    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsLineCallout3DBorder);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsLineCallout3DBorder;
    
    let dnInstantiatedObject = await buildDotNetLineCallout3DBorder(jsLineCallout3DBorder);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for LineCallout3DBorder', e);
    }
    
    return jsLineCallout3DBorder;
}
export async function buildDotNetLineCallout3DBorderGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetLineCallout3DBorder: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetLineCallout3DBorder.color = buildDotNetMapColor(jsObject.color);
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetLineCallout3DBorder.id = k;
                break;
            }
        }
    }

    return dotNetLineCallout3DBorder;
}


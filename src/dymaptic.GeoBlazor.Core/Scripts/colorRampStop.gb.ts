// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';

export async function buildJsColorRampStopGenerated(dotNetObject: any): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsColorRampStop: any = {};
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsColorRampStop.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.label)) {
        jsColorRampStop.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.value)) {
        jsColorRampStop.value = dotNetObject.value;
    }
    
    jsObjectRefs[dotNetObject.id] = jsColorRampStop;
    arcGisObjectRefs[dotNetObject.id] = jsColorRampStop;
    
    return jsColorRampStop;
}


export async function buildDotNetColorRampStopGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetColorRampStop: any = {};
    
    if (hasValue(jsObject.color)) {
        let { buildDotNetMapColor } = await import('./mapColor');
        dotNetColorRampStop.color = buildDotNetMapColor(jsObject.color);
    }
    
    if (hasValue(jsObject.label)) {
        dotNetColorRampStop.label = jsObject.label;
    }
    
    if (hasValue(jsObject.value)) {
        dotNetColorRampStop.value = jsObject.value;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetColorRampStop.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        dotNetColorRampStop.id = await dotNetRef.invokeMethodAsync('GetId');
    }
    if (hasValue(dotNetColorRampStop.id)) {
        jsObjectRefs[dotNetColorRampStop.id] ??= jsObject;
        arcGisObjectRefs[dotNetColorRampStop.id] ??= jsObject;
    }

    return dotNetColorRampStop;
}


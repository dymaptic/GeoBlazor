// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';


export async function buildJsWebSceneWidgetsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsWebSceneWidgets: any = {};
    if (hasValue(dotNetObject.timeSlider)) {
        let { buildJsWebDocTimeSlider } = await import('./webDocTimeSlider');
        jsWebSceneWidgets.timeSlider = await buildJsWebDocTimeSlider(dotNetObject.timeSlider, layerId, viewId) as any;
    }


    let { default: WebSceneWidgetsWrapper } = await import('./webSceneWidgets');

    let webSceneWidgetsWrapper = new WebSceneWidgetsWrapper(jsWebSceneWidgets);
    webSceneWidgetsWrapper.geoBlazorId = dotNetObject.id;
    webSceneWidgetsWrapper.viewId = viewId;
    webSceneWidgetsWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = webSceneWidgetsWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsWebSceneWidgets;
    
    return jsWebSceneWidgets;
}


export async function buildDotNetWebSceneWidgetsGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetWebSceneWidgets: any = {};
    
    if (hasValue(jsObject.timeSlider)) {
        let { buildDotNetWebDocTimeSlider } = await import('./webDocTimeSlider');
        dotNetWebSceneWidgets.timeSlider = await buildDotNetWebDocTimeSlider(jsObject.timeSlider, viewId);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetWebSceneWidgets.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetWebSceneWidgets.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for WebSceneWidgets', e);
            }
        }
    }
    if (hasValue(dotNetWebSceneWidgets.id)) {
        jsObjectRefs[dotNetWebSceneWidgets.id] ??= jsObject;
        arcGisObjectRefs[dotNetWebSceneWidgets.id] ??= jsObject;
    }

    return dotNetWebSceneWidgets;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import Analysis from '@arcgis/core/analysis/Analysis';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './geoBlazorCore';

export async function buildJsAnalysisGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.analysisId)) {
        properties.id = dotNetObject.analysisId;
    }
    if (hasValue(dotNetObject.origin)) {
        properties.origin = dotNetObject.origin;
    }
    let jsAnalysis = new Analysis(properties);
    
    jsObjectRefs[dotNetObject.id] = jsAnalysis;
    arcGisObjectRefs[dotNetObject.id] = jsAnalysis;
    
    return jsAnalysis;
}


export async function buildDotNetAnalysisGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetAnalysis: any = {};
    
    if (hasValue(jsObject.id)) {
        dotNetAnalysis.analysisId = jsObject.id;
    }
    
    if (hasValue(jsObject.origin)) {
        dotNetAnalysis.origin = removeCircularReferences(jsObject.origin);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetAnalysis.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetAnalysis.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for Analysis', e);
            }
        }
    }
    if (hasValue(dotNetAnalysis.id)) {
        jsObjectRefs[dotNetAnalysis.id] ??= jsObject;
        arcGisObjectRefs[dotNetAnalysis.id] ??= jsObject;
    }

    return dotNetAnalysis;
}


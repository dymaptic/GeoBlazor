// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import OpenStreetMapLayer from '@arcgis/core/layers/OpenStreetMapLayer';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class IOpenStreetMapLayerGenerated implements IPropertyWrapper {
    public layer: OpenStreetMapLayer;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(layer: OpenStreetMapLayer) {
        this.layer = layer;
    }
    
    // region methods
   
    unwrap() {
        return this.layer;
    }
    
    async load(options: AbortSignal): Promise<void> {
        await this.layer.load(options);
    }

    async createLayerView(view: any,
        options: any): Promise<any> {
        return await this.layer.createLayerView(view,
            options);
    }

    async fetchAttributionData(): Promise<any> {
        return await this.layer.fetchAttributionData();
    }

    async fetchTile(level: any,
        row: any,
        col: any,
        options: any): Promise<any> {
        return await this.layer.fetchTile(level,
            row,
            col,
            options);
    }

    async getTileUrl(level: any,
        row: any,
        col: any): Promise<any> {
        return this.layer.getTileUrl(level,
            row,
            col);
    }

    async refresh(): Promise<void> {
        this.layer.refresh();
    }

    // region properties
    
    async getFullExtent(): Promise<any> {
        if (!hasValue(this.layer.fullExtent)) {
            return null;
        }
        
        let { buildDotNetExtent } = await import('./extent');
        return buildDotNetExtent(this.layer.fullExtent);
    }
    async getPortalItem(): Promise<any> {
        if (!hasValue(this.layer.portalItem)) {
            return null;
        }
        
        let { buildDotNetPortalItem } = await import('./portalItem');
        return await buildDotNetPortalItem(this.layer.portalItem);
    }
    async setPortalItem(value: any): Promise<void> {
        let { buildJsPortalItem } = await import('./portalItem');
        this.layer.portalItem = await  buildJsPortalItem(value, this.layerId, this.viewId);
    }
    async getSpatialReference(): Promise<any> {
        if (!hasValue(this.layer.spatialReference)) {
            return null;
        }
        
        let { buildDotNetSpatialReference } = await import('./spatialReference');
        return buildDotNetSpatialReference(this.layer.spatialReference);
    }
    async getTileInfo(): Promise<any> {
        if (!hasValue(this.layer.tileInfo)) {
            return null;
        }
        
        let { buildDotNetTileInfo } = await import('./tileInfo');
        return await buildDotNetTileInfo(this.layer.tileInfo);
    }
    async setTileInfo(value: any): Promise<void> {
        let { buildJsTileInfo } = await import('./tileInfo');
        this.layer.tileInfo = await  buildJsTileInfo(value, this.layerId, this.viewId);
    }
    async getVisibilityTimeExtent(): Promise<any> {
        if (!hasValue(this.layer.visibilityTimeExtent)) {
            return null;
        }
        
        let { buildDotNetTimeExtent } = await import('./timeExtent');
        return buildDotNetTimeExtent(this.layer.visibilityTimeExtent);
    }
    async setVisibilityTimeExtent(value: any): Promise<void> {
        let { buildJsTimeExtent } = await import('./timeExtent');
        this.layer.visibilityTimeExtent = await  buildJsTimeExtent(value, this.layerId, this.viewId);
    }
    getProperty(prop: string): any {
        return this.layer[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.layer[prop] = value;
    }
}


export async function buildJsIOpenStreetMapLayerGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsOpenStreetMapLayer = new OpenStreetMapLayer();
    if (hasValue(dotNetObject.portalItem)) {
        let { buildJsPortalItem } = await import('./portalItem');
        jsOpenStreetMapLayer.portalItem = await buildJsPortalItem(dotNetObject.portalItem, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.tileInfo)) {
        let { buildJsTileInfo } = await import('./tileInfo');
        jsOpenStreetMapLayer.tileInfo = await buildJsTileInfo(dotNetObject.tileInfo, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.visibilityTimeExtent)) {
        let { buildJsTimeExtent } = await import('./timeExtent');
        jsOpenStreetMapLayer.visibilityTimeExtent = await buildJsTimeExtent(dotNetObject.visibilityTimeExtent, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.arcGISLayerId)) {
        jsOpenStreetMapLayer.id = dotNetObject.arcGISLayerId;
    }
    if (hasValue(dotNetObject.blendMode)) {
        jsOpenStreetMapLayer.blendMode = dotNetObject.blendMode;
    }
    if (hasValue(dotNetObject.copyright)) {
        jsOpenStreetMapLayer.copyright = dotNetObject.copyright;
    }
    if (hasValue(dotNetObject.effect)) {
        jsOpenStreetMapLayer.effect = dotNetObject.effect;
    }
    if (hasValue(dotNetObject.listMode)) {
        jsOpenStreetMapLayer.listMode = dotNetObject.listMode;
    }
    if (hasValue(dotNetObject.maxScale)) {
        jsOpenStreetMapLayer.maxScale = dotNetObject.maxScale;
    }
    if (hasValue(dotNetObject.minScale)) {
        jsOpenStreetMapLayer.minScale = dotNetObject.minScale;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsOpenStreetMapLayer.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.persistenceEnabled)) {
        jsOpenStreetMapLayer.persistenceEnabled = dotNetObject.persistenceEnabled;
    }
    if (hasValue(dotNetObject.refreshInterval)) {
        jsOpenStreetMapLayer.refreshInterval = dotNetObject.refreshInterval;
    }
    if (hasValue(dotNetObject.subDomains)) {
        jsOpenStreetMapLayer.subDomains = dotNetObject.subDomains;
    }
    if (hasValue(dotNetObject.title)) {
        jsOpenStreetMapLayer.title = dotNetObject.title;
    }
    if (hasValue(dotNetObject.urlTemplate)) {
        jsOpenStreetMapLayer.urlTemplate = dotNetObject.urlTemplate;
    }
    jsOpenStreetMapLayer.on('layerview-create', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsayerviewCreate', evt);
    });
    
    jsOpenStreetMapLayer.on('layerview-create-error', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsayerviewCreateError', evt);
    });
    
    jsOpenStreetMapLayer.on('layerview-destroy', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsayerviewDestroy', evt);
    });
    
    jsOpenStreetMapLayer.on('refresh', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsefresh', evt);
    });
    
    jsOpenStreetMapLayer.on('refresh', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsRefresh', evt);
    });
    

    let { default: IOpenStreetMapLayerWrapper } = await import('./iOpenStreetMapLayer');
    let iOpenStreetMapLayerWrapper = new IOpenStreetMapLayerWrapper(jsOpenStreetMapLayer);
    iOpenStreetMapLayerWrapper.geoBlazorId = dotNetObject.id;
    iOpenStreetMapLayerWrapper.viewId = viewId;
    iOpenStreetMapLayerWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(iOpenStreetMapLayerWrapper);
    jsObjectRefs[dotNetObject.id] = iOpenStreetMapLayerWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsOpenStreetMapLayer;
    let { buildDotNetIOpenStreetMapLayer } = await import('./iOpenStreetMapLayer');
    let dnInstantiatedObject = await buildDotNetIOpenStreetMapLayer(jsOpenStreetMapLayer);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for IOpenStreetMapLayer', e);
    }
    
    return jsOpenStreetMapLayer;
}

export async function buildDotNetIOpenStreetMapLayerGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetIOpenStreetMapLayer: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.fullExtent)) {
            let { buildDotNetExtent } = await import('./extent');
            dotNetIOpenStreetMapLayer.fullExtent = buildDotNetExtent(jsObject.fullExtent);
        }
        if (hasValue(jsObject.portalItem)) {
            let { buildDotNetPortalItem } = await import('./portalItem');
            dotNetIOpenStreetMapLayer.portalItem = await buildDotNetPortalItem(jsObject.portalItem);
        }
        if (hasValue(jsObject.spatialReference)) {
            let { buildDotNetSpatialReference } = await import('./spatialReference');
            dotNetIOpenStreetMapLayer.spatialReference = buildDotNetSpatialReference(jsObject.spatialReference);
        }
        if (hasValue(jsObject.tileInfo)) {
            let { buildDotNetTileInfo } = await import('./tileInfo');
            dotNetIOpenStreetMapLayer.tileInfo = await buildDotNetTileInfo(jsObject.tileInfo);
        }
        if (hasValue(jsObject.visibilityTimeExtent)) {
            let { buildDotNetTimeExtent } = await import('./timeExtent');
            dotNetIOpenStreetMapLayer.visibilityTimeExtent = buildDotNetTimeExtent(jsObject.visibilityTimeExtent);
        }
        if (hasValue(jsObject.id)) {
            dotNetIOpenStreetMapLayer.arcGISLayerId = jsObject.id;
        }
        if (hasValue(jsObject.blendMode)) {
            dotNetIOpenStreetMapLayer.blendMode = jsObject.blendMode;
        }
        if (hasValue(jsObject.copyright)) {
            dotNetIOpenStreetMapLayer.copyright = jsObject.copyright;
        }
        if (hasValue(jsObject.effect)) {
            dotNetIOpenStreetMapLayer.effect = jsObject.effect;
        }
        if (hasValue(jsObject.listMode)) {
            dotNetIOpenStreetMapLayer.listMode = jsObject.listMode;
        }
        if (hasValue(jsObject.loaded)) {
            dotNetIOpenStreetMapLayer.loaded = jsObject.loaded;
        }
        if (hasValue(jsObject.maxScale)) {
            dotNetIOpenStreetMapLayer.maxScale = jsObject.maxScale;
        }
        if (hasValue(jsObject.minScale)) {
            dotNetIOpenStreetMapLayer.minScale = jsObject.minScale;
        }
        if (hasValue(jsObject.opacity)) {
            dotNetIOpenStreetMapLayer.opacity = jsObject.opacity;
        }
        if (hasValue(jsObject.persistenceEnabled)) {
            dotNetIOpenStreetMapLayer.persistenceEnabled = jsObject.persistenceEnabled;
        }
        if (hasValue(jsObject.refreshInterval)) {
            dotNetIOpenStreetMapLayer.refreshInterval = jsObject.refreshInterval;
        }
        if (hasValue(jsObject.subDomains)) {
            dotNetIOpenStreetMapLayer.subDomains = jsObject.subDomains;
        }
        if (hasValue(jsObject.tileServers)) {
            dotNetIOpenStreetMapLayer.tileServers = jsObject.tileServers;
        }
        if (hasValue(jsObject.title)) {
            dotNetIOpenStreetMapLayer.title = jsObject.title;
        }
        if (hasValue(jsObject.type)) {
            dotNetIOpenStreetMapLayer.type = jsObject.type;
        }
        if (hasValue(jsObject.urlTemplate)) {
            dotNetIOpenStreetMapLayer.urlTemplate = jsObject.urlTemplate;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetIOpenStreetMapLayer.id = k;
                break;
            }
        }
    }

    return dotNetIOpenStreetMapLayer;
}


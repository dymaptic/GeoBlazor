// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import IPSInfo from '@arcgis/core/webdoc/IPSInfo';
import { hasValue } from './geoBlazorCore';

export async function buildDotNetIPSInfoGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetIPSInfo: any = {};
    
    if (hasValue(jsObject.configuration)) {
        let { buildDotNetConfiguration } = await import('./configuration');
        dotNetIPSInfo.configuration = await buildDotNetConfiguration(jsObject.configuration, viewId);
    }
    
    if (hasValue(jsObject.positioningService)) {
        let { buildDotNetPositioningService } = await import('./positioningService');
        dotNetIPSInfo.positioningService = await buildDotNetPositioningService(jsObject.positioningService, layerId, viewId);
    }
    

    return dotNetIPSInfo;
}


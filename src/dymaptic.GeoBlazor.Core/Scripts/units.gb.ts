// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, removeCircularReferences } from './arcGisJsInterop';
import { buildDotNetUnits } from './units';

export async function buildJsUnitsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsUnits: any = {};

    if (hasValue(dotNetObject.area)) {
        jsUnits.area = dotNetObject.area;
    }
    if (hasValue(dotNetObject.length)) {
        jsUnits.length = dotNetObject.length;
    }
    if (hasValue(dotNetObject.verticalLength)) {
        jsUnits.verticalLength = dotNetObject.verticalLength;
    }
    
    jsObjectRefs[dotNetObject.id] = jsUnits;
    arcGisObjectRefs[dotNetObject.id] = jsUnits;
    
    return jsUnits;
}


export async function buildDotNetUnitsGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetUnits: any = {};
    
    if (hasValue(jsObject.area)) {
        dotNetUnits.area = removeCircularReferences(jsObject.area);
    }
    
    if (hasValue(jsObject.length)) {
        dotNetUnits.length = removeCircularReferences(jsObject.length);
    }
    
    if (hasValue(jsObject.verticalLength)) {
        dotNetUnits.verticalLength = removeCircularReferences(jsObject.verticalLength);
    }
    

    return dotNetUnits;
}


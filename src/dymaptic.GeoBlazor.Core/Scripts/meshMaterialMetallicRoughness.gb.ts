// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import MeshMaterialMetallicRoughness from '@arcgis/core/geometry/support/MeshMaterialMetallicRoughness';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, sanitize, removeCircularReferences } from './geoBlazorCore';

export async function buildJsMeshMaterialMetallicRoughnessGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        properties.color = buildJsMapColor(dotNetObject.color) as any;
    }
    if (hasValue(dotNetObject.colorTextureTransform)) {
        let { buildJsMeshTextureTransform } = await import('./meshTextureTransform');
        properties.colorTextureTransform = await buildJsMeshTextureTransform(dotNetObject.colorTextureTransform, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.emissiveColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        properties.emissiveColor = buildJsMapColor(dotNetObject.emissiveColor) as any;
    }
    if (hasValue(dotNetObject.emissiveTextureTransform)) {
        let { buildJsMeshTextureTransform } = await import('./meshTextureTransform');
        properties.emissiveTextureTransform = await buildJsMeshTextureTransform(dotNetObject.emissiveTextureTransform, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.normalTextureTransform)) {
        let { buildJsMeshTextureTransform } = await import('./meshTextureTransform');
        properties.normalTextureTransform = await buildJsMeshTextureTransform(dotNetObject.normalTextureTransform, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.occlusionTextureTransform)) {
        let { buildJsMeshTextureTransform } = await import('./meshTextureTransform');
        properties.occlusionTextureTransform = await buildJsMeshTextureTransform(dotNetObject.occlusionTextureTransform, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.alphaCutoff)) {
        properties.alphaCutoff = dotNetObject.alphaCutoff;
    }
    if (hasValue(dotNetObject.alphaMode)) {
        properties.alphaMode = dotNetObject.alphaMode;
    }
    if (hasValue(dotNetObject.colorTexture)) {
        properties.colorTexture = sanitize(dotNetObject.colorTexture);
    }
    if (hasValue(dotNetObject.doubleSided)) {
        properties.doubleSided = dotNetObject.doubleSided;
    }
    if (hasValue(dotNetObject.emissiveTexture)) {
        properties.emissiveTexture = sanitize(dotNetObject.emissiveTexture);
    }
    if (hasValue(dotNetObject.metallic)) {
        properties.metallic = dotNetObject.metallic;
    }
    if (hasValue(dotNetObject.metallicRoughnessTexture)) {
        properties.metallicRoughnessTexture = sanitize(dotNetObject.metallicRoughnessTexture);
    }
    if (hasValue(dotNetObject.normalTexture)) {
        properties.normalTexture = sanitize(dotNetObject.normalTexture);
    }
    if (hasValue(dotNetObject.occlusionTexture)) {
        properties.occlusionTexture = sanitize(dotNetObject.occlusionTexture);
    }
    if (hasValue(dotNetObject.roughness)) {
        properties.roughness = dotNetObject.roughness;
    }
    let jsMeshMaterialMetallicRoughness = new MeshMaterialMetallicRoughness(properties);
    
    jsObjectRefs[dotNetObject.id] = jsMeshMaterialMetallicRoughness;
    arcGisObjectRefs[dotNetObject.id] = jsMeshMaterialMetallicRoughness;
    
    return jsMeshMaterialMetallicRoughness;
}


export async function buildDotNetMeshMaterialMetallicRoughnessGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetMeshMaterialMetallicRoughness: any = {};
    
    if (hasValue(jsObject.color)) {
        let { buildDotNetMapColor } = await import('./mapColor');
        dotNetMeshMaterialMetallicRoughness.color = buildDotNetMapColor(jsObject.color);
    }
    
    if (hasValue(jsObject.colorTextureTransform)) {
        let { buildDotNetMeshTextureTransform } = await import('./meshTextureTransform');
        dotNetMeshMaterialMetallicRoughness.colorTextureTransform = await buildDotNetMeshTextureTransform(jsObject.colorTextureTransform, viewId);
    }
    
    if (hasValue(jsObject.emissiveColor)) {
        let { buildDotNetMapColor } = await import('./mapColor');
        dotNetMeshMaterialMetallicRoughness.emissiveColor = buildDotNetMapColor(jsObject.emissiveColor);
    }
    
    if (hasValue(jsObject.emissiveTextureTransform)) {
        let { buildDotNetMeshTextureTransform } = await import('./meshTextureTransform');
        dotNetMeshMaterialMetallicRoughness.emissiveTextureTransform = await buildDotNetMeshTextureTransform(jsObject.emissiveTextureTransform, viewId);
    }
    
    if (hasValue(jsObject.normalTextureTransform)) {
        let { buildDotNetMeshTextureTransform } = await import('./meshTextureTransform');
        dotNetMeshMaterialMetallicRoughness.normalTextureTransform = await buildDotNetMeshTextureTransform(jsObject.normalTextureTransform, viewId);
    }
    
    if (hasValue(jsObject.occlusionTextureTransform)) {
        let { buildDotNetMeshTextureTransform } = await import('./meshTextureTransform');
        dotNetMeshMaterialMetallicRoughness.occlusionTextureTransform = await buildDotNetMeshTextureTransform(jsObject.occlusionTextureTransform, viewId);
    }
    
    if (hasValue(jsObject.alphaCutoff)) {
        dotNetMeshMaterialMetallicRoughness.alphaCutoff = jsObject.alphaCutoff;
    }
    
    if (hasValue(jsObject.alphaMode)) {
        dotNetMeshMaterialMetallicRoughness.alphaMode = removeCircularReferences(jsObject.alphaMode);
    }
    
    if (hasValue(jsObject.colorTexture)) {
        dotNetMeshMaterialMetallicRoughness.colorTexture = removeCircularReferences(jsObject.colorTexture);
    }
    
    if (hasValue(jsObject.doubleSided)) {
        dotNetMeshMaterialMetallicRoughness.doubleSided = jsObject.doubleSided;
    }
    
    if (hasValue(jsObject.emissiveTexture)) {
        dotNetMeshMaterialMetallicRoughness.emissiveTexture = removeCircularReferences(jsObject.emissiveTexture);
    }
    
    if (hasValue(jsObject.metallic)) {
        dotNetMeshMaterialMetallicRoughness.metallic = jsObject.metallic;
    }
    
    if (hasValue(jsObject.metallicRoughnessTexture)) {
        dotNetMeshMaterialMetallicRoughness.metallicRoughnessTexture = removeCircularReferences(jsObject.metallicRoughnessTexture);
    }
    
    if (hasValue(jsObject.normalTexture)) {
        dotNetMeshMaterialMetallicRoughness.normalTexture = removeCircularReferences(jsObject.normalTexture);
    }
    
    if (hasValue(jsObject.occlusionTexture)) {
        dotNetMeshMaterialMetallicRoughness.occlusionTexture = removeCircularReferences(jsObject.occlusionTexture);
    }
    
    if (hasValue(jsObject.roughness)) {
        dotNetMeshMaterialMetallicRoughness.roughness = jsObject.roughness;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetMeshMaterialMetallicRoughness.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetMeshMaterialMetallicRoughness.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for MeshMaterialMetallicRoughness', e);
            }
        }
    }
    if (hasValue(dotNetMeshMaterialMetallicRoughness.id)) {
        jsObjectRefs[dotNetMeshMaterialMetallicRoughness.id] ??= jsObject;
        arcGisObjectRefs[dotNetMeshMaterialMetallicRoughness.id] ??= jsObject;
    }

    return dotNetMeshMaterialMetallicRoughness;
}


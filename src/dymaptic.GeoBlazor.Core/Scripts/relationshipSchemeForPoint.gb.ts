// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetRelationshipSchemeForPoint } from './relationshipSchemeForPoint';

export async function buildJsRelationshipSchemeForPointGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsRelationshipSchemeForPoint: any = {};
    if (hasValue(dotNetObject.colorsForClassBreaks)) {
        let { buildJsRelationshipSchemeForPointColorsForClassBreaks } = await import('./relationshipSchemeForPointColorsForClassBreaks');
        jsRelationshipSchemeForPoint.colorsForClassBreaks = await Promise.all(dotNetObject.colorsForClassBreaks.map(async i => await buildJsRelationshipSchemeForPointColorsForClassBreaks(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.noDataColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsRelationshipSchemeForPoint.noDataColor = buildJsMapColor(dotNetObject.noDataColor) as any;
    }
    if (hasValue(dotNetObject.outline)) {
        let { buildJsRelationshipSchemeForPointOutline } = await import('./relationshipSchemeForPointOutline');
        jsRelationshipSchemeForPoint.outline = await buildJsRelationshipSchemeForPointOutline(dotNetObject.outline, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.name)) {
        jsRelationshipSchemeForPoint.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsRelationshipSchemeForPoint.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.relationshipSchemeForPointId)) {
        jsRelationshipSchemeForPoint.id = dotNetObject.relationshipSchemeForPointId;
    }
    if (hasValue(dotNetObject.size)) {
        jsRelationshipSchemeForPoint.size = dotNetObject.size;
    }
    if (hasValue(dotNetObject.tags)) {
        jsRelationshipSchemeForPoint.tags = dotNetObject.tags;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsRelationshipSchemeForPoint);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsRelationshipSchemeForPoint;
    
    let dnInstantiatedObject = await buildDotNetRelationshipSchemeForPoint(jsRelationshipSchemeForPoint);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for RelationshipSchemeForPoint', e);
    }
    
    return jsRelationshipSchemeForPoint;
}

export async function buildDotNetRelationshipSchemeForPointGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetRelationshipSchemeForPoint: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.colorsForClassBreaks)) {
            let { buildDotNetRelationshipSchemeForPointColorsForClassBreaks } = await import('./relationshipSchemeForPointColorsForClassBreaks');
            dotNetRelationshipSchemeForPoint.colorsForClassBreaks = await Promise.all(jsObject.colorsForClassBreaks.map(async i => await buildDotNetRelationshipSchemeForPointColorsForClassBreaks(i)));
        }
        if (hasValue(jsObject.noDataColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetRelationshipSchemeForPoint.noDataColor = buildDotNetMapColor(jsObject.noDataColor);
        }
        if (hasValue(jsObject.outline)) {
            let { buildDotNetRelationshipSchemeForPointOutline } = await import('./relationshipSchemeForPointOutline');
            dotNetRelationshipSchemeForPoint.outline = await buildDotNetRelationshipSchemeForPointOutline(jsObject.outline);
        }
    if (hasValue(jsObject.name)) {
        dotNetRelationshipSchemeForPoint.name = jsObject.name;
    }
    if (hasValue(jsObject.opacity)) {
        dotNetRelationshipSchemeForPoint.opacity = jsObject.opacity;
    }
    if (hasValue(jsObject.id)) {
        dotNetRelationshipSchemeForPoint.relationshipSchemeForPointId = jsObject.id;
    }
    if (hasValue(jsObject.size)) {
        dotNetRelationshipSchemeForPoint.size = jsObject.size;
    }
    if (hasValue(jsObject.tags)) {
        dotNetRelationshipSchemeForPoint.tags = jsObject.tags;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetRelationshipSchemeForPoint.id = k;
                break;
            }
        }
    }

    return dotNetRelationshipSchemeForPoint;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetHistogramRangeSliderDataLines } from './histogramRangeSliderDataLines';

export async function buildJsHistogramRangeSliderDataLinesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsHistogramRangeSliderDataLines: any = {};

    if (hasValue(dotNetObject.label)) {
        jsHistogramRangeSliderDataLines.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.value)) {
        jsHistogramRangeSliderDataLines.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsHistogramRangeSliderDataLines);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsHistogramRangeSliderDataLines;
    
    let { buildDotNetHistogramRangeSliderDataLines } = await import('./histogramRangeSliderDataLines');
    let dnInstantiatedObject = await buildDotNetHistogramRangeSliderDataLines(jsHistogramRangeSliderDataLines);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type HistogramRangeSliderDataLines detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for HistogramRangeSliderDataLines', e);
    }
    
    return jsHistogramRangeSliderDataLines;
}


export async function buildDotNetHistogramRangeSliderDataLinesGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetHistogramRangeSliderDataLines: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.label)) {
        dotNetHistogramRangeSliderDataLines.label = jsObject.label;
    }
    if (hasValue(jsObject.value)) {
        dotNetHistogramRangeSliderDataLines.value = jsObject.value;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetHistogramRangeSliderDataLines.id = geoBlazorId;
    }

    return dotNetHistogramRangeSliderDataLines;
}


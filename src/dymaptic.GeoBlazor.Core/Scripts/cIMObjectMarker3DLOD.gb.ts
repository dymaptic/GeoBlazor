// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMObjectMarker3DLOD } from './cIMObjectMarker3DLOD';

export async function buildJsCIMObjectMarker3DLODGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMObjectMarker3DLOD: any = {};

    if (hasValue(dotNetObject.faceCount)) {
        jsCIMObjectMarker3DLOD.faceCount = dotNetObject.faceCount;
    }
    if (hasValue(dotNetObject.modelURI)) {
        jsCIMObjectMarker3DLOD.modelURI = dotNetObject.modelURI;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMObjectMarker3DLOD);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMObjectMarker3DLOD;
    
    let { buildDotNetCIMObjectMarker3DLOD } = await import('./cIMObjectMarker3DLOD');
    let dnInstantiatedObject = await buildDotNetCIMObjectMarker3DLOD(jsCIMObjectMarker3DLOD);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMObjectMarker3DLOD detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMObjectMarker3DLOD', e);
    }
    
    return jsCIMObjectMarker3DLOD;
}


export async function buildDotNetCIMObjectMarker3DLODGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMObjectMarker3DLOD: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.faceCount)) {
        dotNetCIMObjectMarker3DLOD.faceCount = jsObject.faceCount;
    }
    if (hasValue(jsObject.modelURI)) {
        dotNetCIMObjectMarker3DLOD.modelURI = jsObject.modelURI;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMObjectMarker3DLOD.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMObjectMarker3DLOD.id = geoBlazorId;
    }

    return dotNetCIMObjectMarker3DLOD;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import supportUtils = __esri.supportUtils;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class SupportUtilsGenerated implements IPropertyWrapper {
    public component: supportUtils;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: supportUtils) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async getDefaultBandCombination(parameters: any): Promise<any> {
        let { buildJsUtilsGetDefaultBandCombinationParams } = await import('./utilsGetDefaultBandCombinationParams');
        let jsparameters = await buildJsUtilsGetDefaultBandCombinationParams(parameters, this.layerId, this.viewId) as any;
        return await this.component.getDefaultBandCombination(jsparameters);
    }

    async getSupportedRendererInfo(parameters: any): Promise<any> {
        let { buildJsUtilsGetSupportedRendererInfoParams } = await import('./utilsGetSupportedRendererInfoParams');
        let jsparameters = await buildJsUtilsGetSupportedRendererInfoParams(parameters, this.layerId, this.viewId) as any;
        return await this.component.getSupportedRendererInfo(jsparameters);
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsSupportUtilsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jssupportUtils: any = {}


    let { default: SupportUtilsWrapper } = await import('./supportUtils');
    let supportUtilsWrapper = new SupportUtilsWrapper(jssupportUtils);
    supportUtilsWrapper.geoBlazorId = dotNetObject.id;
    supportUtilsWrapper.viewId = viewId;
    supportUtilsWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(supportUtilsWrapper);
    jsObjectRefs[dotNetObject.id] = supportUtilsWrapper;
    arcGisObjectRefs[dotNetObject.id] = jssupportUtils;
    let { buildDotNetSupportUtils } = await import('./supportUtils');
    let dnInstantiatedObject = await buildDotNetSupportUtils(jssupportUtils);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for SupportUtils', e);
    }
    
    return jssupportUtils;
}

export async function buildDotNetSupportUtilsGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSupportUtils: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetSupportUtils.id = k;
                break;
            }
        }
    }

    return dotNetSupportUtils;
}


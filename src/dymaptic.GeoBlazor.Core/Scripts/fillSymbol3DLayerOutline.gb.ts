// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetFillSymbol3DLayerOutline } from './fillSymbol3DLayerOutline';

export async function buildJsFillSymbol3DLayerOutlineGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsFillSymbol3DLayerOutline: any = {}
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsFillSymbol3DLayerOutline.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.pattern)) {
        const { id, dotNetComponentReference, layerId, viewId, ...sanitizedPattern } = dotNetObject.pattern;
        jsFillSymbol3DLayerOutline.pattern = sanitizedPattern;
    }
    if (hasValue(dotNetObject.patternCap)) {
        jsFillSymbol3DLayerOutline.patternCap = dotNetObject.patternCap;
    }
    if (hasValue(dotNetObject.size)) {
        jsFillSymbol3DLayerOutline.size = dotNetObject.size;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsFillSymbol3DLayerOutline);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsFillSymbol3DLayerOutline;
    
    let dnInstantiatedObject = await buildDotNetFillSymbol3DLayerOutline(jsFillSymbol3DLayerOutline);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for FillSymbol3DLayerOutline', e);
    }
    
    return jsFillSymbol3DLayerOutline;
}

export async function buildDotNetFillSymbol3DLayerOutlineGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFillSymbol3DLayerOutline: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetFillSymbol3DLayerOutline.color = buildDotNetMapColor(jsObject.color);
        }
    if (hasValue(jsObject.pattern)) {
        dotNetFillSymbol3DLayerOutline.pattern = jsObject.pattern;
    }
    if (hasValue(jsObject.patternCap)) {
        dotNetFillSymbol3DLayerOutline.patternCap = jsObject.patternCap;
    }
    if (hasValue(jsObject.size)) {
        dotNetFillSymbol3DLayerOutline.size = jsObject.size;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetFillSymbol3DLayerOutline.id = k;
                break;
            }
        }
    }

    return dotNetFillSymbol3DLayerOutline;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import ApplicationProperties from '@arcgis/core/webmap/ApplicationProperties';
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetApplicationProperties } from './applicationProperties';

export async function buildJsApplicationPropertiesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.viewing)) {
        let { buildJsViewing } = await import('./viewing');
        properties.viewing = await buildJsViewing(dotNetObject.viewing, layerId, viewId) as any;
    }

    let jsApplicationProperties = new ApplicationProperties(properties);
    
    jsObjectRefs[dotNetObject.id] = jsApplicationProperties;
    arcGisObjectRefs[dotNetObject.id] = jsApplicationProperties;
    
    return jsApplicationProperties;
}


export async function buildDotNetApplicationPropertiesGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetApplicationProperties: any = {};
    
    if (hasValue(jsObject.viewing)) {
        let { buildDotNetViewing } = await import('./viewing');
        dotNetApplicationProperties.viewing = await buildDotNetViewing(jsObject.viewing, layerId, viewId);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetApplicationProperties.id = geoBlazorId;
    }

    return dotNetApplicationProperties;
}


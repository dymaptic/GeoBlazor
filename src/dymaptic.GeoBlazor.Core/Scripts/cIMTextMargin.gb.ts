// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMTextMargin } from './cIMTextMargin';

export async function buildJsCIMTextMarginGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMTextMargin: any = {};

    if (hasValue(dotNetObject.bottom)) {
        jsCIMTextMargin.bottom = dotNetObject.bottom;
    }
    if (hasValue(dotNetObject.left)) {
        jsCIMTextMargin.left = dotNetObject.left;
    }
    if (hasValue(dotNetObject.right)) {
        jsCIMTextMargin.right = dotNetObject.right;
    }
    if (hasValue(dotNetObject.top)) {
        jsCIMTextMargin.top = dotNetObject.top;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMTextMargin);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMTextMargin;
    
    let { buildDotNetCIMTextMargin } = await import('./cIMTextMargin');
    let dnInstantiatedObject = await buildDotNetCIMTextMargin(jsCIMTextMargin);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMTextMargin detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMTextMargin', e);
    }
    
    return jsCIMTextMargin;
}


export async function buildDotNetCIMTextMarginGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMTextMargin: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.bottom)) {
        dotNetCIMTextMargin.bottom = jsObject.bottom;
    }
    if (hasValue(jsObject.left)) {
        dotNetCIMTextMargin.left = jsObject.left;
    }
    if (hasValue(jsObject.right)) {
        dotNetCIMTextMargin.right = jsObject.right;
    }
    if (hasValue(jsObject.top)) {
        dotNetCIMTextMargin.top = jsObject.top;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMTextMargin.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMTextMargin.id = geoBlazorId;
    }

    return dotNetCIMTextMargin;
}


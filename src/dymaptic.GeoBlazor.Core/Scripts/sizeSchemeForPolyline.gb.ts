// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetSizeSchemeForPolyline } from './sizeSchemeForPolyline';

export async function buildJsSizeSchemeForPolylineGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsSizeSchemeForPolyline: any = {}
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsSizeSchemeForPolyline.color = buildJsMapColor(dotNetObject.color) as any;
    }
    if (hasValue(dotNetObject.noDataColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsSizeSchemeForPolyline.noDataColor = buildJsMapColor(dotNetObject.noDataColor) as any;
    }

    if (hasValue(dotNetObject.maxWidth)) {
        jsSizeSchemeForPolyline.maxWidth = dotNetObject.maxWidth;
    }
    if (hasValue(dotNetObject.minWidth)) {
        jsSizeSchemeForPolyline.minWidth = dotNetObject.minWidth;
    }
    if (hasValue(dotNetObject.noDataWidth)) {
        jsSizeSchemeForPolyline.noDataWidth = dotNetObject.noDataWidth;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsSizeSchemeForPolyline.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.width)) {
        jsSizeSchemeForPolyline.width = dotNetObject.width;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsSizeSchemeForPolyline);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsSizeSchemeForPolyline;
    
    let dnInstantiatedObject = await buildDotNetSizeSchemeForPolyline(jsSizeSchemeForPolyline);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for SizeSchemeForPolyline', e);
    }
    
    return jsSizeSchemeForPolyline;
}

export async function buildDotNetSizeSchemeForPolylineGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSizeSchemeForPolyline: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetSizeSchemeForPolyline.color = buildDotNetMapColor(jsObject.color);
        }
        if (hasValue(jsObject.noDataColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetSizeSchemeForPolyline.noDataColor = buildDotNetMapColor(jsObject.noDataColor);
        }
    if (hasValue(jsObject.maxWidth)) {
        dotNetSizeSchemeForPolyline.maxWidth = jsObject.maxWidth;
    }
    if (hasValue(jsObject.minWidth)) {
        dotNetSizeSchemeForPolyline.minWidth = jsObject.minWidth;
    }
    if (hasValue(jsObject.noDataWidth)) {
        dotNetSizeSchemeForPolyline.noDataWidth = jsObject.noDataWidth;
    }
    if (hasValue(jsObject.opacity)) {
        dotNetSizeSchemeForPolyline.opacity = jsObject.opacity;
    }
    if (hasValue(jsObject.width)) {
        dotNetSizeSchemeForPolyline.width = jsObject.width;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetSizeSchemeForPolyline.id = k;
                break;
            }
        }
    }

    return dotNetSizeSchemeForPolyline;
}


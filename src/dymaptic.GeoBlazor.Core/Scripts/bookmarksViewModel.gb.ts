// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file


import BookmarksViewModel from '@arcgis/core/widgets/Bookmarks/BookmarksViewModel';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class BookmarksViewModelGenerated implements IPropertyWrapper {
    public component: BookmarksViewModel;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: BookmarksViewModel) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async createBookmark(options: any): Promise<any> {
        return await this.component.createBookmark(options);
    }

    async editBookmark(bookmark: any,
        options: any): Promise<any> {
        return await this.component.editBookmark(bookmark,
            options);
    }

    async goTo(bookmark: any): Promise<any> {
        return await this.component.goTo(bookmark);
    }

    // region properties
    
    async getCapabilities(): Promise<any> {
        if (!hasValue(this.component.capabilities)) {
            return null;
        }
        
        let { buildDotNetBookmarksCapabilities } = await import('./bookmarksCapabilities');
        return await buildDotNetBookmarksCapabilities(this.component.capabilities, this.layerId, this.viewId);
    }
    async setCapabilities(value: any): Promise<void> {
        let { buildJsBookmarksCapabilities } = await import('./bookmarksCapabilities');
        this.component.capabilities = await  buildJsBookmarksCapabilities(value, this.layerId, this.viewId);
    }
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsBookmarksViewModelGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsBookmarksViewModel = new BookmarksViewModel();
    if (hasValue(dotNetObject.capabilities)) {
        let { buildJsBookmarksCapabilities } = await import('./bookmarksCapabilities');
        jsBookmarksViewModel.capabilities = await buildJsBookmarksCapabilities(dotNetObject.capabilities, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.bookmarks)) {
        jsBookmarksViewModel.bookmarks = dotNetObject.bookmarks;
    }
    if (hasValue(dotNetObject.defaultCreateOptions)) {
        jsBookmarksViewModel.defaultCreateOptions = dotNetObject.defaultCreateOptions;
    }
    if (hasValue(dotNetObject.defaultEditOptions)) {
        jsBookmarksViewModel.defaultEditOptions = dotNetObject.defaultEditOptions;
    }
    if (hasValue(dotNetObject.goToOverride)) {
        jsBookmarksViewModel.goToOverride = dotNetObject.goToOverride;
    }
    if (hasValue(dotNetObject.view)) {
        jsBookmarksViewModel.view = dotNetObject.view;
    }
    let { default: BookmarksViewModelWrapper } = await import('./bookmarksViewModel');
    let bookmarksViewModelWrapper = new BookmarksViewModelWrapper(jsBookmarksViewModel);
    bookmarksViewModelWrapper.geoBlazorId = dotNetObject.id;
    bookmarksViewModelWrapper.viewId = viewId;
    bookmarksViewModelWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(bookmarksViewModelWrapper);
    jsObjectRefs[dotNetObject.id] = bookmarksViewModelWrapper;

    arcGisObjectRefs[dotNetObject.id] = jsBookmarksViewModel;
    let { buildDotNetBookmarksViewModel } = await import('./bookmarksViewModel');
    let dnInstantiatedObject = await buildDotNetBookmarksViewModel(jsBookmarksViewModel, layerId, viewId);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for BookmarksViewModel', e);
    }
    
    return jsBookmarksViewModel;
}

export async function buildDotNetBookmarksViewModelGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetBookmarksViewModel: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.capabilities)) {
            let { buildDotNetBookmarksCapabilities } = await import('./bookmarksCapabilities');
            dotNetBookmarksViewModel.capabilities = await buildDotNetBookmarksCapabilities(jsObject.capabilities, layerId, viewId);
        }
        if (hasValue(jsObject.activeBookmark)) {
            dotNetBookmarksViewModel.activeBookmark = jsObject.activeBookmark;
        }
        if (hasValue(jsObject.bookmarks)) {
            dotNetBookmarksViewModel.bookmarks = jsObject.bookmarks;
        }
        if (hasValue(jsObject.defaultCreateOptions)) {
            dotNetBookmarksViewModel.defaultCreateOptions = jsObject.defaultCreateOptions;
        }
        if (hasValue(jsObject.defaultEditOptions)) {
            dotNetBookmarksViewModel.defaultEditOptions = jsObject.defaultEditOptions;
        }
        if (hasValue(jsObject.goToOverride)) {
            dotNetBookmarksViewModel.goToOverride = jsObject.goToOverride;
        }
        if (hasValue(jsObject.state)) {
            dotNetBookmarksViewModel.state = jsObject.state;
        }
        if (hasValue(jsObject.view)) {
            dotNetBookmarksViewModel.view = jsObject.view;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetBookmarksViewModel.id = k;
                break;
            }
        }
    }

    return dotNetBookmarksViewModel;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectBuffer } from './cIMGeometricEffectBuffer';

export async function buildJsCIMGeometricEffectBufferGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectBuffer: any = {};

    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectBuffer.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.size)) {
        jsCIMGeometricEffectBuffer.size = dotNetObject.size;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectBuffer);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectBuffer;
    
    let { buildDotNetCIMGeometricEffectBuffer } = await import('./cIMGeometricEffectBuffer');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectBuffer(jsCIMGeometricEffectBuffer);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectBuffer detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectBuffer', e);
    }
    
    return jsCIMGeometricEffectBuffer;
}


export async function buildDotNetCIMGeometricEffectBufferGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectBuffer: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectBuffer.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.size)) {
        dotNetCIMGeometricEffectBuffer.size = jsObject.size;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectBuffer.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectBuffer.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectBuffer;
}


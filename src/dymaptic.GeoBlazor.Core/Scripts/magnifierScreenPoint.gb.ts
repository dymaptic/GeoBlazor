// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetMagnifierScreenPoint } from './magnifierScreenPoint';

export async function buildJsMagnifierScreenPointGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsMagnifierScreenPoint: any = {};

    if (hasValue(dotNetObject.x)) {
        jsMagnifierScreenPoint.x = dotNetObject.x;
    }
    if (hasValue(dotNetObject.y)) {
        jsMagnifierScreenPoint.y = dotNetObject.y;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsMagnifierScreenPoint);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsMagnifierScreenPoint;
    
    let { buildDotNetMagnifierScreenPoint } = await import('./magnifierScreenPoint');
    let dnInstantiatedObject = await buildDotNetMagnifierScreenPoint(jsMagnifierScreenPoint);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type MagnifierScreenPoint detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for MagnifierScreenPoint', e);
    }
    
    return jsMagnifierScreenPoint;
}


export async function buildDotNetMagnifierScreenPointGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetMagnifierScreenPoint: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.x)) {
        dotNetMagnifierScreenPoint.x = jsObject.x;
    }
    if (hasValue(jsObject.y)) {
        dotNetMagnifierScreenPoint.y = jsObject.y;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetMagnifierScreenPoint.id = geoBlazorId;
    }

    return dotNetMagnifierScreenPoint;
}


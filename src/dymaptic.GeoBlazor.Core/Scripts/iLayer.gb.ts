// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { hasValue, sanitize, removeCircularReferences, Pro } from './geoBlazorCore';

export async function buildJsILayerGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }
    
    switch (dotNetObject.geoBlazorClassName) {
        case 'GraphicsLayer':
            let { buildJsGraphicsLayer } = await import('./graphicsLayer');

            return await buildJsGraphicsLayer(dotNetObject, layerId, viewId);


        case 'WebTileLayer':
            let { buildJsWebTileLayer } = await import('./webTileLayer');

            return await buildJsWebTileLayer(dotNetObject, layerId, viewId);


        case 'ImageryLayer':
            let { buildJsImageryLayer } = await import('./imageryLayer');

            return await buildJsImageryLayer(dotNetObject, layerId, viewId);


        case 'UnsupportedLayer':
            let { buildJsUnsupportedLayer } = await import('./unsupportedLayer');

            return await buildJsUnsupportedLayer(dotNetObject, layerId, viewId);


        case 'WFSLayer':
            let { buildJsWFSLayer } = await import('./wFSLayer');

            return await buildJsWFSLayer(dotNetObject, layerId, viewId);


        case 'StreamLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetStreamLayer } = await import('./streamLayer');

                return await buildDotNetStreamLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'KnowledgeGraphLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetKnowledgeGraphLayer } = await import('./knowledgeGraphLayer');

                return await buildDotNetKnowledgeGraphLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'KnowledgeGraphSublayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetKnowledgeGraphSublayer } = await import('./knowledgeGraphSublayer');

                return await buildDotNetKnowledgeGraphSublayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'GeoJSONLayer':
            let { buildJsGeoJSONLayer } = await import('./geoJSONLayer');

            return await buildJsGeoJSONLayer(dotNetObject, layerId, viewId);


        case 'LinkChartLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetLinkChartLayer } = await import('./linkChartLayer');

                return await buildDotNetLinkChartLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'LineOfSightLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetLineOfSightLayer } = await import('./lineOfSightLayer');

                return await buildDotNetLineOfSightLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'SubtypeGroupLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetSubtypeGroupLayer } = await import('./subtypeGroupLayer');

                return await buildDotNetSubtypeGroupLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'CatalogDynamicGroupLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetCatalogDynamicGroupLayer } = await import('./catalogDynamicGroupLayer');

                return await buildDotNetCatalogDynamicGroupLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'MapNotesLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetMapNotesLayer } = await import('./mapNotesLayer');

                return await buildDotNetMapNotesLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'IntegratedMesh3DTilesLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetIntegratedMesh3DTilesLayer } = await import('./integratedMesh3DTilesLayer');

                return await buildDotNetIntegratedMesh3DTilesLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'VoxelLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetVoxelLayer } = await import('./voxelLayer');

                return await buildDotNetVoxelLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'CSVLayer':
            let { buildJsCSVLayer } = await import('./cSVLayer');

            return await buildJsCSVLayer(dotNetObject, layerId, viewId);


        case 'CatalogLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetCatalogLayer } = await import('./catalogLayer');

                return await buildDotNetCatalogLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'PointCloudLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetPointCloudLayer } = await import('./pointCloudLayer');

                return await buildDotNetPointCloudLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'VideoLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetVideoLayer } = await import('./videoLayer');

                return await buildDotNetVideoLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'ViewshedLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetViewshedLayer } = await import('./viewshedLayer');

                return await buildDotNetViewshedLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'WMSLayer':
            let { buildJsWMSLayer } = await import('./wMSLayer');

            return await buildJsWMSLayer(dotNetObject, layerId, viewId);


        case 'FeatureLayer':
            let { buildJsFeatureLayer } = await import('./featureLayer');

            return await buildJsFeatureLayer(dotNetObject, layerId, viewId);


        case 'DimensionLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetDimensionLayer } = await import('./dimensionLayer');

                return await buildDotNetDimensionLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'OpenStreetMapLayer':
            let { buildJsOpenStreetMapLayer } = await import('./openStreetMapLayer');

            return await buildJsOpenStreetMapLayer(dotNetObject, layerId, viewId);


        case 'KMLLayer':
            let { buildJsKMLLayer } = await import('./kMLLayer');

            return await buildJsKMLLayer(dotNetObject, layerId, viewId);


        case 'BuildingSceneLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetBuildingSceneLayer } = await import('./buildingSceneLayer');

                return await buildDotNetBuildingSceneLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'ElevationLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetElevationLayer } = await import('./elevationLayer');

                return await buildDotNetElevationLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'TileLayer':
            let { buildJsTileLayer } = await import('./tileLayer');

            return await buildJsTileLayer(dotNetObject, layerId, viewId);


        case 'OrientedImageryLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetOrientedImageryLayer } = await import('./orientedImageryLayer');

                return await buildDotNetOrientedImageryLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'ImageryTileLayer':
            let { buildJsImageryTileLayer } = await import('./imageryTileLayer');

            return await buildJsImageryTileLayer(dotNetObject, layerId, viewId);


        case 'GeoRSSLayer':
            let { buildJsGeoRSSLayer } = await import('./geoRSSLayer');

            return await buildJsGeoRSSLayer(dotNetObject, layerId, viewId);


        case 'UnknownLayer':
            let { buildJsUnknownLayer } = await import('./unknownLayer');

            return await buildJsUnknownLayer(dotNetObject, layerId, viewId);


        case 'SceneLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetSceneLayer } = await import('./sceneLayer');

                return await buildDotNetSceneLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'GroupLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetGroupLayer } = await import('./groupLayer');

                return await buildDotNetGroupLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'RouteLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetRouteLayer } = await import('./routeLayer');

                return await buildDotNetRouteLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'VectorTileLayer':
            let { buildJsVectorTileLayer } = await import('./vectorTileLayer');

            return await buildJsVectorTileLayer(dotNetObject, layerId, viewId);


        case 'MapImageLayer':
            let { buildJsMapImageLayer } = await import('./mapImageLayer');

            return await buildJsMapImageLayer(dotNetObject, layerId, viewId);


        case 'WMTSLayer':
            let { buildJsWMTSLayer } = await import('./wMTSLayer');

            return await buildJsWMTSLayer(dotNetObject, layerId, viewId);


        case 'WCSLayer':
            let { buildJsWCSLayer } = await import('./wCSLayer');

            return await buildJsWCSLayer(dotNetObject, layerId, viewId);


        case 'MediaLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetMediaLayer } = await import('./mediaLayer');

                return await buildDotNetMediaLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'CatalogFootprintLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetCatalogFootprintLayer } = await import('./catalogFootprintLayer');

                return await buildDotNetCatalogFootprintLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'OGCFeatureLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetOGCFeatureLayer } = await import('./oGCFeatureLayer');

                return await buildDotNetOGCFeatureLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'IntegratedMeshLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetIntegratedMeshLayer } = await import('./integratedMeshLayer');

                return await buildDotNetIntegratedMeshLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'ParquetLayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetParquetLayer } = await import('./parquetLayer');

                return await buildDotNetParquetLayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        case 'Sublayer':
            let { buildJsSublayer } = await import('./sublayer');

            return await buildJsSublayer(dotNetObject, layerId, viewId);


        case 'SubtypeSublayer':
            if (!Pro) {
                return sanitize(dotNetObject);
            }

            try {
                // @ts-ignore GeoBlazor Pro only
                let { buildDotNetSubtypeSublayer } = await import('./subtypeSublayer');

                return await buildDotNetSubtypeSublayer(dotNetObject, layerId, viewId);
            } catch (e) {
                throw e;
            }
        default: 
            return sanitize(dotNetObject);
    }
}     


export async function buildDotNetILayerGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    switch (jsObject.declaredClass) {
        case 'esri.layers.GraphicsLayer':
            let { buildDotNetGraphicsLayer } = await import('./graphicsLayer');

            return await buildDotNetGraphicsLayer(jsObject, layerId, viewId);
        case 'esri.layers.WebTileLayer':
            let { buildDotNetWebTileLayer } = await import('./webTileLayer');

            return await buildDotNetWebTileLayer(jsObject, layerId, viewId);
        case 'esri.layers.ImageryLayer':
            let { buildDotNetImageryLayer } = await import('./imageryLayer');

            return await buildDotNetImageryLayer(jsObject, layerId, viewId);
        case 'esri.layers.UnsupportedLayer':
            let { buildDotNetUnsupportedLayer } = await import('./unsupportedLayer');

            return await buildDotNetUnsupportedLayer(jsObject, layerId, viewId);
        case 'esri.layers.WFSLayer':
            let { buildDotNetWFSLayer } = await import('./wFSLayer');

            return await buildDotNetWFSLayer(jsObject, layerId, viewId);
        case 'esri.layers.StreamLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetStreamLayer } = await import('./streamLayer');

                return await buildDotNetStreamLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.KnowledgeGraphLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetKnowledgeGraphLayer } = await import('./knowledgeGraphLayer');

                return await buildDotNetKnowledgeGraphLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.KnowledgeGraphSublayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetKnowledgeGraphSublayer } = await import('./knowledgeGraphSublayer');

                return await buildDotNetKnowledgeGraphSublayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.GeoJSONLayer':
            let { buildDotNetGeoJSONLayer } = await import('./geoJSONLayer');

            return await buildDotNetGeoJSONLayer(jsObject, layerId, viewId);
        case 'esri.layers.LinkChartLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetLinkChartLayer } = await import('./linkChartLayer');

                return await buildDotNetLinkChartLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.LineOfSightLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetLineOfSightLayer } = await import('./lineOfSightLayer');

                return await buildDotNetLineOfSightLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.SubtypeGroupLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetSubtypeGroupLayer } = await import('./subtypeGroupLayer');

                return await buildDotNetSubtypeGroupLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.CatalogDynamicGroupLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetCatalogDynamicGroupLayer } = await import('./catalogDynamicGroupLayer');

                return await buildDotNetCatalogDynamicGroupLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.MapNotesLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetMapNotesLayer } = await import('./mapNotesLayer');

                return await buildDotNetMapNotesLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.IntegratedMesh3DTilesLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetIntegratedMesh3DTilesLayer } = await import('./integratedMesh3DTilesLayer');

                return await buildDotNetIntegratedMesh3DTilesLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.VoxelLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetVoxelLayer } = await import('./voxelLayer');

                return await buildDotNetVoxelLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.CSVLayer':
            let { buildDotNetCSVLayer } = await import('./cSVLayer');

            return await buildDotNetCSVLayer(jsObject, layerId, viewId);
        case 'esri.layers.CatalogLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetCatalogLayer } = await import('./catalogLayer');

                return await buildDotNetCatalogLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.PointCloudLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetPointCloudLayer } = await import('./pointCloudLayer');

                return await buildDotNetPointCloudLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.VideoLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetVideoLayer } = await import('./videoLayer');

                return await buildDotNetVideoLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.ViewshedLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetViewshedLayer } = await import('./viewshedLayer');

                return await buildDotNetViewshedLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.WMSLayer':
            let { buildDotNetWMSLayer } = await import('./wMSLayer');

            return await buildDotNetWMSLayer(jsObject, layerId, viewId);
        case 'esri.layers.FeatureLayer':
            let { buildDotNetFeatureLayer } = await import('./featureLayer');

            return await buildDotNetFeatureLayer(jsObject, layerId, viewId);
        case 'esri.layers.DimensionLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetDimensionLayer } = await import('./dimensionLayer');

                return await buildDotNetDimensionLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.OpenStreetMapLayer':
            let { buildDotNetOpenStreetMapLayer } = await import('./openStreetMapLayer');

            return await buildDotNetOpenStreetMapLayer(jsObject, layerId, viewId);
        case 'esri.layers.KMLLayer':
            let { buildDotNetKMLLayer } = await import('./kMLLayer');

            return await buildDotNetKMLLayer(jsObject, layerId, viewId);
        case 'esri.layers.BuildingSceneLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetBuildingSceneLayer } = await import('./buildingSceneLayer');

                return await buildDotNetBuildingSceneLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.ElevationLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetElevationLayer } = await import('./elevationLayer');

                return await buildDotNetElevationLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.TileLayer':
            let { buildDotNetTileLayer } = await import('./tileLayer');

            return await buildDotNetTileLayer(jsObject, layerId, viewId);
        case 'esri.layers.OrientedImageryLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetOrientedImageryLayer } = await import('./orientedImageryLayer');

                return await buildDotNetOrientedImageryLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.ImageryTileLayer':
            let { buildDotNetImageryTileLayer } = await import('./imageryTileLayer');

            return await buildDotNetImageryTileLayer(jsObject, layerId, viewId);
        case 'esri.layers.GeoRSSLayer':
            let { buildDotNetGeoRSSLayer } = await import('./geoRSSLayer');

            return await buildDotNetGeoRSSLayer(jsObject, layerId, viewId);
        case 'esri.layers.UnknownLayer':
            let { buildDotNetUnknownLayer } = await import('./unknownLayer');

            return await buildDotNetUnknownLayer(jsObject, layerId, viewId);
        case 'esri.layers.SceneLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetSceneLayer } = await import('./sceneLayer');

                return await buildDotNetSceneLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.GroupLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetGroupLayer } = await import('./groupLayer');

                return await buildDotNetGroupLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.RouteLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetRouteLayer } = await import('./routeLayer');

                return await buildDotNetRouteLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.VectorTileLayer':
            let { buildDotNetVectorTileLayer } = await import('./vectorTileLayer');

            return await buildDotNetVectorTileLayer(jsObject, layerId, viewId);
        case 'esri.layers.MapImageLayer':
            let { buildDotNetMapImageLayer } = await import('./mapImageLayer');

            return await buildDotNetMapImageLayer(jsObject, layerId, viewId);
        case 'esri.layers.WMTSLayer':
            let { buildDotNetWMTSLayer } = await import('./wMTSLayer');

            return await buildDotNetWMTSLayer(jsObject, layerId, viewId);
        case 'esri.layers.WCSLayer':
            let { buildDotNetWCSLayer } = await import('./wCSLayer');

            return await buildDotNetWCSLayer(jsObject, layerId, viewId);
        case 'esri.layers.MediaLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetMediaLayer } = await import('./mediaLayer');

                return await buildDotNetMediaLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.CatalogFootprintLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetCatalogFootprintLayer } = await import('./catalogFootprintLayer');

                return await buildDotNetCatalogFootprintLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.OGCFeatureLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetOGCFeatureLayer } = await import('./oGCFeatureLayer');

                return await buildDotNetOGCFeatureLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.IntegratedMeshLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetIntegratedMeshLayer } = await import('./integratedMeshLayer');

                return await buildDotNetIntegratedMeshLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.ParquetLayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetParquetLayer } = await import('./parquetLayer');

                return await buildDotNetParquetLayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        case 'esri.layers.Sublayer':
            let { buildDotNetSublayer } = await import('./sublayer');

            return await buildDotNetSublayer(jsObject, layerId, viewId);
        case 'esri.layers.SubtypeSublayer':
            if (!Pro) {
                return removeCircularReferences(jsObject);
            }
            
            try {
              // @ts-ignore GeoBlazor Pro only

                let { buildDotNetSubtypeSublayer } = await import('./subtypeSublayer');

                return await buildDotNetSubtypeSublayer(jsObject, layerId, viewId);
            } catch (e) {
                throw e;
                }

        default: 
            return removeCircularReferences(jsObject);
    }
}     


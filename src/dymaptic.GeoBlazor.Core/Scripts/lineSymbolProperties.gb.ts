// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { hasValue, sanitize } from './arcGisJsInterop';
export async function buildJsLineSymbolPropertiesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }
    
    switch (dotNetObject.type) {
        case 'any': 
            let { buildJsLineSymbolProperties } = await import('./lineSymbolProperties');
            return await buildJsLineSymbolProperties(dotNetObject, layerId, viewId);
        default: 
            return sanitize(dotNetObject);
    }
}     

export async function buildDotNetLineSymbolPropertiesGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    switch (jsObject.type) {
        case 'any': 
            let { buildDotNetLineSymbolProperties } = await import('./lineSymbolProperties');
            return await buildDotNetLineSymbolProperties(jsObject);
        default: 
            return jsObject;
    }
}     


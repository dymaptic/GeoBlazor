// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { ArcgisLocate } from '@arcgis/map-components/dist/components/arcgis-locate';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences, buildJsStreamReference } from './geoBlazorCore';
import { buildDotNetLocateWidget } from './locateWidget';
import { buildDotNetGraphic } from './graphic';
import { buildJsGraphic } from './graphic';
import BaseComponent from "./baseComponent";

export default class LocateWidgetGenerated extends BaseComponent {
    public widget: ArcgisLocate;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(widget: ArcgisLocate) {
        super(widget);
        this.widget = widget;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.graphic)) {
            this.widget.graphic = buildJsGraphic(dotNetObject.graphic);
        }


        if (hasValue(dotNetObject.autoDestroyDisabled)) {
            this.widget.autoDestroyDisabled = dotNetObject.autoDestroyDisabled;
        }
        if (hasValue(dotNetObject.geolocationOptions)) {
            this.widget.geolocationOptions = dotNetObject.geolocationOptions;
        }
        if (hasValue(dotNetObject.goToLocationDisabled)) {
            this.widget.goToLocationDisabled = dotNetObject.goToLocationDisabled;
        }
        if (hasValue(dotNetObject.icon)) {
            this.widget.icon = dotNetObject.icon;
        }
        if (hasValue(dotNetObject.label)) {
            this.widget.label = dotNetObject.label;
        }
        if (hasValue(dotNetObject.popupDisabled)) {
            this.widget.popupDisabled = dotNetObject.popupDisabled;
        }
        if (hasValue(dotNetObject.position)) {
            this.widget.position = dotNetObject.position;
        }
        if (hasValue(dotNetObject.scale)) {
            this.widget.scale = dotNetObject.scale;
        }
        if (hasValue(dotNetObject.state)) {
            this.widget.state = dotNetObject.state;
        }
    }
    
    // region methods
    async cancelLocate(): Promise<void> {
        await this.widget.cancelLocate();
    }

    async locate(): Promise<any> {
        let result = await this.widget.locate() as any;
        if (hasValue(result)) {
            return result;
        }
                               
        return null;
    }

    // region properties
    
    async getGraphic(): Promise<any> {
        
        if (hasValue(this.widget.graphic)) {
            return buildDotNetGraphic(this.widget.graphic, this.layerId, this.viewId);
        }
                               
        return null;
    }

    async setGraphic(value: any): Promise<void> {
        if (hasValue(value)) {
            this.widget.graphic = buildJsGraphic(value);
        }

    }

}


export async function buildJsLocateWidgetGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsArcgisLocate = document.getElementById(`widget-component-${dotNetObject.id}`) as ArcgisLocate;

    if (hasValue(viewId)) {
        jsArcgisLocate.referenceElement = document.querySelector(`#map-container-${viewId}`) as any;
    }
    if (hasValue(dotNetObject.hasGoToOverride) && dotNetObject.hasGoToOverride) {
        jsArcgisLocate.goToOverride = async (view,
        goToParameters) => {
            let { buildDotNetMapView } = await import('./mapView');

            let dnView = await buildDotNetMapView(view, layerId, viewId);
            let { buildDotNetGoToParameters } = await import('./goToParameters');

            let dnGoToParameters = buildDotNetGoToParameters(goToParameters, layerId, viewId);

            await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsGoToOverride', dnView,
            dnGoToParameters);
        };
    }
    if (hasValue(dotNetObject.graphic)) {
        jsArcgisLocate.graphic = buildJsGraphic(dotNetObject.graphic);
    }


    if (hasValue(dotNetObject.autoDestroyDisabled)) {
        jsArcgisLocate.autoDestroyDisabled = dotNetObject.autoDestroyDisabled;
    }

    if (hasValue(dotNetObject.geolocationOptions)) {
        jsArcgisLocate.geolocationOptions = dotNetObject.geolocationOptions;
    }

    if (hasValue(dotNetObject.goToLocationDisabled)) {
        jsArcgisLocate.goToLocationDisabled = dotNetObject.goToLocationDisabled;
    }

    if (hasValue(dotNetObject.icon)) {
        jsArcgisLocate.icon = dotNetObject.icon;
    }

    if (hasValue(dotNetObject.label)) {
        jsArcgisLocate.label = dotNetObject.label;
    }

    if (hasValue(dotNetObject.popupDisabled)) {
        jsArcgisLocate.popupDisabled = dotNetObject.popupDisabled;
    }

    if (hasValue(dotNetObject.position)) {
        jsArcgisLocate.position = dotNetObject.position;
    }

    if (hasValue(dotNetObject.scale)) {
        jsArcgisLocate.scale = dotNetObject.scale;
    }

    if (hasValue(dotNetObject.state)) {
        jsArcgisLocate.state = dotNetObject.state;
    }

    if (hasValue(dotNetObject.hasErrorListener) && dotNetObject.hasErrorListener) {
        jsArcgisLocate.addEventListener('error', async (evt: any) => {
                let streamRef = buildJsStreamReference(evt ?? {});
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsError', streamRef);
            });
    }

    if (hasValue(dotNetObject.hasPropertyChangeListener) && dotNetObject.hasPropertyChangeListener) {
        jsArcgisLocate.addEventListener('property-change', async (evt: any) => {
                let streamRef = buildJsStreamReference(evt ?? {});
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsPropertyChange', streamRef);
            });
    }

    if (hasValue(dotNetObject.hasReadyListener) && dotNetObject.hasReadyListener) {
        jsArcgisLocate.addEventListener('ready', async (evt: any) => {
                let streamRef = buildJsStreamReference(evt ?? {});
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsReady', streamRef);
            });
    }

    if (hasValue(dotNetObject.hasSuccessListener) && dotNetObject.hasSuccessListener) {
        jsArcgisLocate.addEventListener('success', async (evt: any) => {
                let streamRef = buildJsStreamReference(evt ?? {});
                await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsSuccess', streamRef);
            });
    }

    let { default: LocateWidgetWrapper } = await import('./locateWidget');

    let locateWidgetWrapper = new LocateWidgetWrapper(jsArcgisLocate);
    locateWidgetWrapper.geoBlazorId = dotNetObject.id;
    locateWidgetWrapper.viewId = viewId;
    locateWidgetWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = locateWidgetWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsArcgisLocate;
    
    // serialize data and send back to .NET to populate properties
    // we call requestAnimationFrame to pull this out of the synchronous render flow
    requestAnimationFrame(async () => {
        try {
            let jsObjectRef = DotNet.createJSObjectReference(locateWidgetWrapper);
            let { buildDotNetLocateWidget } = await import('./locateWidget');

            let dnInstantiatedObject = await buildDotNetLocateWidget(jsArcgisLocate, layerId, viewId);

            let dnStream = buildJsStreamReference(dnInstantiatedObject);
            await dotNetObject.dotNetComponentReference?.invokeMethodAsync('OnJsComponentCreated', 
                jsObjectRef, dnStream);
        } catch (e) {
            console.error('Error invoking OnJsComponentCreated for LocateWidget', e);
        }
    });
    
    return jsArcgisLocate;
}


export async function buildDotNetLocateWidgetGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetLocateWidget: any = {};
    
        if (hasValue(jsObject.graphic)) {
            dotNetLocateWidget.graphic = buildDotNetGraphic(jsObject.graphic, layerId, viewId);
        }

    if (hasValue(jsObject.autoDestroyDisabled)) {
        dotNetLocateWidget.autoDestroyDisabled = jsObject.autoDestroyDisabled;
    }

    if (hasValue(jsObject.geolocationOptions)) {
        dotNetLocateWidget.geolocationOptions = removeCircularReferences(jsObject.geolocationOptions);
    }

    if (hasValue(jsObject.goToLocationDisabled)) {
        dotNetLocateWidget.goToLocationDisabled = jsObject.goToLocationDisabled;
    }

    if (hasValue(jsObject.icon)) {
        dotNetLocateWidget.icon = jsObject.icon;
    }

    if (hasValue(jsObject.label)) {
        dotNetLocateWidget.label = jsObject.label;
    }

    if (hasValue(jsObject.popupDisabled)) {
        dotNetLocateWidget.popupDisabled = jsObject.popupDisabled;
    }

    if (hasValue(jsObject.position)) {
        dotNetLocateWidget.position = removeCircularReferences(jsObject.position);
    }

    if (hasValue(jsObject.scale)) {
        dotNetLocateWidget.scale = jsObject.scale;
    }

    if (hasValue(jsObject.state)) {
        dotNetLocateWidget.state = removeCircularReferences(jsObject.state);
    }

    if (hasValue(jsObject.type)) {
        dotNetLocateWidget.type = removeCircularReferences(jsObject.type);
    }


    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetLocateWidget.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetLocateWidget.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for LocateWidget', e);
            }
        }
    }

    if (hasValue(dotNetLocateWidget.id)) {
        if (!jsObjectRefs.hasOwnProperty(dotNetLocateWidget.id)) {
            let { default: LocateWidgetWrapper } = await import('./locateWidget');
            let locateWidgetWrapper = new LocateWidgetWrapper(jsObject);
            locateWidgetWrapper.geoBlazorId = dotNetLocateWidget.id;
            locateWidgetWrapper.viewId = viewId;
            locateWidgetWrapper.layerId = layerId;
            jsObjectRefs[dotNetLocateWidget.id] = locateWidgetWrapper;
        }
        arcGisObjectRefs[dotNetLocateWidget.id] ??= jsObject;
    }
    return dotNetLocateWidget;
}


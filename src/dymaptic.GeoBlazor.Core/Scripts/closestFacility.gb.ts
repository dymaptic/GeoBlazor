// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import closestFacility = __esri.closestFacility;
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class ClosestFacilityGenerated implements IPropertyWrapper {
    public component: closestFacility;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: closestFacility) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async solve(url: any,
        parameters: any,
        requestOptions: any): Promise<any> {
        let { buildJsClosestFacilityParameters } = await import('./closestFacilityParameters');
        let jsparameters = await buildJsClosestFacilityParameters(parameters, this.layerId, this.viewId) as any;
        return await this.component.solve(url,
            jsparameters,
            requestOptions);
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsClosestFacilityGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsclosestFacility: any = {}


    let { default: ClosestFacilityWrapper } = await import('./closestFacility');
    let closestFacilityWrapper = new ClosestFacilityWrapper(jsclosestFacility);
    closestFacilityWrapper.geoBlazorId = dotNetObject.id;
    closestFacilityWrapper.viewId = viewId;
    closestFacilityWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(closestFacilityWrapper);
    jsObjectRefs[dotNetObject.id] = closestFacilityWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsclosestFacility;
    let { buildDotNetClosestFacility } = await import('./closestFacility');
    let dnInstantiatedObject = await buildDotNetClosestFacility(jsclosestFacility);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ClosestFacility', e);
    }
    
    return jsclosestFacility;
}

export async function buildDotNetClosestFacilityGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetClosestFacility: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetClosestFacility.id = k;
                break;
            }
        }
    }

    return dotNetClosestFacility;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetTraceItem } from './traceItem';

export async function buildJsTraceItemGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsTraceItem: any = {};

    if (hasValue(dotNetObject.selected)) {
        jsTraceItem.selected = dotNetObject.selected;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsTraceItem);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsTraceItem;
    
    let { buildDotNetTraceItem } = await import('./traceItem');
    let dnInstantiatedObject = await buildDotNetTraceItem(jsTraceItem, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type TraceItem detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for TraceItem', e);
    }
    
    return jsTraceItem;
}


export async function buildDotNetTraceItemGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTraceItem: any = {};
    
    if (hasValue(jsObject.selected)) {
        dotNetTraceItem.selected = jsObject.selected;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetTraceItem.id = geoBlazorId;
    }

    return dotNetTraceItem;
}


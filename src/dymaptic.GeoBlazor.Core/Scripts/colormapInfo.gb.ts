// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import ColormapInfo from '@arcgis/core/renderers/support/ColormapInfo';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetColormapInfo } from './colormapInfo';

export async function buildJsColormapInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsColormapInfo = new ColormapInfo();
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsColormapInfo.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.label)) {
        jsColormapInfo.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.value)) {
        jsColormapInfo.value = dotNetObject.value;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsColormapInfo);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsColormapInfo;
    
    let dnInstantiatedObject = await buildDotNetColormapInfo(jsColormapInfo);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ColormapInfo', e);
    }
    
    return jsColormapInfo;
}

export async function buildDotNetColormapInfoGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetColormapInfo: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetColormapInfo.color = buildDotNetMapColor(jsObject.color);
        }
    if (hasValue(jsObject.label)) {
        dotNetColormapInfo.label = jsObject.label;
    }
    if (hasValue(jsObject.value)) {
        dotNetColormapInfo.value = jsObject.value;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetColormapInfo.id = k;
                break;
            }
        }
    }

    return dotNetColormapInfo;
}


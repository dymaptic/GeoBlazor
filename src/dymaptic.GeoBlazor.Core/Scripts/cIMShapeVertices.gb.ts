// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMShapeVertices } from './cIMShapeVertices';

export async function buildJsCIMShapeVerticesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMShapeVertices: any = {};

    if (hasValue(dotNetObject.indices)) {
        jsCIMShapeVertices.indices = dotNetObject.indices;
    }
    if (hasValue(dotNetObject.shapes)) {
        jsCIMShapeVertices.shapes = dotNetObject.shapes;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMShapeVertices);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMShapeVertices;
    
    let { buildDotNetCIMShapeVertices } = await import('./cIMShapeVertices');
    let dnInstantiatedObject = await buildDotNetCIMShapeVertices(jsCIMShapeVertices);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMShapeVertices detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMShapeVertices', e);
    }
    
    return jsCIMShapeVertices;
}


export async function buildDotNetCIMShapeVerticesGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMShapeVertices: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.indices)) {
        dotNetCIMShapeVertices.indices = jsObject.indices;
    }
    if (hasValue(jsObject.shapes)) {
        dotNetCIMShapeVertices.shapes = jsObject.shapes;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMShapeVertices.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMShapeVertices.id = geoBlazorId;
    }

    return dotNetCIMShapeVertices;
}


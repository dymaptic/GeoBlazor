// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMCalloutBase } from './cIMCalloutBase';

export async function buildJsCIMCalloutBaseGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMCalloutBase: any = {};

    if (hasValue(dotNetObject.leaderOffset)) {
        jsCIMCalloutBase.leaderOffset = dotNetObject.leaderOffset;
    }
    if (hasValue(dotNetObject.leaderTolerance)) {
        jsCIMCalloutBase.leaderTolerance = dotNetObject.leaderTolerance;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMCalloutBase);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMCalloutBase;
    
    let { buildDotNetCIMCalloutBase } = await import('./cIMCalloutBase');
    let dnInstantiatedObject = await buildDotNetCIMCalloutBase(jsCIMCalloutBase);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMCalloutBase detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMCalloutBase', e);
    }
    
    return jsCIMCalloutBase;
}


export async function buildDotNetCIMCalloutBaseGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMCalloutBase: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.leaderOffset)) {
        dotNetCIMCalloutBase.leaderOffset = jsObject.leaderOffset;
    }
    if (hasValue(jsObject.leaderTolerance)) {
        dotNetCIMCalloutBase.leaderTolerance = jsObject.leaderTolerance;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMCalloutBase.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMCalloutBase.id = geoBlazorId;
    }

    return dotNetCIMCalloutBase;
}


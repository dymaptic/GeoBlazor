// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import BasemapStyle from '@arcgis/core/support/BasemapStyle';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './arcGisJsInterop';

export async function buildDotNetBasemapStyleGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetBasemapStyle: any = {};
    
    if (hasValue(jsObject.apiKey)) {
        dotNetBasemapStyle.apiKey = jsObject.apiKey;
    }
    
    if (hasValue(jsObject.language)) {
        dotNetBasemapStyle.language = jsObject.language;
    }
    
    if (hasValue(jsObject.id)) {
        dotNetBasemapStyle.name = removeCircularReferences(jsObject.id);
    }
    
    if (hasValue(jsObject.places)) {
        dotNetBasemapStyle.places = removeCircularReferences(jsObject.places);
    }
    
    if (hasValue(jsObject.serviceUrl)) {
        dotNetBasemapStyle.serviceUrl = jsObject.serviceUrl;
    }
    
    if (hasValue(jsObject.worldview)) {
        dotNetBasemapStyle.worldview = jsObject.worldview;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetBasemapStyle.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        dotNetBasemapStyle.id = await dotNetRef.invokeMethodAsync('GetId');
    }
    if (hasValue(dotNetBasemapStyle.id)) {
        jsObjectRefs[dotNetBasemapStyle.id] ??= jsObject;
        arcGisObjectRefs[dotNetBasemapStyle.id] ??= jsObject;
    }

    return dotNetBasemapStyle;
}


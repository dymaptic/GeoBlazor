// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectRotate } from './cIMGeometricEffectRotate';

export async function buildJsCIMGeometricEffectRotateGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectRotate: any = {};

    if (hasValue(dotNetObject.angle)) {
        jsCIMGeometricEffectRotate.angle = dotNetObject.angle;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectRotate.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectRotate);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectRotate;
    
    let { buildDotNetCIMGeometricEffectRotate } = await import('./cIMGeometricEffectRotate');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectRotate(jsCIMGeometricEffectRotate);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectRotate detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectRotate', e);
    }
    
    return jsCIMGeometricEffectRotate;
}


export async function buildDotNetCIMGeometricEffectRotateGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectRotate: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.angle)) {
        dotNetCIMGeometricEffectRotate.angle = jsObject.angle;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectRotate.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectRotate.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectRotate.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectRotate;
}


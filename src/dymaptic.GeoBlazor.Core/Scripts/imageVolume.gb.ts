// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import ImageVolume from '@arcgis/core/rest/support/ImageVolume';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetImageVolume } from './imageVolume';

export async function buildJsImageVolumeGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.area)) {
        properties.area = dotNetObject.area;
    }
    if (hasValue(dotNetObject.cut)) {
        properties.cut = dotNetObject.cut;
    }
    if (hasValue(dotNetObject.cutCellCount)) {
        properties.cutCellCount = dotNetObject.cutCellCount;
    }
    if (hasValue(dotNetObject.fill)) {
        properties.fill = dotNetObject.fill;
    }
    if (hasValue(dotNetObject.fillCellCount)) {
        properties.fillCellCount = dotNetObject.fillCellCount;
    }
    if (hasValue(dotNetObject.maxZ)) {
        properties.maxZ = dotNetObject.maxZ;
    }
    if (hasValue(dotNetObject.meanZ)) {
        properties.meanZ = dotNetObject.meanZ;
    }
    if (hasValue(dotNetObject.minZ)) {
        properties.minZ = dotNetObject.minZ;
    }
    let jsImageVolume = new ImageVolume(properties);
    
    jsObjectRefs[dotNetObject.id] = jsImageVolume;
    arcGisObjectRefs[dotNetObject.id] = jsImageVolume;
    
    return jsImageVolume;
}


export async function buildDotNetImageVolumeGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetImageVolume: any = {};
    
    if (hasValue(jsObject.area)) {
        dotNetImageVolume.area = jsObject.area;
    }
    
    if (hasValue(jsObject.cut)) {
        dotNetImageVolume.cut = jsObject.cut;
    }
    
    if (hasValue(jsObject.cutCellCount)) {
        dotNetImageVolume.cutCellCount = jsObject.cutCellCount;
    }
    
    if (hasValue(jsObject.fill)) {
        dotNetImageVolume.fill = jsObject.fill;
    }
    
    if (hasValue(jsObject.fillCellCount)) {
        dotNetImageVolume.fillCellCount = jsObject.fillCellCount;
    }
    
    if (hasValue(jsObject.maxZ)) {
        dotNetImageVolume.maxZ = jsObject.maxZ;
    }
    
    if (hasValue(jsObject.meanZ)) {
        dotNetImageVolume.meanZ = jsObject.meanZ;
    }
    
    if (hasValue(jsObject.minZ)) {
        dotNetImageVolume.minZ = jsObject.minZ;
    }
    

    return dotNetImageVolume;
}


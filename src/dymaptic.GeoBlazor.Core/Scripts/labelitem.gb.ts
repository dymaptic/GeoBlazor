// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetLabelitem } from './labelitem';

export async function buildJsLabelitemGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsLabelitem: any = {};

    if (hasValue(dotNetObject.label)) {
        jsLabelitem.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.value)) {
        jsLabelitem.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsLabelitem);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsLabelitem;
    
    try {
        let { buildDotNetLabelitem } = await import('./labelitem');
        let dnInstantiatedObject = await buildDotNetLabelitem(jsLabelitem, layerId, viewId);

        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference?.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type Labelitem detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Labelitem', e);
    }
    
    return jsLabelitem;
}


export async function buildDotNetLabelitemGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetLabelitem: any = {};
    
    if (hasValue(jsObject.label)) {
        dotNetLabelitem.label = jsObject.label;
    }
    
    if (hasValue(jsObject.value)) {
        dotNetLabelitem.value = jsObject.value;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetLabelitem.id = geoBlazorId;
    }

    return dotNetLabelitem;
}


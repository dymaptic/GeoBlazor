// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetTypeSchemeForMesh } from './typeSchemeForMesh';
import TypeSchemeForMesh = __esri.TypeSchemeForMesh;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsTypeSchemeForMeshGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsTypeSchemeForMesh: any = {}
    if (hasValue(dotNetObject.colors)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsTypeSchemeForMesh.colors = dotNetObject.colors.map(i => buildJsMapColor(i)) as any;
    }
    if (hasValue(dotNetObject.noDataColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsTypeSchemeForMesh.noDataColor = buildJsMapColor(dotNetObject.noDataColor) as any;
    }

    if (hasValue(dotNetObject.name)) {
        jsTypeSchemeForMesh.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsTypeSchemeForMesh.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.tags)) {
        jsTypeSchemeForMesh.tags = dotNetObject.tags;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsTypeSchemeForMesh);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsTypeSchemeForMesh;
    
    let dnInstantiatedObject = await buildDotNetTypeSchemeForMesh(jsTypeSchemeForMesh);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for TypeSchemeForMesh', e);
    }
    
    return jsTypeSchemeForMesh;
}

export async function buildDotNetTypeSchemeForMeshGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTypeSchemeForMesh: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.colors)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetTypeSchemeForMesh.colors = jsObject.colors.map(i => buildDotNetMapColor(i));
        }
        if (hasValue(jsObject.noDataColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetTypeSchemeForMesh.noDataColor = buildDotNetMapColor(jsObject.noDataColor);
        }
        if (hasValue(jsObject.name)) {
            dotNetTypeSchemeForMesh.name = jsObject.name;
        }
        if (hasValue(jsObject.opacity)) {
            dotNetTypeSchemeForMesh.opacity = jsObject.opacity;
        }
        if (hasValue(jsObject.tags)) {
            dotNetTypeSchemeForMesh.tags = jsObject.tags;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetTypeSchemeForMesh.id = k;
                break;
            }
        }
    }

    return dotNetTypeSchemeForMesh;
}


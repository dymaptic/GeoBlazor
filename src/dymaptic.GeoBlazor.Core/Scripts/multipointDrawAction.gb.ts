// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file

import MultipointDrawAction = __esri.MultipointDrawAction;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class MultipointDrawActionGenerated implements IPropertyWrapper {
    public component: MultipointDrawAction;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: MultipointDrawAction) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async canRedo(): Promise<any> {
        return this.component.canRedo();
    }

    async canUndo(): Promise<any> {
        return this.component.canUndo();
    }

    async complete(): Promise<void> {
        this.component.complete();
    }

    async getCoordsAndPointFromScreenPoint(screenPoint: any): Promise<any> {
        return this.component.getCoordsAndPointFromScreenPoint(screenPoint);
    }

    async getCoordsFromScreenPoint(screenPoint: any): Promise<any> {
        return this.component.getCoordsFromScreenPoint(screenPoint);
    }

    async redo(): Promise<void> {
        this.component.redo();
    }

    async screenToMap(screenPoint: any): Promise<any> {
        return this.component.screenToMap(screenPoint);
    }

    async undo(): Promise<void> {
        this.component.undo();
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsMultipointDrawActionGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsMultipointDrawAction = new MultipointDrawAction();

    if (hasValue(dotNetObject.hasZ)) {
        jsMultipointDrawAction.hasZ = dotNetObject.hasZ;
    }
    if (hasValue(dotNetObject.view)) {
        jsMultipointDrawAction.view = dotNetObject.view;
    }
    jsMultipointDrawAction.on('cursor-update', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsCursorUpdate', evt);
    });
    
    jsMultipointDrawAction.on('draw-complete', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsDrawComplete', evt);
    });
    
    jsMultipointDrawAction.on('redo', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsRedo', evt);
    });
    
    jsMultipointDrawAction.on('undo', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsUndo', evt);
    });
    
    jsMultipointDrawAction.on('vertex-add', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsVertexAdd', evt);
    });
    
    jsMultipointDrawAction.on('vertex-remove', async (evt: any) => {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsVertexRemove', evt);
    });
    

    let { default: MultipointDrawActionWrapper } = await import('./multipointDrawAction');
    let multipointDrawActionWrapper = new MultipointDrawActionWrapper(jsMultipointDrawAction);
    multipointDrawActionWrapper.geoBlazorId = dotNetObject.id;
    multipointDrawActionWrapper.viewId = viewId;
    multipointDrawActionWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(multipointDrawActionWrapper);
    jsObjectRefs[dotNetObject.id] = multipointDrawActionWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsMultipointDrawAction;
    let { buildDotNetMultipointDrawAction } = await import('./multipointDrawAction');
    let dnInstantiatedObject = await buildDotNetMultipointDrawAction(jsMultipointDrawAction);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for MultipointDrawAction', e);
    }
    
    return jsMultipointDrawAction;
}
export async function buildDotNetMultipointDrawActionGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetMultipointDrawAction: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.hasZ)) {
            dotNetMultipointDrawAction.hasZ = jsObject.hasZ;
        }
        if (hasValue(jsObject.vertices)) {
            dotNetMultipointDrawAction.vertices = jsObject.vertices;
        }
        if (hasValue(jsObject.view)) {
            dotNetMultipointDrawAction.view = jsObject.view;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetMultipointDrawAction.id = k;
                break;
            }
        }
    }

    return dotNetMultipointDrawAction;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectRadial } from './cIMGeometricEffectRadial';

export async function buildJsCIMGeometricEffectRadialGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectRadial: any = {};

    if (hasValue(dotNetObject.angle)) {
        jsCIMGeometricEffectRadial.angle = dotNetObject.angle;
    }
    if (hasValue(dotNetObject.length)) {
        jsCIMGeometricEffectRadial.length = dotNetObject.length;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectRadial.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectRadial);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectRadial;
    
    let { buildDotNetCIMGeometricEffectRadial } = await import('./cIMGeometricEffectRadial');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectRadial(jsCIMGeometricEffectRadial);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectRadial detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectRadial', e);
    }
    
    return jsCIMGeometricEffectRadial;
}


export async function buildDotNetCIMGeometricEffectRadialGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectRadial: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.angle)) {
        dotNetCIMGeometricEffectRadial.angle = jsObject.angle;
    }
    if (hasValue(jsObject.length)) {
        dotNetCIMGeometricEffectRadial.length = jsObject.length;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectRadial.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectRadial.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectRadial.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectRadial;
}


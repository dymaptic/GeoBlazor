// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetFormatPosition } from './formatPosition';

export async function buildJsFormatPositionGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsFormatPosition: any = {};
    if (hasValue(dotNetObject.location)) {
        let { buildJsPoint } = await import('./point');
        jsFormatPosition.location = buildJsPoint(dotNetObject.location) as any;
    }

    if (hasValue(dotNetObject.coordinate)) {
        jsFormatPosition.coordinate = dotNetObject.coordinate;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsFormatPosition);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsFormatPosition;
    
    let dnInstantiatedObject = await buildDotNetFormatPosition(jsFormatPosition);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for FormatPosition', e);
    }
    
    return jsFormatPosition;
}

export async function buildDotNetFormatPositionGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFormatPosition: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.location)) {
            let { buildDotNetPoint } = await import('./point');
            dotNetFormatPosition.location = buildDotNetPoint(jsObject.location);
        }
    if (hasValue(jsObject.coordinate)) {
        dotNetFormatPosition.coordinate = jsObject.coordinate;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetFormatPosition.id = k;
                break;
            }
        }
    }

    return dotNetFormatPosition;
}


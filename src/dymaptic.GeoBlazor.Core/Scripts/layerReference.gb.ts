// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import LayerReference from '@arcgis/core/webscene/support/LayerReference';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';

export async function buildJsLayerReferenceGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.layerId)) {
        properties.layerId = dotNetObject.layerId;
    }
    if (hasValue(dotNetObject.sublayerId)) {
        properties.sublayerId = dotNetObject.sublayerId;
    }
    let jsLayerReference = new LayerReference(properties);
    
    jsObjectRefs[dotNetObject.id] = jsLayerReference;
    arcGisObjectRefs[dotNetObject.id] = jsLayerReference;
    
    return jsLayerReference;
}


export async function buildDotNetLayerReferenceGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetLayerReference: any = {};
    
    if (hasValue(jsObject.layerId)) {
        dotNetLayerReference.layerId = jsObject.layerId;
    }
    
    if (hasValue(jsObject.sublayerId)) {
        dotNetLayerReference.sublayerId = jsObject.sublayerId;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetLayerReference.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetLayerReference.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for LayerReference', e);
            }
        }
    }
    if (hasValue(dotNetLayerReference.id)) {
        jsObjectRefs[dotNetLayerReference.id] ??= jsObject;
        arcGisObjectRefs[dotNetLayerReference.id] ??= jsObject;
    }

    return dotNetLayerReference;
}


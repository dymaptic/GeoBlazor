// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import SliceAnalysis from '@arcgis/core/analysis/SliceAnalysis';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './geoBlazorCore';

export async function buildJsSliceAnalysisGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.excludedLayers) && dotNetObject.excludedLayers.length > 0) {
        properties.excludedLayers = dotNetObject.excludedLayers;
    }

    if (hasValue(dotNetObject.analysisId)) {
        properties.id = dotNetObject.analysisId;
    }
    if (hasValue(dotNetObject.excludeGroundSurface)) {
        properties.excludeGroundSurface = dotNetObject.excludeGroundSurface;
    }
    if (hasValue(dotNetObject.origin)) {
        properties.origin = dotNetObject.origin;
    }
    if (hasValue(dotNetObject.shape)) {
        properties.shape = dotNetObject.shape;
    }
    if (hasValue(dotNetObject.tiltEnabled)) {
        properties.tiltEnabled = dotNetObject.tiltEnabled;
    }
    let jsSliceAnalysis = new SliceAnalysis(properties);
    
    jsObjectRefs[dotNetObject.id] = jsSliceAnalysis;
    arcGisObjectRefs[dotNetObject.id] = jsSliceAnalysis;
    
    return jsSliceAnalysis;
}


export async function buildDotNetSliceAnalysisGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSliceAnalysis: any = {};
    
    if (hasValue(jsObject.id)) {
        dotNetSliceAnalysis.analysisId = jsObject.id;
    }
    
    if (hasValue(jsObject.excludedLayers)) {
        dotNetSliceAnalysis.excludedLayers = removeCircularReferences(jsObject.excludedLayers);
    }
    
    if (hasValue(jsObject.excludeGroundSurface)) {
        dotNetSliceAnalysis.excludeGroundSurface = jsObject.excludeGroundSurface;
    }
    
    if (hasValue(jsObject.origin)) {
        dotNetSliceAnalysis.origin = removeCircularReferences(jsObject.origin);
    }
    
    if (hasValue(jsObject.shape)) {
        dotNetSliceAnalysis.shape = removeCircularReferences(jsObject.shape);
    }
    
    if (hasValue(jsObject.tiltEnabled)) {
        dotNetSliceAnalysis.tiltEnabled = jsObject.tiltEnabled;
    }
    
    if (hasValue(jsObject.type)) {
        dotNetSliceAnalysis.type = removeCircularReferences(jsObject.type);
    }
    
    if (hasValue(jsObject.valid)) {
        dotNetSliceAnalysis.valid = jsObject.valid;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetSliceAnalysis.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetSliceAnalysis.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for SliceAnalysis', e);
            }
        }
    }
    if (hasValue(dotNetSliceAnalysis.id)) {
        jsObjectRefs[dotNetSliceAnalysis.id] ??= jsObject;
        arcGisObjectRefs[dotNetSliceAnalysis.id] ??= jsObject;
    }

    return dotNetSliceAnalysis;
}


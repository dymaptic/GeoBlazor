// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import ColorBackground from '@arcgis/core/webmap/background/ColorBackground';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';

export async function buildJsColorBackgroundGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        properties.color = buildJsMapColor(dotNetObject.color) as any;
    }

    let jsColorBackground = new ColorBackground(properties);
    
    jsObjectRefs[dotNetObject.id] = jsColorBackground;
    arcGisObjectRefs[dotNetObject.id] = jsColorBackground;
    
    return jsColorBackground;
}


export async function buildDotNetColorBackgroundGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetColorBackground: any = {};
    
    if (hasValue(jsObject.color)) {
        let { buildDotNetMapColor } = await import('./mapColor');
        dotNetColorBackground.color = buildDotNetMapColor(jsObject.color);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetColorBackground.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetColorBackground.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for ColorBackground', e);
            }
        }
    }
    if (hasValue(dotNetColorBackground.id)) {
        jsObjectRefs[dotNetColorBackground.id] ??= jsObject;
        arcGisObjectRefs[dotNetColorBackground.id] ??= jsObject;
    }

    return dotNetColorBackground;
}


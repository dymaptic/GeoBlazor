// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetHeatmapScheme } from './heatmapScheme';

export async function buildJsHeatmapSchemeGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsHeatmapScheme: any = {}
    if (hasValue(dotNetObject.colors)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsHeatmapScheme.colors = dotNetObject.colors.map(i => buildJsMapColor(i)) as any;
    }

    if (hasValue(dotNetObject.heatmapSchemeId)) {
        jsHeatmapScheme.id = dotNetObject.heatmapSchemeId;
    }
    if (hasValue(dotNetObject.name)) {
        jsHeatmapScheme.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsHeatmapScheme.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.tags)) {
        jsHeatmapScheme.tags = dotNetObject.tags;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsHeatmapScheme);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsHeatmapScheme;
    
    let dnInstantiatedObject = await buildDotNetHeatmapScheme(jsHeatmapScheme);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for HeatmapScheme', e);
    }
    
    return jsHeatmapScheme;
}

export async function buildDotNetHeatmapSchemeGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetHeatmapScheme: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.colors)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetHeatmapScheme.colors = jsObject.colors.map(i => buildDotNetMapColor(i));
        }
    if (hasValue(jsObject.id)) {
        dotNetHeatmapScheme.heatmapSchemeId = jsObject.id;
    }
    if (hasValue(jsObject.name)) {
        dotNetHeatmapScheme.name = jsObject.name;
    }
    if (hasValue(jsObject.opacity)) {
        dotNetHeatmapScheme.opacity = jsObject.opacity;
    }
    if (hasValue(jsObject.tags)) {
        dotNetHeatmapScheme.tags = jsObject.tags;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetHeatmapScheme.id = k;
                break;
            }
        }
    }

    return dotNetHeatmapScheme;
}


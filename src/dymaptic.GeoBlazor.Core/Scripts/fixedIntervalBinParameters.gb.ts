// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import FixedIntervalBinParameters from '@arcgis/core/rest/support/FixedIntervalBinParameters';
import { arcGisObjectRefs, jsObjectRefs, hasValue, removeCircularReferences } from './arcGisJsInterop';
import { buildDotNetFixedIntervalBinParameters } from './fixedIntervalBinParameters';

export async function buildJsFixedIntervalBinParametersGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.splitBy)) {
        let { buildJsAttributeBinsGrouping } = await import('./attributeBinsGrouping');
        properties.splitBy = await buildJsAttributeBinsGrouping(dotNetObject.splitBy, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.stackBy)) {
        let { buildJsAttributeBinsGrouping } = await import('./attributeBinsGrouping');
        properties.stackBy = await buildJsAttributeBinsGrouping(dotNetObject.stackBy, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.end)) {
        properties.end = dotNetObject.end;
    }
    if (hasValue(dotNetObject.expression)) {
        properties.expression = dotNetObject.expression;
    }
    if (hasValue(dotNetObject.expressionValueType)) {
        properties.expressionValueType = dotNetObject.expressionValueType;
    }
    if (hasValue(dotNetObject.field)) {
        properties.field = dotNetObject.field;
    }
    if (hasValue(dotNetObject.firstDayOfWeek)) {
        properties.firstDayOfWeek = dotNetObject.firstDayOfWeek;
    }
    if (hasValue(dotNetObject.hideUpperBound)) {
        properties.hideUpperBound = dotNetObject.hideUpperBound;
    }
    if (hasValue(dotNetObject.interval)) {
        properties.interval = dotNetObject.interval;
    }
    if (hasValue(dotNetObject.normalizationField)) {
        properties.normalizationField = dotNetObject.normalizationField;
    }
    if (hasValue(dotNetObject.normalizationMaxValue)) {
        properties.normalizationMaxValue = dotNetObject.normalizationMaxValue;
    }
    if (hasValue(dotNetObject.normalizationMinValue)) {
        properties.normalizationMinValue = dotNetObject.normalizationMinValue;
    }
    if (hasValue(dotNetObject.normalizationTotal)) {
        properties.normalizationTotal = dotNetObject.normalizationTotal;
    }
    if (hasValue(dotNetObject.normalizationType)) {
        properties.normalizationType = dotNetObject.normalizationType;
    }
    if (hasValue(dotNetObject.start)) {
        properties.start = dotNetObject.start;
    }
    let jsFixedIntervalBinParameters = new FixedIntervalBinParameters(properties);
    
    jsObjectRefs[dotNetObject.id] = jsFixedIntervalBinParameters;
    arcGisObjectRefs[dotNetObject.id] = jsFixedIntervalBinParameters;
    
    return jsFixedIntervalBinParameters;
}


export async function buildDotNetFixedIntervalBinParametersGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFixedIntervalBinParameters: any = {};
    
    if (hasValue(jsObject.splitBy)) {
        let { buildDotNetAttributeBinsGrouping } = await import('./attributeBinsGrouping');
        dotNetFixedIntervalBinParameters.splitBy = await buildDotNetAttributeBinsGrouping(jsObject.splitBy);
    }
    
    if (hasValue(jsObject.stackBy)) {
        let { buildDotNetAttributeBinsGrouping } = await import('./attributeBinsGrouping');
        dotNetFixedIntervalBinParameters.stackBy = await buildDotNetAttributeBinsGrouping(jsObject.stackBy);
    }
    
    if (hasValue(jsObject.end)) {
        dotNetFixedIntervalBinParameters.end = jsObject.end;
    }
    
    if (hasValue(jsObject.expression)) {
        dotNetFixedIntervalBinParameters.expression = jsObject.expression;
    }
    
    if (hasValue(jsObject.expressionValueType)) {
        dotNetFixedIntervalBinParameters.expressionValueType = removeCircularReferences(jsObject.expressionValueType);
    }
    
    if (hasValue(jsObject.field)) {
        dotNetFixedIntervalBinParameters.field = jsObject.field;
    }
    
    if (hasValue(jsObject.firstDayOfWeek)) {
        dotNetFixedIntervalBinParameters.firstDayOfWeek = jsObject.firstDayOfWeek;
    }
    
    if (hasValue(jsObject.hideUpperBound)) {
        dotNetFixedIntervalBinParameters.hideUpperBound = jsObject.hideUpperBound;
    }
    
    if (hasValue(jsObject.interval)) {
        dotNetFixedIntervalBinParameters.interval = jsObject.interval;
    }
    
    if (hasValue(jsObject.normalizationField)) {
        dotNetFixedIntervalBinParameters.normalizationField = jsObject.normalizationField;
    }
    
    if (hasValue(jsObject.normalizationMaxValue)) {
        dotNetFixedIntervalBinParameters.normalizationMaxValue = jsObject.normalizationMaxValue;
    }
    
    if (hasValue(jsObject.normalizationMinValue)) {
        dotNetFixedIntervalBinParameters.normalizationMinValue = jsObject.normalizationMinValue;
    }
    
    if (hasValue(jsObject.normalizationTotal)) {
        dotNetFixedIntervalBinParameters.normalizationTotal = jsObject.normalizationTotal;
    }
    
    if (hasValue(jsObject.normalizationType)) {
        dotNetFixedIntervalBinParameters.normalizationType = removeCircularReferences(jsObject.normalizationType);
    }
    
    if (hasValue(jsObject.start)) {
        dotNetFixedIntervalBinParameters.start = jsObject.start;
    }
    
    if (hasValue(jsObject.type)) {
        dotNetFixedIntervalBinParameters.type = removeCircularReferences(jsObject.type);
    }
    

    return dotNetFixedIntervalBinParameters;
}


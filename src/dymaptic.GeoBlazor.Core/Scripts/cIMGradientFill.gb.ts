// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGradientFill } from './cIMGradientFill';

export async function buildJsCIMGradientFillGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGradientFill: any = {};
    if (hasValue(dotNetObject.effects)) {
        let { buildJsICIMGeometricEffectType } = await import('./iCIMGeometricEffectType');
        jsCIMGradientFill.effects = dotNetObject.effects.map(i => buildJsICIMGeometricEffectType(i)) as any;
    }

    if (hasValue(dotNetObject.angle)) {
        jsCIMGradientFill.angle = dotNetObject.angle;
    }
    if (hasValue(dotNetObject.colorLocked)) {
        jsCIMGradientFill.colorLocked = dotNetObject.colorLocked;
    }
    if (hasValue(dotNetObject.colorRamp)) {
        jsCIMGradientFill.colorRamp = dotNetObject.colorRamp;
    }
    if (hasValue(dotNetObject.enable)) {
        jsCIMGradientFill.enable = dotNetObject.enable;
    }
    if (hasValue(dotNetObject.gradientMethod)) {
        jsCIMGradientFill.gradientMethod = dotNetObject.gradientMethod;
    }
    if (hasValue(dotNetObject.gradientSize)) {
        jsCIMGradientFill.gradientSize = dotNetObject.gradientSize;
    }
    if (hasValue(dotNetObject.gradientSizeUnits)) {
        jsCIMGradientFill.gradientSizeUnits = dotNetObject.gradientSizeUnits;
    }
    if (hasValue(dotNetObject.gradientType)) {
        jsCIMGradientFill.gradientType = dotNetObject.gradientType;
    }
    if (hasValue(dotNetObject.interval)) {
        jsCIMGradientFill.interval = dotNetObject.interval;
    }
    if (hasValue(dotNetObject.name)) {
        jsCIMGradientFill.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.overprint)) {
        jsCIMGradientFill.overprint = dotNetObject.overprint;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGradientFill.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGradientFill);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGradientFill;
    
    let { buildDotNetCIMGradientFill } = await import('./cIMGradientFill');
    let dnInstantiatedObject = await buildDotNetCIMGradientFill(jsCIMGradientFill);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGradientFill detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGradientFill', e);
    }
    
    return jsCIMGradientFill;
}


export async function buildDotNetCIMGradientFillGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGradientFill: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.effects)) {
        let { buildDotNetICIMGeometricEffectType } = await import('./iCIMGeometricEffectType');
        dotNetCIMGradientFill.effects = jsObject.effects.map(i => buildDotNetICIMGeometricEffectType(i));
    }
    if (hasValue(jsObject.angle)) {
        dotNetCIMGradientFill.angle = jsObject.angle;
    }
    if (hasValue(jsObject.colorLocked)) {
        dotNetCIMGradientFill.colorLocked = jsObject.colorLocked;
    }
    if (hasValue(jsObject.colorRamp)) {
        dotNetCIMGradientFill.colorRamp = jsObject.colorRamp;
    }
    if (hasValue(jsObject.enable)) {
        dotNetCIMGradientFill.enable = jsObject.enable;
    }
    if (hasValue(jsObject.gradientMethod)) {
        dotNetCIMGradientFill.gradientMethod = jsObject.gradientMethod;
    }
    if (hasValue(jsObject.gradientSize)) {
        dotNetCIMGradientFill.gradientSize = jsObject.gradientSize;
    }
    if (hasValue(jsObject.gradientSizeUnits)) {
        dotNetCIMGradientFill.gradientSizeUnits = jsObject.gradientSizeUnits;
    }
    if (hasValue(jsObject.gradientType)) {
        dotNetCIMGradientFill.gradientType = jsObject.gradientType;
    }
    if (hasValue(jsObject.interval)) {
        dotNetCIMGradientFill.interval = jsObject.interval;
    }
    if (hasValue(jsObject.name)) {
        dotNetCIMGradientFill.name = jsObject.name;
    }
    if (hasValue(jsObject.overprint)) {
        dotNetCIMGradientFill.overprint = jsObject.overprint;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGradientFill.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGradientFill.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGradientFill.id = geoBlazorId;
    }

    return dotNetCIMGradientFill;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectArrow } from './cIMGeometricEffectArrow';

export async function buildJsCIMGeometricEffectArrowGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectArrow: any = {};

    if (hasValue(dotNetObject.geometricEffectArrowType)) {
        jsCIMGeometricEffectArrow.geometricEffectArrowType = dotNetObject.geometricEffectArrowType;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectArrow.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.width)) {
        jsCIMGeometricEffectArrow.width = dotNetObject.width;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectArrow);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectArrow;
    
    let { buildDotNetCIMGeometricEffectArrow } = await import('./cIMGeometricEffectArrow');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectArrow(jsCIMGeometricEffectArrow);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectArrow detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectArrow', e);
    }
    
    return jsCIMGeometricEffectArrow;
}


export async function buildDotNetCIMGeometricEffectArrowGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectArrow: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.geometricEffectArrowType)) {
        dotNetCIMGeometricEffectArrow.geometricEffectArrowType = jsObject.geometricEffectArrowType;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectArrow.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectArrow.type = jsObject.type;
    }
    if (hasValue(jsObject.width)) {
        dotNetCIMGeometricEffectArrow.width = jsObject.width;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectArrow.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectArrow;
}


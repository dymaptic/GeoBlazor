// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetStopInfo } from './stopInfo';
import StopInfo = __esri.StopInfo;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsStopInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsStopInfo: any = {}
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsStopInfo.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.offset)) {
        jsStopInfo.offset = dotNetObject.offset;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsStopInfo);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsStopInfo;
    
    let dnInstantiatedObject = await buildDotNetStopInfo(jsStopInfo);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for StopInfo', e);
    }
    
    return jsStopInfo;
}

export async function buildDotNetStopInfoGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetStopInfo: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetStopInfo.color = buildDotNetMapColor(jsObject.color);
        }
        if (hasValue(jsObject.offset)) {
            dotNetStopInfo.offset = jsObject.offset;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetStopInfo.id = k;
                break;
            }
        }
    }

    return dotNetStopInfo;
}


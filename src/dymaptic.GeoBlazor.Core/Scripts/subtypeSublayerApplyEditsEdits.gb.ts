// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetSubtypeSublayerApplyEditsEdits } from './subtypeSublayerApplyEditsEdits';

export async function buildJsSubtypeSublayerApplyEditsEditsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsSubtypeSublayerApplyEditsEdits: any = {};
    if (hasValue(dotNetObject.addAttachments) && dotNetObject.addAttachments.length > 0) {
        let { buildJsAttachmentEdit } = await import('./attachmentEdit');
        jsSubtypeSublayerApplyEditsEdits.addAttachments = await Promise.all(dotNetObject.addAttachments.map(async i => await buildJsAttachmentEdit(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.addFeatures) && dotNetObject.addFeatures.length > 0) {
        let { buildJsGraphic } = await import('./graphic');
        jsSubtypeSublayerApplyEditsEdits.addFeatures = dotNetObject.addFeatures.map(i => buildJsGraphic(i)) as any;
    }
    if (hasValue(dotNetObject.graphicCollectionDeleteFeatures) && dotNetObject.graphicCollectionDeleteFeatures.length > 0) {
        let { buildJsGraphic } = await import('./graphic');
        jsSubtypeSublayerApplyEditsEdits.deleteFeatures = dotNetObject.graphicCollectionDeleteFeatures.map(i => buildJsGraphic(i)) as any;
    }
    else if (hasValue(dotNetObject.stringCollectionDeleteFeatures) && dotNetObject.stringCollectionDeleteFeatures.length > 0) {
        jsSubtypeSublayerApplyEditsEdits.deleteFeatures = dotNetObject.stringCollectionDeleteFeatures;
    }
    if (hasValue(dotNetObject.updateAttachments) && dotNetObject.updateAttachments.length > 0) {
        let { buildJsAttachmentEdit } = await import('./attachmentEdit');
        jsSubtypeSublayerApplyEditsEdits.updateAttachments = await Promise.all(dotNetObject.updateAttachments.map(async i => await buildJsAttachmentEdit(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.updateFeatures) && dotNetObject.updateFeatures.length > 0) {
        let { buildJsGraphic } = await import('./graphic');
        jsSubtypeSublayerApplyEditsEdits.updateFeatures = dotNetObject.updateFeatures.map(i => buildJsGraphic(i)) as any;
    }

    if (hasValue(dotNetObject.deleteAttachments) && dotNetObject.deleteAttachments.length > 0) {
        jsSubtypeSublayerApplyEditsEdits.deleteAttachments = dotNetObject.deleteAttachments;
    }
    
    jsObjectRefs[dotNetObject.id] = jsSubtypeSublayerApplyEditsEdits;
    arcGisObjectRefs[dotNetObject.id] = jsSubtypeSublayerApplyEditsEdits;
    
    return jsSubtypeSublayerApplyEditsEdits;
}


export async function buildDotNetSubtypeSublayerApplyEditsEditsGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSubtypeSublayerApplyEditsEdits: any = {};
    
    if (hasValue(jsObject.addAttachments)) {
        let { buildDotNetAttachmentEdit } = await import('./attachmentEdit');
        dotNetSubtypeSublayerApplyEditsEdits.addAttachments = await Promise.all(jsObject.addAttachments.map(async i => await buildDotNetAttachmentEdit(i, layerId, viewId)));
    }
    
    if (hasValue(jsObject.addFeatures)) {
        let { buildDotNetGraphic } = await import('./graphic');
        dotNetSubtypeSublayerApplyEditsEdits.addFeatures = jsObject.addFeatures.map(i => buildDotNetGraphic(i, layerId, viewId));
    }
    
    if (hasValue(jsObject.deleteFeatures)) {
        let { buildDotNetGraphic } = await import('./graphic');
        dotNetSubtypeSublayerApplyEditsEdits.deleteFeatures = jsObject.deleteFeatures.map(i => buildDotNetGraphic(i, layerId, viewId));
    }
    
    if (hasValue(jsObject.updateAttachments)) {
        let { buildDotNetAttachmentEdit } = await import('./attachmentEdit');
        dotNetSubtypeSublayerApplyEditsEdits.updateAttachments = await Promise.all(jsObject.updateAttachments.map(async i => await buildDotNetAttachmentEdit(i, layerId, viewId)));
    }
    
    if (hasValue(jsObject.updateFeatures)) {
        let { buildDotNetGraphic } = await import('./graphic');
        dotNetSubtypeSublayerApplyEditsEdits.updateFeatures = jsObject.updateFeatures.map(i => buildDotNetGraphic(i, layerId, viewId));
    }
    
    if (hasValue(jsObject.deleteAttachments)) {
        dotNetSubtypeSublayerApplyEditsEdits.deleteAttachments = jsObject.deleteAttachments;
    }
    

    return dotNetSubtypeSublayerApplyEditsEdits;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMPictureMarkerAnchorPoint } from './cIMPictureMarkerAnchorPoint';

export async function buildJsCIMPictureMarkerAnchorPointGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMPictureMarkerAnchorPoint: any = {};

    if (hasValue(dotNetObject.x)) {
        jsCIMPictureMarkerAnchorPoint.x = dotNetObject.x;
    }
    if (hasValue(dotNetObject.y)) {
        jsCIMPictureMarkerAnchorPoint.y = dotNetObject.y;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMPictureMarkerAnchorPoint);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMPictureMarkerAnchorPoint;
    
    let { buildDotNetCIMPictureMarkerAnchorPoint } = await import('./cIMPictureMarkerAnchorPoint');
    let dnInstantiatedObject = await buildDotNetCIMPictureMarkerAnchorPoint(jsCIMPictureMarkerAnchorPoint);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMPictureMarkerAnchorPoint detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMPictureMarkerAnchorPoint', e);
    }
    
    return jsCIMPictureMarkerAnchorPoint;
}


export async function buildDotNetCIMPictureMarkerAnchorPointGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMPictureMarkerAnchorPoint: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.x)) {
        dotNetCIMPictureMarkerAnchorPoint.x = jsObject.x;
    }
    if (hasValue(jsObject.y)) {
        dotNetCIMPictureMarkerAnchorPoint.y = jsObject.y;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMPictureMarkerAnchorPoint.id = geoBlazorId;
    }

    return dotNetCIMPictureMarkerAnchorPoint;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import SearchLayerField from '@arcgis/core/webdoc/applicationProperties/SearchLayerField';
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetSearchLayerField } from './searchLayerField';

export async function buildJsSearchLayerFieldGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};

    if (hasValue(dotNetObject.exactMatch)) {
        properties.exactMatch = dotNetObject.exactMatch;
    }
    if (hasValue(dotNetObject.name)) {
        properties.name = dotNetObject.name;
    }
    let jsSearchLayerField = new SearchLayerField(properties);
    
    let jsObjectRef = DotNet.createJSObjectReference(jsSearchLayerField);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsSearchLayerField;
    
    let { buildDotNetSearchLayerField } = await import('./searchLayerField');
    let dnInstantiatedObject = await buildDotNetSearchLayerField(jsSearchLayerField, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type SearchLayerField detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for SearchLayerField', e);
    }
    
    return jsSearchLayerField;
}


export async function buildDotNetSearchLayerFieldGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsSearchLayerField } = await import('./searchLayerField');
        jsComponentRef = await buildJsSearchLayerField(jsObject, layerId, viewId);
    }
    
    let dotNetSearchLayerField: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.exactMatch)) {
        dotNetSearchLayerField.exactMatch = jsObject.exactMatch;
    }
    if (hasValue(jsObject.name)) {
        dotNetSearchLayerField.name = jsObject.name;
    }
    if (hasValue(jsObject.type)) {
        dotNetSearchLayerField.type = jsObject.type;
    }


    if (hasValue(geoBlazorId)) {
        dotNetSearchLayerField.id = geoBlazorId;
    }

    return dotNetSearchLayerField;
}


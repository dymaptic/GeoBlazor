// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetDirectionsViewModelLastRoute } from './directionsViewModelLastRoute';
import DirectionsViewModelLastRoute = __esri.DirectionsViewModelLastRoute;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsDirectionsViewModelLastRouteGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsDirectionsViewModelLastRoute: any = {}
    if (hasValue(dotNetObject.directionLines)) {
        let { buildJsDirectionLine } = await import('./directionLine');
        jsDirectionsViewModelLastRoute.directionLines = await Promise.all(dotNetObject.directionLines.map(async i => await buildJsDirectionLine(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.directionPoints)) {
        let { buildJsDirectionPoint } = await import('./directionPoint');
        jsDirectionsViewModelLastRoute.directionPoints = await Promise.all(dotNetObject.directionPoints.map(async i => await buildJsDirectionPoint(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.pointBarriers)) {
        let { buildJsPointBarrier } = await import('./pointBarrier');
        jsDirectionsViewModelLastRoute.pointBarriers = await Promise.all(dotNetObject.pointBarriers.map(async i => await buildJsPointBarrier(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.polygonBarriers)) {
        let { buildJsPolygonBarrier } = await import('./polygonBarrier');
        jsDirectionsViewModelLastRoute.polygonBarriers = await Promise.all(dotNetObject.polygonBarriers.map(async i => await buildJsPolygonBarrier(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.polylineBarriers)) {
        let { buildJsPolylineBarrier } = await import('./polylineBarrier');
        jsDirectionsViewModelLastRoute.polylineBarriers = await Promise.all(dotNetObject.polylineBarriers.map(async i => await buildJsPolylineBarrier(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.routeInfo)) {
        let { buildJsRouteInfo } = await import('./routeInfo');
        jsDirectionsViewModelLastRoute.routeInfo = await buildJsRouteInfo(dotNetObject.routeInfo, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.stops)) {
        let { buildJsStop } = await import('./stop');
        jsDirectionsViewModelLastRoute.stops = await Promise.all(dotNetObject.stops.map(async i => await buildJsStop(i, layerId, viewId))) as any;
    }

    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsDirectionsViewModelLastRoute);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsDirectionsViewModelLastRoute;
    
    let dnInstantiatedObject = await buildDotNetDirectionsViewModelLastRoute(jsDirectionsViewModelLastRoute);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for DirectionsViewModelLastRoute', e);
    }
    
    return jsDirectionsViewModelLastRoute;
}

export async function buildDotNetDirectionsViewModelLastRouteGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetDirectionsViewModelLastRoute: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.directionLines)) {
            let { buildDotNetDirectionLine } = await import('./directionLine');
            dotNetDirectionsViewModelLastRoute.directionLines = await Promise.all(jsObject.directionLines.map(async i => await buildDotNetDirectionLine(i)));
        }
        if (hasValue(jsObject.directionPoints)) {
            let { buildDotNetDirectionPoint } = await import('./directionPoint');
            dotNetDirectionsViewModelLastRoute.directionPoints = await Promise.all(jsObject.directionPoints.map(async i => await buildDotNetDirectionPoint(i)));
        }
        if (hasValue(jsObject.pointBarriers)) {
            let { buildDotNetPointBarrier } = await import('./pointBarrier');
            dotNetDirectionsViewModelLastRoute.pointBarriers = await Promise.all(jsObject.pointBarriers.map(async i => await buildDotNetPointBarrier(i)));
        }
        if (hasValue(jsObject.polygonBarriers)) {
            let { buildDotNetPolygonBarrier } = await import('./polygonBarrier');
            dotNetDirectionsViewModelLastRoute.polygonBarriers = await Promise.all(jsObject.polygonBarriers.map(async i => await buildDotNetPolygonBarrier(i)));
        }
        if (hasValue(jsObject.polylineBarriers)) {
            let { buildDotNetPolylineBarrier } = await import('./polylineBarrier');
            dotNetDirectionsViewModelLastRoute.polylineBarriers = await Promise.all(jsObject.polylineBarriers.map(async i => await buildDotNetPolylineBarrier(i)));
        }
        if (hasValue(jsObject.routeInfo)) {
            let { buildDotNetRouteInfo } = await import('./routeInfo');
            dotNetDirectionsViewModelLastRoute.routeInfo = await buildDotNetRouteInfo(jsObject.routeInfo);
        }
        if (hasValue(jsObject.stops)) {
            let { buildDotNetStop } = await import('./stop');
            dotNetDirectionsViewModelLastRoute.stops = await Promise.all(jsObject.stops.map(async i => await buildDotNetStop(i)));
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetDirectionsViewModelLastRoute.id = k;
                break;
            }
        }
    }

    return dotNetDirectionsViewModelLastRoute;
}


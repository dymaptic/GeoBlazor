// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import AreaMeasurement2D from '@arcgis/core/widgets/AreaMeasurement2D';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences, buildJsStreamReference, generateSerializableJson } from './geoBlazorCore';
import BaseComponent from "./baseComponent";

export default class AreaMeasurement2DWidgetGenerated extends BaseComponent {
    public widget: AreaMeasurement2D;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(widget: AreaMeasurement2D) {
        super(widget);
        this.widget = widget;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.snappingOptions)) {
            let { buildJsSnappingOptions } = await import('./snappingOptions');
            this.widget.snappingOptions = await buildJsSnappingOptions(dotNetObject.snappingOptions, this.layerId, this.viewId) as any;
        }

        if (hasValue(dotNetObject.destroyed)) {
            this.widget.destroyed = dotNetObject.destroyed;
        }
        if (hasValue(dotNetObject.icon)) {
            this.widget.icon = dotNetObject.icon;
        }
        if (hasValue(dotNetObject.label)) {
            this.widget.label = dotNetObject.label;
        }
        if (hasValue(dotNetObject.unit)) {
            this.widget.unit = dotNetObject.unit;
        }
        if (hasValue(dotNetObject.unitOptions) && dotNetObject.unitOptions.length > 0) {
            this.widget.unitOptions = dotNetObject.unitOptions;
        }
        if (hasValue(dotNetObject.visible)) {
            this.widget.visible = dotNetObject.visible;
        }
        if (hasValue(dotNetObject.widgetId)) {
            this.widget.id = dotNetObject.widgetId;
        }
    }
    
    // region methods
    async classes(): Promise<any> {
        return this.widget.classes();
    }

    async isFulfilled(): Promise<any> {
        return this.widget.isFulfilled();
    }

    async isRejected(): Promise<any> {
        return this.widget.isRejected();
    }

    async isResolved(): Promise<any> {
        return this.widget.isResolved();
    }

    async postInitialize(): Promise<void> {
        this.widget.postInitialize();
    }

    async render(): Promise<any> {
        return this.widget.render();
    }

    async renderNow(): Promise<void> {
        this.widget.renderNow();
    }

    async scheduleRender(): Promise<void> {
        this.widget.scheduleRender();
    }

    async when(onFulfilled: any,
        onRejected: any): Promise<any> {
        return await this.widget.when(onFulfilled,
            onRejected);
    }

    // region properties
    
    getIcon(): any {
        if (!hasValue(this.widget.icon)) {
            return null;
        }
        
        return generateSerializableJson(this.widget.icon);
    }
    
    setIcon(value: any): void {
        this.widget.icon = JSON.parse(value);
    }
    
    getLabel(): any {
        if (!hasValue(this.widget.label)) {
            return null;
        }
        
        return generateSerializableJson(this.widget.label);
    }
    
    setLabel(value: any): void {
        this.widget.label = JSON.parse(value);
    }
    
    async getSnappingOptions(): Promise<any> {
        if (!hasValue(this.widget.snappingOptions)) {
            return null;
        }
        
        let { buildDotNetSnappingOptions } = await import('./snappingOptions');
        return await buildDotNetSnappingOptions(this.widget.snappingOptions, this.viewId);
    }
    
    async setSnappingOptions(value: any): Promise<void> {
        let { buildJsSnappingOptions } = await import('./snappingOptions');
        this.widget.snappingOptions = await  buildJsSnappingOptions(value, this.layerId, this.viewId);
    }
    
    async getViewModel(): Promise<any> {
        if (!hasValue(this.widget.viewModel)) {
            return null;
        }
        
        let { buildDotNetAreaMeasurement2DViewModel } = await import('./areaMeasurement2DViewModel');
        return await buildDotNetAreaMeasurement2DViewModel(this.widget.viewModel, this.viewId);
    }
    
    async setViewModel(value: any): Promise<void> {
        let { buildJsAreaMeasurement2DViewModel } = await import('./areaMeasurement2DViewModel');
        this.widget.viewModel = await  buildJsAreaMeasurement2DViewModel(value, this.layerId, this.viewId);
    }
    
    getWidgetId(): any {
        if (!hasValue(this.widget.id)) {
            return null;
        }
        
        return generateSerializableJson(this.widget.id);
    }
    
    setWidgetId(value: any): void {
        this.widget.id = JSON.parse(value);
    }
    
}


export async function buildJsAreaMeasurement2DWidgetGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(viewId)) {
        properties.view = arcGisObjectRefs[viewId!];
    }
    if (hasValue(dotNetObject.snappingOptions)) {
        let { buildJsSnappingOptions } = await import('./snappingOptions');
        properties.snappingOptions = await buildJsSnappingOptions(dotNetObject.snappingOptions, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.viewModel)) {
        let { buildJsAreaMeasurement2DViewModel } = await import('./areaMeasurement2DViewModel');
        properties.viewModel = await buildJsAreaMeasurement2DViewModel(dotNetObject.viewModel, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.destroyed)) {
        properties.destroyed = dotNetObject.destroyed;
    }
    if (hasValue(dotNetObject.icon)) {
        properties.icon = dotNetObject.icon;
    }
    if (hasValue(dotNetObject.label)) {
        properties.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.unit)) {
        properties.unit = dotNetObject.unit;
    }
    if (hasValue(dotNetObject.unitOptions) && dotNetObject.unitOptions.length > 0) {
        properties.unitOptions = dotNetObject.unitOptions;
    }
    if (hasValue(dotNetObject.visible)) {
        properties.visible = dotNetObject.visible;
    }
    if (hasValue(dotNetObject.widgetId)) {
        properties.id = dotNetObject.widgetId;
    }
    let jsAreaMeasurement2D = new AreaMeasurement2D(properties);

    let { default: AreaMeasurement2DWidgetWrapper } = await import('./areaMeasurement2DWidget');

    let areaMeasurement2DWidgetWrapper = new AreaMeasurement2DWidgetWrapper(jsAreaMeasurement2D);
    areaMeasurement2DWidgetWrapper.geoBlazorId = dotNetObject.id;
    areaMeasurement2DWidgetWrapper.viewId = viewId;
    areaMeasurement2DWidgetWrapper.layerId = layerId;
    
    jsObjectRefs[dotNetObject.id] = areaMeasurement2DWidgetWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsAreaMeasurement2D;
    
    // serialize data and send back to .NET to populate properties
    // we call requestAnimationFrame to pull this out of the synchronous render flow
    requestAnimationFrame(async () => {
        try {
            let jsObjectRef = DotNet.createJSObjectReference(areaMeasurement2DWidgetWrapper);
            let { buildDotNetAreaMeasurement2DWidget } = await import('./areaMeasurement2DWidget');
            let dnInstantiatedObject = await buildDotNetAreaMeasurement2DWidget(jsAreaMeasurement2D, viewId);

            let dnStream = buildJsStreamReference(dnInstantiatedObject);
            await dotNetObject.dotNetComponentReference?.invokeMethodAsync('OnJsComponentCreated', 
                jsObjectRef, dnStream);
        } catch (e) {
            console.error('Error invoking OnJsComponentCreated for AreaMeasurement2DWidget', e);
        }
    });
    
    return jsAreaMeasurement2D;
}


export async function buildDotNetAreaMeasurement2DWidgetGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetAreaMeasurement2DWidget: any = {};
    
    if (hasValue(jsObject.snappingOptions)) {
        let { buildDotNetSnappingOptions } = await import('./snappingOptions');
        dotNetAreaMeasurement2DWidget.snappingOptions = await buildDotNetSnappingOptions(jsObject.snappingOptions, viewId);
    }
    
    if (hasValue(jsObject.viewModel)) {
        let { buildDotNetAreaMeasurement2DViewModel } = await import('./areaMeasurement2DViewModel');
        dotNetAreaMeasurement2DWidget.viewModel = await buildDotNetAreaMeasurement2DViewModel(jsObject.viewModel, viewId);
    }
    
    if (hasValue(jsObject.destroyed)) {
        dotNetAreaMeasurement2DWidget.destroyed = jsObject.destroyed;
    }
    
    if (hasValue(jsObject.icon)) {
        dotNetAreaMeasurement2DWidget.icon = jsObject.icon;
    }
    
    if (hasValue(jsObject.label)) {
        dotNetAreaMeasurement2DWidget.label = jsObject.label;
    }
    
    if (hasValue(jsObject.type)) {
        dotNetAreaMeasurement2DWidget.type = removeCircularReferences(jsObject.type);
    }
    
    if (hasValue(jsObject.unit)) {
        dotNetAreaMeasurement2DWidget.unit = removeCircularReferences(jsObject.unit);
    }
    
    if (hasValue(jsObject.unitOptions)) {
        dotNetAreaMeasurement2DWidget.unitOptions = removeCircularReferences(jsObject.unitOptions);
    }
    
    if (hasValue(jsObject.visible)) {
        dotNetAreaMeasurement2DWidget.visible = jsObject.visible;
    }
    
    if (hasValue(jsObject.id)) {
        dotNetAreaMeasurement2DWidget.widgetId = jsObject.id;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetAreaMeasurement2DWidget.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetAreaMeasurement2DWidget.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for AreaMeasurement2DWidget', e);
            }
        }
    }
    if (hasValue(dotNetAreaMeasurement2DWidget.id)) {
        jsObjectRefs[dotNetAreaMeasurement2DWidget.id] ??= jsObject;
        arcGisObjectRefs[dotNetAreaMeasurement2DWidget.id] ??= jsObject;
    }

    return dotNetAreaMeasurement2DWidget;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetContinuousRendererResultColor } from './continuousRendererResultColor';
import ContinuousRendererResultColor = __esri.ContinuousRendererResultColor;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsContinuousRendererResultColorGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsContinuousRendererResultColor: any = {}
    if (hasValue(dotNetObject.visualVariable)) {
        let { buildJsColorVariable } = await import('./colorVariable');
        jsContinuousRendererResultColor.visualVariable = await buildJsColorVariable(dotNetObject.visualVariable, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.colorScheme)) {
        jsContinuousRendererResultColor.colorScheme = dotNetObject.colorScheme;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsContinuousRendererResultColor);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsContinuousRendererResultColor;
    
    let dnInstantiatedObject = await buildDotNetContinuousRendererResultColor(jsContinuousRendererResultColor);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ContinuousRendererResultColor', e);
    }
    
    return jsContinuousRendererResultColor;
}

export async function buildDotNetContinuousRendererResultColorGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetContinuousRendererResultColor: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.visualVariable)) {
            let { buildDotNetColorVariable } = await import('./colorVariable');
            dotNetContinuousRendererResultColor.visualVariable = await buildDotNetColorVariable(jsObject.visualVariable);
        }
        if (hasValue(jsObject.colorScheme)) {
            dotNetContinuousRendererResultColor.colorScheme = jsObject.colorScheme;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetContinuousRendererResultColor.id = k;
                break;
            }
        }
    }

    return dotNetContinuousRendererResultColor;
}


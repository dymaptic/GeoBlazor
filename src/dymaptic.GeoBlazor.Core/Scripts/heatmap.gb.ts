// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import heatmap = __esri.heatmap;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class HeatmapGenerated implements IPropertyWrapper {
    public component: heatmap;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: heatmap) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    async createRenderer(parameters: any): Promise<any> {
        let { buildJsHeatmapCreateRendererParams } = await import('./heatmapCreateRendererParams');
        let jsparameters = await buildJsHeatmapCreateRendererParams(parameters, this.layerId, this.viewId) as any;
        return await this.component.createRenderer(jsparameters);
    }

    async updateRenderer(parameters: any): Promise<any> {
        let { buildJsHeatmapUpdateRendererParams } = await import('./heatmapUpdateRendererParams');
        let jsparameters = await buildJsHeatmapUpdateRendererParams(parameters, this.layerId, this.viewId) as any;
        return this.component.updateRenderer(jsparameters);
    }

    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}


export async function buildJsHeatmapGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsheatmap: any = {}


    let { default: HeatmapWrapper } = await import('./heatmap');
    let heatmapWrapper = new HeatmapWrapper(jsheatmap);
    heatmapWrapper.geoBlazorId = dotNetObject.id;
    heatmapWrapper.viewId = viewId;
    heatmapWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(heatmapWrapper);
    jsObjectRefs[dotNetObject.id] = heatmapWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsheatmap;
    let { buildDotNetHeatmap } = await import('./heatmap');
    let dnInstantiatedObject = await buildDotNetHeatmap(jsheatmap);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Heatmap', e);
    }
    
    return jsheatmap;
}

export async function buildDotNetHeatmapGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetHeatmap: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetHeatmap.id = k;
                break;
            }
        }
    }

    return dotNetHeatmap;
}


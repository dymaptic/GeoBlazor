// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetIntegratedMesh3DTilesLayerElevationInfo } from './integratedMesh3DTilesLayerElevationInfo';

export async function buildJsIntegratedMesh3DTilesLayerElevationInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsIntegratedMesh3DTilesLayerElevationInfo: any = {};

    if (hasValue(dotNetObject.mode)) {
        jsIntegratedMesh3DTilesLayerElevationInfo.mode = dotNetObject.mode;
    }
    if (hasValue(dotNetObject.offset)) {
        jsIntegratedMesh3DTilesLayerElevationInfo.offset = dotNetObject.offset;
    }
    if (hasValue(dotNetObject.unit)) {
        jsIntegratedMesh3DTilesLayerElevationInfo.unit = dotNetObject.unit;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsIntegratedMesh3DTilesLayerElevationInfo);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsIntegratedMesh3DTilesLayerElevationInfo;
    
    let { buildDotNetIntegratedMesh3DTilesLayerElevationInfo } = await import('./integratedMesh3DTilesLayerElevationInfo');
    let dnInstantiatedObject = await buildDotNetIntegratedMesh3DTilesLayerElevationInfo(jsIntegratedMesh3DTilesLayerElevationInfo, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type IntegratedMesh3DTilesLayerElevationInfo detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for IntegratedMesh3DTilesLayerElevationInfo', e);
    }
    
    return jsIntegratedMesh3DTilesLayerElevationInfo;
}


export async function buildDotNetIntegratedMesh3DTilesLayerElevationInfoGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsIntegratedMesh3DTilesLayerElevationInfo } = await import('./integratedMesh3DTilesLayerElevationInfo');
        jsComponentRef = await buildJsIntegratedMesh3DTilesLayerElevationInfo(jsObject, layerId, viewId);
    }
    
    let dotNetIntegratedMesh3DTilesLayerElevationInfo: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.mode)) {
        dotNetIntegratedMesh3DTilesLayerElevationInfo.mode = jsObject.mode;
    }
    if (hasValue(jsObject.offset)) {
        dotNetIntegratedMesh3DTilesLayerElevationInfo.offset = jsObject.offset;
    }
    if (hasValue(jsObject.unit)) {
        dotNetIntegratedMesh3DTilesLayerElevationInfo.unit = jsObject.unit;
    }


    if (hasValue(geoBlazorId)) {
        dotNetIntegratedMesh3DTilesLayerElevationInfo.id = geoBlazorId;
    }

    return dotNetIntegratedMesh3DTilesLayerElevationInfo;
}


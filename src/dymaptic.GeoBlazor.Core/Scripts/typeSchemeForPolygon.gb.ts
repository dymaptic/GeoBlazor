// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetTypeSchemeForPolygon } from './typeSchemeForPolygon';

export async function buildJsTypeSchemeForPolygonGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsTypeSchemeForPolygon: any = {}
    if (hasValue(dotNetObject.colors)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsTypeSchemeForPolygon.colors = dotNetObject.colors.map(i => buildJsMapColor(i)) as any;
    }
    if (hasValue(dotNetObject.noDataColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsTypeSchemeForPolygon.noDataColor = buildJsMapColor(dotNetObject.noDataColor) as any;
    }
    if (hasValue(dotNetObject.outline)) {
        let { buildJsTypeSchemeForPolygonOutline } = await import('./typeSchemeForPolygonOutline');
        jsTypeSchemeForPolygon.outline = await buildJsTypeSchemeForPolygonOutline(dotNetObject.outline, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.name)) {
        jsTypeSchemeForPolygon.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.opacity)) {
        jsTypeSchemeForPolygon.opacity = dotNetObject.opacity;
    }
    if (hasValue(dotNetObject.tags)) {
        jsTypeSchemeForPolygon.tags = dotNetObject.tags;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsTypeSchemeForPolygon);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsTypeSchemeForPolygon;
    
    let dnInstantiatedObject = await buildDotNetTypeSchemeForPolygon(jsTypeSchemeForPolygon);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for TypeSchemeForPolygon', e);
    }
    
    return jsTypeSchemeForPolygon;
}

export async function buildDotNetTypeSchemeForPolygonGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTypeSchemeForPolygon: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.colors)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetTypeSchemeForPolygon.colors = jsObject.colors.map(i => buildDotNetMapColor(i));
        }
        if (hasValue(jsObject.noDataColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetTypeSchemeForPolygon.noDataColor = buildDotNetMapColor(jsObject.noDataColor);
        }
        if (hasValue(jsObject.outline)) {
            let { buildDotNetTypeSchemeForPolygonOutline } = await import('./typeSchemeForPolygonOutline');
            dotNetTypeSchemeForPolygon.outline = await buildDotNetTypeSchemeForPolygonOutline(jsObject.outline);
        }
    if (hasValue(jsObject.name)) {
        dotNetTypeSchemeForPolygon.name = jsObject.name;
    }
    if (hasValue(jsObject.opacity)) {
        dotNetTypeSchemeForPolygon.opacity = jsObject.opacity;
    }
    if (hasValue(jsObject.tags)) {
        dotNetTypeSchemeForPolygon.tags = jsObject.tags;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetTypeSchemeForPolygon.id = k;
                break;
            }
        }
    }

    return dotNetTypeSchemeForPolygon;
}


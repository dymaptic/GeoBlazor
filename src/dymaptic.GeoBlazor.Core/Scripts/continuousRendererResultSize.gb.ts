// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file

import ContinuousRendererResultSize = __esri.ContinuousRendererResultSize;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class ContinuousRendererResultSizeGenerated implements IPropertyWrapper {
    public component: ContinuousRendererResultSize;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: ContinuousRendererResultSize) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    // region properties
    
    async getVisualVariables(): Promise<any> {
        if (!hasValue(this.component.visualVariables)) {
            return null;
        }
        
        let { buildDotNetSizeVariable } = await import('./sizeVariable');
        return await Promise.all(this.component.visualVariables.map(async i => await buildDotNetSizeVariable(i)));
    }
    
    async setVisualVariables(value: any): Promise<void> {
        let { buildJsSizeVariable } = await import('./sizeVariable');
        this.component.visualVariables = await Promise.all(value.map(async i => await buildJsSizeVariable(i, this.layerId, this.viewId))) as any;
    }
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsContinuousRendererResultSizeGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsContinuousRendererResultSize: any = {}
    if (hasValue(dotNetObject.sizeScheme)) {
        jsContinuousRendererResultSize.sizeScheme = dotNetObject.iSizeScheme;
    }
    if (hasValue(dotNetObject.visualVariables)) {
        let { buildJsSizeVariable } = await import('./sizeVariable');
        jsContinuousRendererResultSize.visualVariables = await Promise.all(dotNetObject.visualVariables.map(async i => await buildJsSizeVariable(i, layerId, viewId))) as any;
    }


    let { default: ContinuousRendererResultSizeWrapper } = await import('./continuousRendererResultSize');
    let continuousRendererResultSizeWrapper = new ContinuousRendererResultSizeWrapper(jsContinuousRendererResultSize);
    continuousRendererResultSizeWrapper.geoBlazorId = dotNetObject.id;
    continuousRendererResultSizeWrapper.viewId = viewId;
    continuousRendererResultSizeWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(continuousRendererResultSizeWrapper);
    jsObjectRefs[dotNetObject.id] = continuousRendererResultSizeWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsContinuousRendererResultSize;
    let { buildDotNetContinuousRendererResultSize } = await import('./continuousRendererResultSize');
    let dnInstantiatedObject = await buildDotNetContinuousRendererResultSize(jsContinuousRendererResultSize);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ContinuousRendererResultSize', e);
    }
    
    return jsContinuousRendererResultSize;
}
export async function buildDotNetContinuousRendererResultSizeGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetContinuousRendererResultSize: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        dotNetContinuousRendererResultSize.sizeScheme = jsObject.sizeScheme;
        if (hasValue(jsObject.visualVariables)) {
            let { buildDotNetSizeVariable } = await import('./sizeVariable');
            dotNetContinuousRendererResultSize.visualVariables = await Promise.all(jsObject.visualVariables.map(async i => await buildDotNetSizeVariable(i)));
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetContinuousRendererResultSize.id = k;
                break;
            }
        }
    }

    return dotNetContinuousRendererResultSize;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import FeatureReference from '@arcgis/core/webscene/support/FeatureReference';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';

export async function buildJsFeatureReferenceGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.featureReferenceId)) {
        let { buildJsIFeatureReferenceFeatureReferenceId } = await import('./iFeatureReferenceFeatureReferenceId');
        properties.id = buildJsIFeatureReferenceFeatureReferenceId(dotNetObject.featureReferenceId) as any;
    }
    if (hasValue(dotNetObject.layerReference)) {
        let { buildJsLayerReference } = await import('./layerReference');
        properties.layerReference = await buildJsLayerReference(dotNetObject.layerReference, layerId, viewId) as any;
    }

    let jsFeatureReference = new FeatureReference(properties);
    
    jsObjectRefs[dotNetObject.id] = jsFeatureReference;
    arcGisObjectRefs[dotNetObject.id] = jsFeatureReference;
    
    return jsFeatureReference;
}


export async function buildDotNetFeatureReferenceGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFeatureReference: any = {};
    
    if (hasValue(jsObject.id)) {
        let { buildDotNetIFeatureReferenceFeatureReferenceId } = await import('./iFeatureReferenceFeatureReferenceId');
        dotNetFeatureReference.featureReferenceId = buildDotNetIFeatureReferenceFeatureReferenceId(jsObject.id);
    }
    
    if (hasValue(jsObject.layerReference)) {
        let { buildDotNetLayerReference } = await import('./layerReference');
        dotNetFeatureReference.layerReference = await buildDotNetLayerReference(jsObject.layerReference, viewId);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetFeatureReference.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetFeatureReference.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for FeatureReference', e);
            }
        }
    }
    if (hasValue(dotNetFeatureReference.id)) {
        jsObjectRefs[dotNetFeatureReference.id] ??= jsObject;
        arcGisObjectRefs[dotNetFeatureReference.id] ??= jsObject;
    }

    return dotNetFeatureReference;
}


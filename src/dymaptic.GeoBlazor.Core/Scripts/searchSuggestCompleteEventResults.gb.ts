// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './geoBlazorCore';

export async function buildJsSearchSuggestCompleteEventResultsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsSearchSuggestCompleteEventResults: any = {};
    if (hasValue(dotNetObject.results) && dotNetObject.results.length > 0) {
        let { buildJsSuggestResult } = await import('./suggestResult');
        jsSearchSuggestCompleteEventResults.results = dotNetObject.results.map(i => buildJsSuggestResult(i)) as any;
    }
    if (hasValue(dotNetObject.source)) {
        let { buildJsSearchSource } = await import('./searchSource');
        jsSearchSuggestCompleteEventResults.source = await buildJsSearchSource(dotNetObject.source, viewId) as any;
    }

    if (hasValue(dotNetObject.sourceIndex)) {
        jsSearchSuggestCompleteEventResults.sourceIndex = dotNetObject.sourceIndex;
    }
    
    jsObjectRefs[dotNetObject.id] = jsSearchSuggestCompleteEventResults;
    arcGisObjectRefs[dotNetObject.id] = jsSearchSuggestCompleteEventResults;
    
    return jsSearchSuggestCompleteEventResults;
}


export async function buildDotNetSearchSuggestCompleteEventResultsGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSearchSuggestCompleteEventResults: any = {};
    
    if (hasValue(jsObject.results)) {
        let { buildDotNetSuggestResult } = await import('./suggestResult');
        dotNetSearchSuggestCompleteEventResults.results = jsObject.results.map(i => buildDotNetSuggestResult(i));
    }
    
    if (hasValue(jsObject.source)) {
        let { buildDotNetSearchSource } = await import('./searchSource');
        dotNetSearchSuggestCompleteEventResults.source = await buildDotNetSearchSource(jsObject.source, viewId);
    }
    
    if (hasValue(jsObject.sourceIndex)) {
        dotNetSearchSuggestCompleteEventResults.sourceIndex = jsObject.sourceIndex;
    }
    

    return dotNetSearchSuggestCompleteEventResults;
}


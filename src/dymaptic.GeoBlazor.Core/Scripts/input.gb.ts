// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import Input from '@arcgis/core/views/input/Input';
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetInput } from './input';

export async function buildJsInputGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let properties: any = {};

    let jsInput = new Input(properties);
    
    let jsObjectRef = DotNet.createJSObjectReference(jsInput);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsInput;
    
    let { buildDotNetInput } = await import('./input');
    let dnInstantiatedObject = await buildDotNetInput(jsInput, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type Input detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Input', e);
    }
    
    return jsInput;
}


export async function buildDotNetInputGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsInput } = await import('./input');
        jsComponentRef = await buildJsInput(jsObject, layerId, viewId);
    }
    
    let dotNetInput: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.gamepad)) {
        let { buildDotNetGamepadSettings } = await import('./gamepadSettings');
        dotNetInput.gamepad = await buildDotNetGamepadSettings(jsObject.gamepad, layerId, viewId);
    }


    if (hasValue(geoBlazorId)) {
        dotNetInput.id = geoBlazorId;
    }

    return dotNetInput;
}


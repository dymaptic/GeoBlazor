// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetRasterStretchColorrampResult } from './rasterStretchColorrampResult';
import RasterStretchColorrampResult = __esri.RasterStretchColorrampResult;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsRasterStretchColorrampResultGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsRasterStretchColorrampResult: any = {}
    if (hasValue(dotNetObject.renderer)) {
        let { buildJsRasterStretchRenderer } = await import('./rasterStretchRenderer');
        jsRasterStretchColorrampResult.renderer = await buildJsRasterStretchRenderer(dotNetObject.renderer, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.bandId)) {
        jsRasterStretchColorrampResult.bandId = dotNetObject.bandId;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsRasterStretchColorrampResult);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsRasterStretchColorrampResult;
    
    let dnInstantiatedObject = await buildDotNetRasterStretchColorrampResult(jsRasterStretchColorrampResult);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for RasterStretchColorrampResult', e);
    }
    
    return jsRasterStretchColorrampResult;
}

export async function buildDotNetRasterStretchColorrampResultGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetRasterStretchColorrampResult: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.renderer)) {
            let { buildDotNetRasterStretchRenderer } = await import('./rasterStretchRenderer');
            dotNetRasterStretchColorrampResult.renderer = await buildDotNetRasterStretchRenderer(jsObject.renderer);
        }
        if (hasValue(jsObject.bandId)) {
            dotNetRasterStretchColorrampResult.bandId = jsObject.bandId;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetRasterStretchColorrampResult.id = k;
                break;
            }
        }
    }

    return dotNetRasterStretchColorrampResult;
}


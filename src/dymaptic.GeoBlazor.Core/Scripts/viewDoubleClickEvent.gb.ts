// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetViewDoubleClickEvent } from './viewDoubleClickEvent';

export async function buildJsViewDoubleClickEventGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsViewDoubleClickEvent: any = {}
    if (hasValue(dotNetObject.mapPoint)) {
        let { buildJsPoint } = await import('./point');
        jsViewDoubleClickEvent.mapPoint = buildJsPoint(dotNetObject.mapPoint) as any;
    }

    if (hasValue(dotNetObject.button)) {
        jsViewDoubleClickEvent.button = dotNetObject.button;
    }
    if (hasValue(dotNetObject.buttons)) {
        jsViewDoubleClickEvent.buttons = dotNetObject.buttons;
    }
    if (hasValue(dotNetObject.native)) {
        jsViewDoubleClickEvent.native = dotNetObject.native;
    }
    if (hasValue(dotNetObject.stopPropagation)) {
        jsViewDoubleClickEvent.stopPropagation = dotNetObject.stopPropagation;
    }
    if (hasValue(dotNetObject.timestamp)) {
        jsViewDoubleClickEvent.timestamp = dotNetObject.timestamp;
    }
    if (hasValue(dotNetObject.x)) {
        jsViewDoubleClickEvent.x = dotNetObject.x;
    }
    if (hasValue(dotNetObject.y)) {
        jsViewDoubleClickEvent.y = dotNetObject.y;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsViewDoubleClickEvent);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsViewDoubleClickEvent;
    
    let dnInstantiatedObject = await buildDotNetViewDoubleClickEvent(jsViewDoubleClickEvent);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ViewDoubleClickEvent', e);
    }
    
    return jsViewDoubleClickEvent;
}

export async function buildDotNetViewDoubleClickEventGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetViewDoubleClickEvent: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.mapPoint)) {
            let { buildDotNetPoint } = await import('./point');
            dotNetViewDoubleClickEvent.mapPoint = buildDotNetPoint(jsObject.mapPoint);
        }
        if (hasValue(jsObject.button)) {
            dotNetViewDoubleClickEvent.button = jsObject.button;
        }
        if (hasValue(jsObject.buttons)) {
            dotNetViewDoubleClickEvent.buttons = jsObject.buttons;
        }
        if (hasValue(jsObject.native)) {
            dotNetViewDoubleClickEvent.native = jsObject.native;
        }
        if (hasValue(jsObject.stopPropagation)) {
            dotNetViewDoubleClickEvent.stopPropagation = jsObject.stopPropagation;
        }
        if (hasValue(jsObject.timestamp)) {
            dotNetViewDoubleClickEvent.timestamp = jsObject.timestamp;
        }
        if (hasValue(jsObject.type)) {
            dotNetViewDoubleClickEvent.type = jsObject.type;
        }
        if (hasValue(jsObject.x)) {
            dotNetViewDoubleClickEvent.x = jsObject.x;
        }
        if (hasValue(jsObject.y)) {
            dotNetViewDoubleClickEvent.y = jsObject.y;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetViewDoubleClickEvent.id = k;
                break;
            }
        }
    }

    return dotNetViewDoubleClickEvent;
}


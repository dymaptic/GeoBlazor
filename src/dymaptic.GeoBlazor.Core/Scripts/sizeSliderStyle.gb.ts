// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetSizeSliderStyle } from './sizeSliderStyle';

export async function buildJsSizeSliderStyleGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsSizeSliderStyle: any = {}
    if (hasValue(dotNetObject.trackBackgroundColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsSizeSliderStyle.trackBackgroundColor = buildJsMapColor(dotNetObject.trackBackgroundColor) as any;
    }
    if (hasValue(dotNetObject.trackFillColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsSizeSliderStyle.trackFillColor = buildJsMapColor(dotNetObject.trackFillColor) as any;
    }

    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsSizeSliderStyle);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsSizeSliderStyle;
    
    let dnInstantiatedObject = await buildDotNetSizeSliderStyle(jsSizeSliderStyle);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for SizeSliderStyle', e);
    }
    
    return jsSizeSliderStyle;
}

export async function buildDotNetSizeSliderStyleGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSizeSliderStyle: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.trackBackgroundColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetSizeSliderStyle.trackBackgroundColor = buildDotNetMapColor(jsObject.trackBackgroundColor);
        }
        if (hasValue(jsObject.trackFillColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetSizeSliderStyle.trackFillColor = buildDotNetMapColor(jsObject.trackFillColor);
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetSizeSliderStyle.id = k;
                break;
            }
        }
    }

    return dotNetSizeSliderStyle;
}


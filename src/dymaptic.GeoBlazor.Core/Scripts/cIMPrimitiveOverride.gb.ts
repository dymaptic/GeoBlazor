// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMPrimitiveOverride } from './cIMPrimitiveOverride';

export async function buildJsCIMPrimitiveOverrideGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMPrimitiveOverride: any = {};
    if (hasValue(dotNetObject.valueExpressionInfo)) {
        let { buildJsCIMExpressionInfo } = await import('./cIMExpressionInfo');
        jsCIMPrimitiveOverride.valueExpressionInfo = await buildJsCIMExpressionInfo(dotNetObject.valueExpressionInfo, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.expression)) {
        jsCIMPrimitiveOverride.expression = dotNetObject.expression;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMPrimitiveOverride.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.propertyName)) {
        jsCIMPrimitiveOverride.propertyName = dotNetObject.propertyName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMPrimitiveOverride);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMPrimitiveOverride;
    
    let { buildDotNetCIMPrimitiveOverride } = await import('./cIMPrimitiveOverride');
    let dnInstantiatedObject = await buildDotNetCIMPrimitiveOverride(jsCIMPrimitiveOverride);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMPrimitiveOverride detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMPrimitiveOverride', e);
    }
    
    return jsCIMPrimitiveOverride;
}


export async function buildDotNetCIMPrimitiveOverrideGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMPrimitiveOverride: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.valueExpressionInfo)) {
        let { buildDotNetCIMExpressionInfo } = await import('./cIMExpressionInfo');
        dotNetCIMPrimitiveOverride.valueExpressionInfo = await buildDotNetCIMExpressionInfo(jsObject.valueExpressionInfo);
    }
    if (hasValue(jsObject.expression)) {
        dotNetCIMPrimitiveOverride.expression = jsObject.expression;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMPrimitiveOverride.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.propertyName)) {
        dotNetCIMPrimitiveOverride.propertyName = jsObject.propertyName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMPrimitiveOverride.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMPrimitiveOverride.id = geoBlazorId;
    }

    return dotNetCIMPrimitiveOverride;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectMove } from './cIMGeometricEffectMove';

export async function buildJsCIMGeometricEffectMoveGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectMove: any = {};

    if (hasValue(dotNetObject.offsetX)) {
        jsCIMGeometricEffectMove.offsetX = dotNetObject.offsetX;
    }
    if (hasValue(dotNetObject.offsetY)) {
        jsCIMGeometricEffectMove.offsetY = dotNetObject.offsetY;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectMove.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectMove);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectMove;
    
    let { buildDotNetCIMGeometricEffectMove } = await import('./cIMGeometricEffectMove');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectMove(jsCIMGeometricEffectMove);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectMove detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectMove', e);
    }
    
    return jsCIMGeometricEffectMove;
}


export async function buildDotNetCIMGeometricEffectMoveGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectMove: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.offsetX)) {
        dotNetCIMGeometricEffectMove.offsetX = jsObject.offsetX;
    }
    if (hasValue(jsObject.offsetY)) {
        dotNetCIMGeometricEffectMove.offsetY = jsObject.offsetY;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectMove.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectMove.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectMove.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectMove;
}


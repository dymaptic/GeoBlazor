// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { buildDotNetContinuousRendererResult } from './continuousRendererResult';
import ContinuousRendererResult = __esri.ContinuousRendererResult;
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';

export async function buildJsContinuousRendererResultGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsContinuousRendererResult: any = {}
    if (hasValue(dotNetObject.renderer)) {
        let { buildJsClassBreaksRenderer } = await import('./classBreaksRenderer');
        jsContinuousRendererResult.renderer = await buildJsClassBreaksRenderer(dotNetObject.renderer, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.visualVariable)) {
        let { buildJsColorVariable } = await import('./colorVariable');
        jsContinuousRendererResult.visualVariable = await buildJsColorVariable(dotNetObject.visualVariable, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.basemapId)) {
        jsContinuousRendererResult.basemapId = dotNetObject.basemapId;
    }
    if (hasValue(dotNetObject.basemapTheme)) {
        jsContinuousRendererResult.basemapTheme = dotNetObject.basemapTheme;
    }
    if (hasValue(dotNetObject.colorScheme)) {
        jsContinuousRendererResult.colorScheme = dotNetObject.colorScheme;
    }
    if (hasValue(dotNetObject.defaultValuesUsed)) {
        jsContinuousRendererResult.defaultValuesUsed = dotNetObject.defaultValuesUsed;
    }
    if (hasValue(dotNetObject.statistics)) {
        jsContinuousRendererResult.statistics = dotNetObject.statistics;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsContinuousRendererResult);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsContinuousRendererResult;
    
    let dnInstantiatedObject = await buildDotNetContinuousRendererResult(jsContinuousRendererResult);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ContinuousRendererResult', e);
    }
    
    return jsContinuousRendererResult;
}

export async function buildDotNetContinuousRendererResultGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetContinuousRendererResult: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.renderer)) {
            let { buildDotNetClassBreaksRenderer } = await import('./classBreaksRenderer');
            dotNetContinuousRendererResult.renderer = await buildDotNetClassBreaksRenderer(jsObject.renderer);
        }
        if (hasValue(jsObject.visualVariable)) {
            let { buildDotNetColorVariable } = await import('./colorVariable');
            dotNetContinuousRendererResult.visualVariable = await buildDotNetColorVariable(jsObject.visualVariable);
        }
        if (hasValue(jsObject.basemapId)) {
            dotNetContinuousRendererResult.basemapId = jsObject.basemapId;
        }
        if (hasValue(jsObject.basemapTheme)) {
            dotNetContinuousRendererResult.basemapTheme = jsObject.basemapTheme;
        }
        if (hasValue(jsObject.colorScheme)) {
            dotNetContinuousRendererResult.colorScheme = jsObject.colorScheme;
        }
        if (hasValue(jsObject.defaultValuesUsed)) {
            dotNetContinuousRendererResult.defaultValuesUsed = jsObject.defaultValuesUsed;
        }
        if (hasValue(jsObject.statistics)) {
            dotNetContinuousRendererResult.statistics = jsObject.statistics;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetContinuousRendererResult.id = k;
                break;
            }
        }
    }

    return dotNetContinuousRendererResult;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import ElevationInfo from '@arcgis/core/symbols/support/ElevationInfo';
import { arcGisObjectRefs, jsObjectRefs, hasValue, removeCircularReferences } from './geoBlazorCore';

export async function buildJsElevationInfoGenerated(dotNetObject: any): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.featureExpressionInfo)) {
        let { buildJsFeatureExpressionInfo } = await import('./featureExpressionInfo');
        properties.featureExpressionInfo = await buildJsFeatureExpressionInfo(dotNetObject.featureExpressionInfo) as any;
    }

    if (hasValue(dotNetObject.mode)) {
        properties.mode = dotNetObject.mode;
    }
    if (hasValue(dotNetObject.offset)) {
        properties.offset = dotNetObject.offset;
    }
    if (hasValue(dotNetObject.unit)) {
        properties.unit = dotNetObject.unit;
    }
    let jsElevationInfo = new ElevationInfo(properties);

    jsObjectRefs[dotNetObject.id] = jsElevationInfo;
    arcGisObjectRefs[dotNetObject.id] = jsElevationInfo;

    return jsElevationInfo;
}


export async function buildDotNetElevationInfoGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }

    let dotNetElevationInfo: any = {};

    if (hasValue(jsObject.featureExpressionInfo)) {
        let { buildDotNetFeatureExpressionInfo } = await import('./featureExpressionInfo');
        dotNetElevationInfo.featureExpressionInfo = await buildDotNetFeatureExpressionInfo(jsObject.featureExpressionInfo);
    }

    if (hasValue(jsObject.mode)) {
        dotNetElevationInfo.mode = removeCircularReferences(jsObject.mode);
    }

    if (hasValue(jsObject.offset)) {
        dotNetElevationInfo.offset = jsObject.offset;
    }

    if (hasValue(jsObject.unit)) {
        dotNetElevationInfo.unit = removeCircularReferences(jsObject.unit);
    }


    return dotNetElevationInfo;
}


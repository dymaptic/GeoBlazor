// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectLocalizerFeather } from './cIMGeometricEffectLocalizerFeather';

export async function buildJsCIMGeometricEffectLocalizerFeatherGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectLocalizerFeather: any = {};

    if (hasValue(dotNetObject.angle)) {
        jsCIMGeometricEffectLocalizerFeather.angle = dotNetObject.angle;
    }
    if (hasValue(dotNetObject.length)) {
        jsCIMGeometricEffectLocalizerFeather.length = dotNetObject.length;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectLocalizerFeather.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.style)) {
        jsCIMGeometricEffectLocalizerFeather.style = dotNetObject.style;
    }
    if (hasValue(dotNetObject.width)) {
        jsCIMGeometricEffectLocalizerFeather.width = dotNetObject.width;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectLocalizerFeather);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectLocalizerFeather;
    
    let { buildDotNetCIMGeometricEffectLocalizerFeather } = await import('./cIMGeometricEffectLocalizerFeather');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectLocalizerFeather(jsCIMGeometricEffectLocalizerFeather);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectLocalizerFeather detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectLocalizerFeather', e);
    }
    
    return jsCIMGeometricEffectLocalizerFeather;
}


export async function buildDotNetCIMGeometricEffectLocalizerFeatherGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectLocalizerFeather: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.angle)) {
        dotNetCIMGeometricEffectLocalizerFeather.angle = jsObject.angle;
    }
    if (hasValue(jsObject.length)) {
        dotNetCIMGeometricEffectLocalizerFeather.length = jsObject.length;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectLocalizerFeather.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.style)) {
        dotNetCIMGeometricEffectLocalizerFeather.style = jsObject.style;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectLocalizerFeather.type = jsObject.type;
    }
    if (hasValue(jsObject.width)) {
        dotNetCIMGeometricEffectLocalizerFeather.width = jsObject.width;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectLocalizerFeather.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectLocalizerFeather;
}


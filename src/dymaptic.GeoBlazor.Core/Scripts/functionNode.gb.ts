// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetFunctionNode } from './functionNode';

export async function buildJsFunctionNodeGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsFunctionNode: any = {};
    if (hasValue(dotNetObject.args)) {
        let { buildJsListNode } = await import('./listNode');
        jsFunctionNode.args = await buildJsListNode(dotNetObject.args, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.name)) {
        jsFunctionNode.name = dotNetObject.name;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsFunctionNode);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsFunctionNode;
    
    return jsFunctionNode;
}


export async function buildDotNetFunctionNodeGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetFunctionNode: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.args)) {
        let { buildDotNetListNode } = await import('./listNode');
        dotNetFunctionNode.args = await buildDotNetListNode(jsObject.args);
    }
    if (hasValue(jsObject.name)) {
        dotNetFunctionNode.name = jsObject.name;
    }
    if (hasValue(jsObject.type)) {
        dotNetFunctionNode.type = jsObject.type;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetFunctionNode.id = k;
                break;
            }
        }
    }

    return dotNetFunctionNode;
}


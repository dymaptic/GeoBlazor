// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetHistogramDataLines } from './histogramDataLines';

export async function buildJsHistogramDataLinesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsHistogramDataLines: any = {};

    if (hasValue(dotNetObject.label)) {
        jsHistogramDataLines.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.value)) {
        jsHistogramDataLines.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsHistogramDataLines);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsHistogramDataLines;
    
    try {
        let { buildDotNetHistogramDataLines } = await import('./histogramDataLines');
        let dnInstantiatedObject = await buildDotNetHistogramDataLines(jsHistogramDataLines, layerId, viewId);

        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference?.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type HistogramDataLines detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for HistogramDataLines', e);
    }
    
    return jsHistogramDataLines;
}


export async function buildDotNetHistogramDataLinesGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetHistogramDataLines: any = {};
    
    if (hasValue(jsObject.label)) {
        dotNetHistogramDataLines.label = jsObject.label;
    }
    
    if (hasValue(jsObject.value)) {
        dotNetHistogramDataLines.value = jsObject.value;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetHistogramDataLines.id = geoBlazorId;
    }

    return dotNetHistogramDataLines;
}


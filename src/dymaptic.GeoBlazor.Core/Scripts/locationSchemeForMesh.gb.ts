// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetLocationSchemeForMesh } from './locationSchemeForMesh';

export async function buildJsLocationSchemeForMeshGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsLocationSchemeForMesh: any = {};
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsLocationSchemeForMesh.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.opacity)) {
        jsLocationSchemeForMesh.opacity = dotNetObject.opacity;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsLocationSchemeForMesh);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsLocationSchemeForMesh;
    
    let dnInstantiatedObject = await buildDotNetLocationSchemeForMesh(jsLocationSchemeForMesh);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for LocationSchemeForMesh', e);
    }
    
    return jsLocationSchemeForMesh;
}

export async function buildDotNetLocationSchemeForMeshGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetLocationSchemeForMesh: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetLocationSchemeForMesh.color = buildDotNetMapColor(jsObject.color);
        }
    if (hasValue(jsObject.opacity)) {
        dotNetLocationSchemeForMesh.opacity = jsObject.opacity;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetLocationSchemeForMesh.id = k;
                break;
            }
        }
    }

    return dotNetLocationSchemeForMesh;
}


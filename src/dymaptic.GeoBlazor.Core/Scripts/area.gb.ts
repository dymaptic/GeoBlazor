// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetArea } from './area';

export async function buildJsAreaGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsArea: any = {};

    if (hasValue(dotNetObject.unit)) {
        jsArea.unit = dotNetObject.unit;
    }
    if (hasValue(dotNetObject.value)) {
        jsArea.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsArea);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsArea;
    
    let { buildDotNetArea } = await import('./area');
    let dnInstantiatedObject = await buildDotNetArea(jsArea);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type Area detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Area', e);
    }
    
    return jsArea;
}


export async function buildDotNetAreaGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetArea: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.type)) {
        dotNetArea.type = jsObject.type;
    }
    if (hasValue(jsObject.unit)) {
        dotNetArea.unit = jsObject.unit;
    }
    if (hasValue(jsObject.value)) {
        dotNetArea.value = jsObject.value;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetArea.id = geoBlazorId;
    }

    return dotNetArea;
}


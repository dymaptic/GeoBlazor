// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetConsumedNodes } from './consumedNodes';

export async function buildJsConsumedNodesGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsConsumedNodes: any = {};

    if (hasValue(dotNetObject.optional) && dotNetObject.optional.length > 0) {
        jsConsumedNodes.optional = dotNetObject.optional;
    }
    if (hasValue(dotNetObject.required) && dotNetObject.required.length > 0) {
        jsConsumedNodes.required = dotNetObject.required;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsConsumedNodes);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsConsumedNodes;
    
    let { buildDotNetConsumedNodes } = await import('./consumedNodes');
    let dnInstantiatedObject = await buildDotNetConsumedNodes(jsConsumedNodes, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type ConsumedNodes detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ConsumedNodes', e);
    }
    
    return jsConsumedNodes;
}


export async function buildDotNetConsumedNodesGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetConsumedNodes: any = {};
    
    if (hasValue(jsObject.optional)) {
        dotNetConsumedNodes.optional = jsObject.optional;
    }
    
    if (hasValue(jsObject.required)) {
        dotNetConsumedNodes.required = jsObject.required;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetConsumedNodes.id = geoBlazorId;
    }

    return dotNetConsumedNodes;
}


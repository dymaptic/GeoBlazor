// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file
import Column from '@arcgis/core/widgets/FeatureTable/Grid/Column';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class ColumnGenerated implements IPropertyWrapper {
    public component: Column;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: Column) {
        this.component = component;
    }

    // region methods

    unwrap() {
        return this.component;
    }

    async closeMenu(): Promise<void> {
        this.component.closeMenu();
    }

    async openMenu(): Promise<void> {
        this.component.openMenu();
    }

    async sort(): Promise<void> {
        this.component.sort();
    }

    // region properties

    async getMenuConfig(): Promise<any> {
        if (!hasValue(this.component.menuConfig)) {
            return null;
        }

        let {buildDotNetColumnTableMenuConfig} = await import('./columnTableMenuConfig');
        return await buildDotNetColumnTableMenuConfig(this.component.menuConfig);
    }

    getProperty(prop: string): any {
        return this.component[prop];
    }

    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsColumnGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsColumn = new Column();

    if (hasValue(dotNetObject.autoWidth)) {
        jsColumn.autoWidth = dotNetObject.autoWidth;
    }
    if (hasValue(dotNetObject.direction)) {
        jsColumn.direction = dotNetObject.direction;
    }
    if (hasValue(dotNetObject.effectiveLabel)) {
        jsColumn.effectiveLabel = dotNetObject.effectiveLabel;
    }
    if (hasValue(dotNetObject.flexGrow)) {
        jsColumn.flexGrow = dotNetObject.flexGrow;
    }
    if (hasValue(dotNetObject.icon)) {
        jsColumn.icon = dotNetObject.icon;
    }
    if (hasValue(dotNetObject.invalid)) {
        jsColumn.invalid = dotNetObject.invalid;
    }
    if (hasValue(dotNetObject.label)) {
        jsColumn.label = dotNetObject.label;
    }
    if (hasValue(dotNetObject.sortable)) {
        jsColumn.sortable = dotNetObject.sortable;
    }
    if (hasValue(dotNetObject.textAlign)) {
        jsColumn.textAlign = dotNetObject.textAlign;
    }
    if (hasValue(dotNetObject.textWrap)) {
        jsColumn.textWrap = dotNetObject.textWrap;
    }
    if (hasValue(dotNetObject.visibleElements)) {
        const {
            id,
            dotNetComponentReference,
            layerId,
            viewId,
            ...sanitizedVisibleElements
        } = dotNetObject.visibleElements;
        jsColumn.visibleElements = sanitizedVisibleElements;
    }
    if (hasValue(dotNetObject.width)) {
        jsColumn.width = dotNetObject.width;
    }

    let {default: ColumnWrapper} = await import('./column');
    let columnWrapper = new ColumnWrapper(jsColumn);
    columnWrapper.geoBlazorId = dotNetObject.id;
    columnWrapper.viewId = viewId;
    columnWrapper.layerId = layerId;

    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(columnWrapper);
    jsObjectRefs[dotNetObject.id] = columnWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsColumn;
    let {buildDotNetColumn} = await import('./column');
    let dnInstantiatedObject = await buildDotNetColumn(jsColumn);

    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Column', e);
    }

    return jsColumn;
}

export async function buildDotNetColumnGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }

    let dotNetColumn: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.menuConfig)) {
        let {buildDotNetColumnTableMenuConfig} = await import('./columnTableMenuConfig');
        dotNetColumn.menuConfig = await buildDotNetColumnTableMenuConfig(jsObject.menuConfig);
    }
    if (hasValue(jsObject.autoWidth)) {
        dotNetColumn.autoWidth = jsObject.autoWidth;
    }
    if (hasValue(jsObject.direction)) {
        dotNetColumn.direction = jsObject.direction;
    }
    if (hasValue(jsObject.effectiveLabel)) {
        dotNetColumn.effectiveLabel = jsObject.effectiveLabel;
    }
    if (hasValue(jsObject.fieldName)) {
        dotNetColumn.fieldName = jsObject.fieldName;
    }
    if (hasValue(jsObject.flexGrow)) {
        dotNetColumn.flexGrow = jsObject.flexGrow;
    }
    if (hasValue(jsObject.formatFunction)) {
        dotNetColumn.formatFunction = jsObject.formatFunction;
    }
    if (hasValue(jsObject.frozen)) {
        dotNetColumn.frozen = jsObject.frozen;
    }
    if (hasValue(jsObject.frozenToEnd)) {
        dotNetColumn.frozenToEnd = jsObject.frozenToEnd;
    }
    if (hasValue(jsObject.icon)) {
        dotNetColumn.icon = jsObject.icon;
    }
    if (hasValue(jsObject.initialSortPriority)) {
        dotNetColumn.initialSortPriority = jsObject.initialSortPriority;
    }
    if (hasValue(jsObject.invalid)) {
        dotNetColumn.invalid = jsObject.invalid;
    }
    if (hasValue(jsObject.label)) {
        dotNetColumn.label = jsObject.label;
    }
    if (hasValue(jsObject.menu)) {
        dotNetColumn.menu = jsObject.menu;
    }
    if (hasValue(jsObject.menuIsOpen)) {
        dotNetColumn.menuIsOpen = jsObject.menuIsOpen;
    }
    if (hasValue(jsObject.menuIsVisible)) {
        dotNetColumn.menuIsVisible = jsObject.menuIsVisible;
    }
    if (hasValue(jsObject.resizable)) {
        dotNetColumn.resizable = jsObject.resizable;
    }
    if (hasValue(jsObject.sortable)) {
        dotNetColumn.sortable = jsObject.sortable;
    }
    if (hasValue(jsObject.textAlign)) {
        dotNetColumn.textAlign = jsObject.textAlign;
    }
    if (hasValue(jsObject.textWrap)) {
        dotNetColumn.textWrap = jsObject.textWrap;
    }
    if (hasValue(jsObject.timeZone)) {
        dotNetColumn.timeZone = jsObject.timeZone;
    }
    if (hasValue(jsObject.visibleElements)) {
        dotNetColumn.visibleElements = jsObject.visibleElements;
    }
    if (hasValue(jsObject.width)) {
        dotNetColumn.width = jsObject.width;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetColumn.id = k;
                break;
            }
        }
    }

    return dotNetColumn;
}


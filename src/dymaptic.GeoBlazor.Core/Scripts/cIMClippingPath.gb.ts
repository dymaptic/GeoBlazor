// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMClippingPath } from './cIMClippingPath';

export async function buildJsCIMClippingPathGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMClippingPath: any = {};
    if (hasValue(dotNetObject.path)) {
        let { buildJsExternalReferencePolygon } = await import('./externalReferencePolygon');
        jsCIMClippingPath.path = await buildJsExternalReferencePolygon(dotNetObject.path, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.clippingType)) {
        jsCIMClippingPath.clippingType = dotNetObject.clippingType;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMClippingPath);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMClippingPath;
    
    let { buildDotNetCIMClippingPath } = await import('./cIMClippingPath');
    let dnInstantiatedObject = await buildDotNetCIMClippingPath(jsCIMClippingPath);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMClippingPath detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMClippingPath', e);
    }
    
    return jsCIMClippingPath;
}


export async function buildDotNetCIMClippingPathGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMClippingPath: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.path)) {
        let { buildDotNetExternalReferencePolygon } = await import('./externalReferencePolygon');
        dotNetCIMClippingPath.path = await buildDotNetExternalReferencePolygon(jsObject.path);
    }
    if (hasValue(jsObject.clippingType)) {
        dotNetCIMClippingPath.clippingType = jsObject.clippingType;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMClippingPath.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMClippingPath.id = geoBlazorId;
    }

    return dotNetCIMClippingPath;
}


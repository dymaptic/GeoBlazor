// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMVectorMarkerAnchorPoint } from './cIMVectorMarkerAnchorPoint';

export async function buildJsCIMVectorMarkerAnchorPointGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMVectorMarkerAnchorPoint: any = {};

    if (hasValue(dotNetObject.x)) {
        jsCIMVectorMarkerAnchorPoint.x = dotNetObject.x;
    }
    if (hasValue(dotNetObject.y)) {
        jsCIMVectorMarkerAnchorPoint.y = dotNetObject.y;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMVectorMarkerAnchorPoint);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMVectorMarkerAnchorPoint;
    
    let { buildDotNetCIMVectorMarkerAnchorPoint } = await import('./cIMVectorMarkerAnchorPoint');
    let dnInstantiatedObject = await buildDotNetCIMVectorMarkerAnchorPoint(jsCIMVectorMarkerAnchorPoint);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMVectorMarkerAnchorPoint detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMVectorMarkerAnchorPoint', e);
    }
    
    return jsCIMVectorMarkerAnchorPoint;
}


export async function buildDotNetCIMVectorMarkerAnchorPointGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMVectorMarkerAnchorPoint: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.x)) {
        dotNetCIMVectorMarkerAnchorPoint.x = jsObject.x;
    }
    if (hasValue(jsObject.y)) {
        dotNetCIMVectorMarkerAnchorPoint.y = jsObject.y;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMVectorMarkerAnchorPoint.id = geoBlazorId;
    }

    return dotNetCIMVectorMarkerAnchorPoint;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetDirectionalPadVisibleElements } from './directionalPadVisibleElements';

export async function buildJsDirectionalPadVisibleElementsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsDirectionalPadVisibleElements: any = {};

    if (hasValue(dotNetObject.directionalButtons)) {
        jsDirectionalPadVisibleElements.directionalButtons = dotNetObject.directionalButtons;
    }
    if (hasValue(dotNetObject.rotationResetButton)) {
        jsDirectionalPadVisibleElements.rotationResetButton = dotNetObject.rotationResetButton;
    }
    if (hasValue(dotNetObject.rotationSlider)) {
        jsDirectionalPadVisibleElements.rotationSlider = dotNetObject.rotationSlider;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsDirectionalPadVisibleElements);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsDirectionalPadVisibleElements;
    
    let { buildDotNetDirectionalPadVisibleElements } = await import('./directionalPadVisibleElements');
    let dnInstantiatedObject = await buildDotNetDirectionalPadVisibleElements(jsDirectionalPadVisibleElements, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type DirectionalPadVisibleElements detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for DirectionalPadVisibleElements', e);
    }
    
    return jsDirectionalPadVisibleElements;
}


export async function buildDotNetDirectionalPadVisibleElementsGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsDirectionalPadVisibleElements } = await import('./directionalPadVisibleElements');
        jsComponentRef = await buildJsDirectionalPadVisibleElements(jsObject, layerId, viewId);
    }
    
    let dotNetDirectionalPadVisibleElements: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.directionalButtons)) {
        dotNetDirectionalPadVisibleElements.directionalButtons = jsObject.directionalButtons;
    }
    if (hasValue(jsObject.rotationResetButton)) {
        dotNetDirectionalPadVisibleElements.rotationResetButton = jsObject.rotationResetButton;
    }
    if (hasValue(jsObject.rotationSlider)) {
        dotNetDirectionalPadVisibleElements.rotationSlider = jsObject.rotationSlider;
    }


    if (hasValue(geoBlazorId)) {
        dotNetDirectionalPadVisibleElements.id = geoBlazorId;
    }

    return dotNetDirectionalPadVisibleElements;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import Point from '@arcgis/core/geometry/Point';
import { hasValue, generateSerializableJson } from './geoBlazorCore';
import BaseComponent from "./baseComponent";

export default class PointGenerated extends BaseComponent {
    public component: Point;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: Point) {
        super(component);
        this.component = component;
    }
    

    async updateComponent(dotNetObject: any): Promise<void> {
        if (hasValue(dotNetObject.spatialReference)) {
            let { buildJsSpatialReference } = await import('./spatialReference');
            this.component.spatialReference = buildJsSpatialReference(dotNetObject.spatialReference) as any;
        }

        if (hasValue(dotNetObject.hasM)) {
            this.component.hasM = dotNetObject.hasM;
        }
        if (hasValue(dotNetObject.hasZ)) {
            this.component.hasZ = dotNetObject.hasZ;
        }
        if (hasValue(dotNetObject.latitude)) {
            this.component.latitude = dotNetObject.latitude;
        }
        if (hasValue(dotNetObject.longitude)) {
            this.component.longitude = dotNetObject.longitude;
        }
        if (hasValue(dotNetObject.m)) {
            this.component.m = dotNetObject.m;
        }
        if (hasValue(dotNetObject.x)) {
            this.component.x = dotNetObject.x;
        }
        if (hasValue(dotNetObject.y)) {
            this.component.y = dotNetObject.y;
        }
        if (hasValue(dotNetObject.z)) {
            this.component.z = dotNetObject.z;
        }
    }
    
    // region methods
    async copy(other: any): Promise<any> {
        let { buildJsPoint } = await import('./point');
        let jsOther = buildJsPoint(other) as any;
        let result = this.component.copy(jsOther);
        let { buildDotNetPoint } = await import('./point');
        return buildDotNetPoint(result);
    }

    async distance(other: any): Promise<any> {
        let { buildJsPoint } = await import('./point');
        let jsOther = buildJsPoint(other) as any;
        return this.component.distance(jsOther);
    }

    async normalize(): Promise<any> {
        let result = this.component.normalize();
        let { buildDotNetPoint } = await import('./point');
        return buildDotNetPoint(result);
    }

    // region properties
    
    getCache(): any {
        if (!hasValue(this.component.cache)) {
            return null;
        }
        
        return generateSerializableJson(this.component.cache);
    }
    
    async getExtent(): Promise<any> {
        if (!hasValue(this.component.extent)) {
            return null;
        }
        
        let { buildDotNetExtent } = await import('./extent');
        return buildDotNetExtent(this.component.extent);
    }
    
    async getSpatialReference(): Promise<any> {
        if (!hasValue(this.component.spatialReference)) {
            return null;
        }
        
        let { buildDotNetSpatialReference } = await import('./spatialReference');
        return buildDotNetSpatialReference(this.component.spatialReference);
    }
    
    async setSpatialReference(value: any): Promise<void> {
        let { buildJsSpatialReference } = await import('./spatialReference');
        this.component.spatialReference =  buildJsSpatialReference(value);
    }
    
}


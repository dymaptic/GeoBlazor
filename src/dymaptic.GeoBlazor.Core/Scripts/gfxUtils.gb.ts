// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetGfxUtils } from './gfxUtils';

export async function buildJsGfxUtilsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsgfxUtils: any = {}
    if (hasValue(dotNetObject.defaultThematicColor)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsgfxUtils.defaultThematicColor = buildJsMapColor(dotNetObject.defaultThematicColor) as any;
    }

    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsgfxUtils);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsgfxUtils;
    
    let dnInstantiatedObject = await buildDotNetGfxUtils(jsgfxUtils);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for GfxUtils', e);
    }
    
    return jsgfxUtils;
}

export async function buildDotNetGfxUtilsGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetGfxUtils: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.defaultThematicColor)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetGfxUtils.defaultThematicColor = buildDotNetMapColor(jsObject.defaultThematicColor);
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetGfxUtils.id = k;
                break;
            }
        }
    }

    return dotNetGfxUtils;
}


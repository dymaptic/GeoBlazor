// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file


import ActiveLayerInfo from '@arcgis/core/widgets/Legend/support/ActiveLayerInfo';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class ActiveLayerInfoGenerated implements IPropertyWrapper {
    public component: ActiveLayerInfo;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: ActiveLayerInfo) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    // region properties
    
    async getChildren(): Promise<any> {
        if (!hasValue(this.component.children)) {
            return null;
        }
        
        let { buildDotNetActiveLayerInfo } = await import('./activeLayerInfo');
        return await Promise.all(this.component.children.map(async i => await buildDotNetActiveLayerInfo(i)));
    }
    
    async setChildren(value: any): Promise<void> {
        let { buildJsActiveLayerInfo } = await import('./activeLayerInfo');
        this.component.children = await Promise.all(value.map(async i => await buildJsActiveLayerInfo(i, this.layerId, this.viewId))) as any;
    }
    
    async getLayer(): Promise<any> {
        if (!hasValue(this.component.layer)) {
            return null;
        }
        
        let { buildDotNetLayer } = await import('./layer');
        return await buildDotNetLayer(this.component.layer);
    }
    async setLayer(value: any): Promise<void> {
        let { buildJsLayer } = await import('./layer');
        this.component.layer = await  buildJsLayer(value, this.layerId, this.viewId);
    }
    async getLayerView(): Promise<any> {
        if (!hasValue(this.component.layerView)) {
            return null;
        }
        
        let { buildDotNetLayerView } = await import('./layerView');
        return await buildDotNetLayerView(this.component.layerView);
    }
    async setLayerView(value: any): Promise<void> {
        let { buildJsLayerView } = await import('./layerView');
        this.component.layerView = await  buildJsLayerView(value, this.layerId, this.viewId);
    }
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsActiveLayerInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsActiveLayerInfo = new ActiveLayerInfo();
    if (hasValue(dotNetObject.layer)) {
        let { buildJsLayer } = await import('./layer');
        jsActiveLayerInfo.layer = await buildJsLayer(dotNetObject.layer, layerId, viewId) as any;
    }
    if (hasValue(dotNetObject.layerView)) {
        let { buildJsLayerView } = await import('./layerView');
        jsActiveLayerInfo.layerView = await buildJsLayerView(dotNetObject.layerView, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.hideLayersNotInCurrentView)) {
        jsActiveLayerInfo.hideLayersNotInCurrentView = dotNetObject.hideLayersNotInCurrentView;
    }
    if (hasValue(dotNetObject.isScaleDriven)) {
        jsActiveLayerInfo.isScaleDriven = dotNetObject.isScaleDriven;
    }
    if (hasValue(dotNetObject.legendElements)) {
        jsActiveLayerInfo.legendElements = dotNetObject.legendElements;
    }
    if (hasValue(dotNetObject.ready)) {
        jsActiveLayerInfo.ready = dotNetObject.ready;
    }
    if (hasValue(dotNetObject.respectLayerVisibility)) {
        jsActiveLayerInfo.respectLayerVisibility = dotNetObject.respectLayerVisibility;
    }
    if (hasValue(dotNetObject.scale)) {
        jsActiveLayerInfo.scale = dotNetObject.scale;
    }
    if (hasValue(dotNetObject.sublayerIds)) {
        jsActiveLayerInfo.sublayerIds = dotNetObject.sublayerIds;
    }
    if (hasValue(dotNetObject.title)) {
        jsActiveLayerInfo.title = dotNetObject.title;
    }
    if (hasValue(dotNetObject.version)) {
        jsActiveLayerInfo.version = dotNetObject.version;
    }
    if (hasValue(dotNetObject.view)) {
        jsActiveLayerInfo.view = dotNetObject.view;
    }
    let { default: ActiveLayerInfoWrapper } = await import('./activeLayerInfo');
    let activeLayerInfoWrapper = new ActiveLayerInfoWrapper(jsActiveLayerInfo);
    activeLayerInfoWrapper.geoBlazorId = dotNetObject.id;
    activeLayerInfoWrapper.viewId = viewId;
    activeLayerInfoWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(activeLayerInfoWrapper);
    jsObjectRefs[dotNetObject.id] = activeLayerInfoWrapper;

    arcGisObjectRefs[dotNetObject.id] = jsActiveLayerInfo;
    let { buildDotNetActiveLayerInfo } = await import('./activeLayerInfo');
    let dnInstantiatedObject = await buildDotNetActiveLayerInfo(jsActiveLayerInfo, layerId, viewId);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ActiveLayerInfo', e);
    }
    
    return jsActiveLayerInfo;
}

export async function buildDotNetActiveLayerInfoGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetActiveLayerInfo: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.layer)) {
            let { buildDotNetLayer } = await import('./layer');
            dotNetActiveLayerInfo.layer = await buildDotNetLayer(jsObject.layer);
        }
        if (hasValue(jsObject.layerView)) {
            let { buildDotNetLayerView } = await import('./layerView');
            dotNetActiveLayerInfo.layerView = await buildDotNetLayerView(jsObject.layerView);
        }
        if (hasValue(jsObject.hideLayersNotInCurrentView)) {
            dotNetActiveLayerInfo.hideLayersNotInCurrentView = jsObject.hideLayersNotInCurrentView;
        }
        if (hasValue(jsObject.isScaleDriven)) {
            dotNetActiveLayerInfo.isScaleDriven = jsObject.isScaleDriven;
        }
        if (hasValue(jsObject.legendElements)) {
            dotNetActiveLayerInfo.legendElements = jsObject.legendElements;
        }
        if (hasValue(jsObject.opacity)) {
            dotNetActiveLayerInfo.opacity = jsObject.opacity;
        }
        if (hasValue(jsObject.ready)) {
            dotNetActiveLayerInfo.ready = jsObject.ready;
        }
        if (hasValue(jsObject.respectLayerVisibility)) {
            dotNetActiveLayerInfo.respectLayerVisibility = jsObject.respectLayerVisibility;
        }
        if (hasValue(jsObject.scale)) {
            dotNetActiveLayerInfo.scale = jsObject.scale;
        }
        if (hasValue(jsObject.sublayerIds)) {
            dotNetActiveLayerInfo.sublayerIds = jsObject.sublayerIds;
        }
        if (hasValue(jsObject.title)) {
            dotNetActiveLayerInfo.title = jsObject.title;
        }
        if (hasValue(jsObject.version)) {
            dotNetActiveLayerInfo.version = jsObject.version;
        }
        if (hasValue(jsObject.view)) {
            dotNetActiveLayerInfo.view = jsObject.view;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetActiveLayerInfo.id = k;
                break;
            }
        }
    }

    return dotNetActiveLayerInfo;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMCGAAttribute } from './cIMCGAAttribute';

export async function buildJsCIMCGAAttributeGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMCGAAttribute: any = {};

    if (hasValue(dotNetObject.cGAAttributeType)) {
        jsCIMCGAAttribute.CGAAttributeType = dotNetObject.cGAAttributeType;
    }
    if (hasValue(dotNetObject.name)) {
        jsCIMCGAAttribute.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.value)) {
        jsCIMCGAAttribute.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMCGAAttribute);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMCGAAttribute;
    
    let { buildDotNetCIMCGAAttribute } = await import('./cIMCGAAttribute');
    let dnInstantiatedObject = await buildDotNetCIMCGAAttribute(jsCIMCGAAttribute);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMCGAAttribute detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMCGAAttribute', e);
    }
    
    return jsCIMCGAAttribute;
}


export async function buildDotNetCIMCGAAttributeGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMCGAAttribute: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.CGAAttributeType)) {
        dotNetCIMCGAAttribute.cGAAttributeType = jsObject.CGAAttributeType;
    }
    if (hasValue(jsObject.name)) {
        dotNetCIMCGAAttribute.name = jsObject.name;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMCGAAttribute.type = jsObject.type;
    }
    if (hasValue(jsObject.value)) {
        dotNetCIMCGAAttribute.value = jsObject.value;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMCGAAttribute.id = geoBlazorId;
    }

    return dotNetCIMCGAAttribute;
}


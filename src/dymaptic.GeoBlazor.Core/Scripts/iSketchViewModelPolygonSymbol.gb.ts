// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetISketchViewModelPolygonSymbol } from './iSketchViewModelPolygonSymbol';

export async function buildJsISketchViewModelPolygonSymbolGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsSketchViewModelPolygonSymbol: any = {};

    
    let jsObjectRef = DotNet.createJSObjectReference(jsSketchViewModelPolygonSymbol);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsSketchViewModelPolygonSymbol;
    
    let { buildDotNetISketchViewModelPolygonSymbol } = await import('./iSketchViewModelPolygonSymbol');
    let dnInstantiatedObject = await buildDotNetISketchViewModelPolygonSymbol(jsSketchViewModelPolygonSymbol, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type ISketchViewModelPolygonSymbol detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ISketchViewModelPolygonSymbol', e);
    }
    
    return jsSketchViewModelPolygonSymbol;
}


export async function buildDotNetISketchViewModelPolygonSymbolGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsISketchViewModelPolygonSymbol } = await import('./iSketchViewModelPolygonSymbol');
        jsComponentRef = await buildJsISketchViewModelPolygonSymbol(jsObject, layerId, viewId);
    }
    
    let dotNetISketchViewModelPolygonSymbol: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };


    if (hasValue(geoBlazorId)) {
        dotNetISketchViewModelPolygonSymbol.id = geoBlazorId;
    }

    return dotNetISketchViewModelPolygonSymbol;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectExtension } from './cIMGeometricEffectExtension';

export async function buildJsCIMGeometricEffectExtensionGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectExtension: any = {};

    if (hasValue(dotNetObject.deflection)) {
        jsCIMGeometricEffectExtension.deflection = dotNetObject.deflection;
    }
    if (hasValue(dotNetObject.length)) {
        jsCIMGeometricEffectExtension.length = dotNetObject.length;
    }
    if (hasValue(dotNetObject.origin)) {
        jsCIMGeometricEffectExtension.origin = dotNetObject.origin;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectExtension.primitiveName = dotNetObject.primitiveName;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectExtension);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectExtension;
    
    let { buildDotNetCIMGeometricEffectExtension } = await import('./cIMGeometricEffectExtension');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectExtension(jsCIMGeometricEffectExtension);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectExtension detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectExtension', e);
    }
    
    return jsCIMGeometricEffectExtension;
}


export async function buildDotNetCIMGeometricEffectExtensionGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectExtension: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.deflection)) {
        dotNetCIMGeometricEffectExtension.deflection = jsObject.deflection;
    }
    if (hasValue(jsObject.length)) {
        dotNetCIMGeometricEffectExtension.length = jsObject.length;
    }
    if (hasValue(jsObject.origin)) {
        dotNetCIMGeometricEffectExtension.origin = jsObject.origin;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectExtension.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectExtension.type = jsObject.type;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectExtension.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectExtension;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetUniqueValuesResultUniqueValueInfos } from './uniqueValuesResultUniqueValueInfos';

export async function buildJsUniqueValuesResultUniqueValueInfosGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsUniqueValuesResultUniqueValueInfos: any = {};

    if (hasValue(dotNetObject.count)) {
        jsUniqueValuesResultUniqueValueInfos.count = dotNetObject.count;
    }
    if (hasValue(dotNetObject.value)) {
        jsUniqueValuesResultUniqueValueInfos.value = dotNetObject.value;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsUniqueValuesResultUniqueValueInfos);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsUniqueValuesResultUniqueValueInfos;
    
    let { buildDotNetUniqueValuesResultUniqueValueInfos } = await import('./uniqueValuesResultUniqueValueInfos');
    let dnInstantiatedObject = await buildDotNetUniqueValuesResultUniqueValueInfos(jsUniqueValuesResultUniqueValueInfos, layerId, viewId);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.debug(`Circular reference in serializing type UniqueValuesResultUniqueValueInfos detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for UniqueValuesResultUniqueValueInfos', e);
    }
    
    return jsUniqueValuesResultUniqueValueInfos;
}


export async function buildDotNetUniqueValuesResultUniqueValueInfosGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let geoBlazorId = lookupGeoBlazorId(jsObject);
    
    let jsComponentRef: any;
    if (hasValue(geoBlazorId)) {
        jsComponentRef = jsObjectRefs[geoBlazorId!];
    } else {
        let { buildJsUniqueValuesResultUniqueValueInfos } = await import('./uniqueValuesResultUniqueValueInfos');
        jsComponentRef = await buildJsUniqueValuesResultUniqueValueInfos(jsObject, layerId, viewId);
    }
    
    let dotNetUniqueValuesResultUniqueValueInfos: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsComponentRef)
    };
    if (hasValue(jsObject.count)) {
        dotNetUniqueValuesResultUniqueValueInfos.count = jsObject.count;
    }
    if (hasValue(jsObject.value)) {
        dotNetUniqueValuesResultUniqueValueInfos.value = jsObject.value;
    }


    if (hasValue(geoBlazorId)) {
        dotNetUniqueValuesResultUniqueValueInfos.id = geoBlazorId;
    }

    return dotNetUniqueValuesResultUniqueValueInfos;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import SlideElements from '@arcgis/core/webscene/support/SlideElements';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './geoBlazorCore';

export async function buildJsSlideElementsGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.analyses) && dotNetObject.analyses.length > 0) {
        let { buildJsSliceAnalysis } = await import('./sliceAnalysis');
        properties.analyses = await Promise.all(dotNetObject.analyses.map(async i => await buildJsSliceAnalysis(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.popupInfo)) {
        let { buildJsSlidePopupInfo } = await import('./slidePopupInfo');
        properties.popupInfo = await buildJsSlidePopupInfo(dotNetObject.popupInfo, layerId, viewId) as any;
    }

    if (hasValue(dotNetObject.legendInfo)) {
        properties.legendInfo = dotNetObject.legendInfo;
    }
    let jsSlideElements = new SlideElements(properties);
    
    jsObjectRefs[dotNetObject.id] = jsSlideElements;
    arcGisObjectRefs[dotNetObject.id] = jsSlideElements;
    
    return jsSlideElements;
}


export async function buildDotNetSlideElementsGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSlideElements: any = {};
    
    if (hasValue(jsObject.analyses)) {
        let { buildDotNetSliceAnalysis } = await import('./sliceAnalysis');
        dotNetSlideElements.analyses = await Promise.all(jsObject.analyses.map(async i => await buildDotNetSliceAnalysis(i, viewId)));
    }
    
    if (hasValue(jsObject.popupInfo)) {
        let { buildDotNetSlidePopupInfo } = await import('./slidePopupInfo');
        dotNetSlideElements.popupInfo = await buildDotNetSlidePopupInfo(jsObject.popupInfo, layerId, viewId);
    }
    
    if (hasValue(jsObject.legendInfo)) {
        dotNetSlideElements.legendInfo = removeCircularReferences(jsObject.legendInfo);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetSlideElements.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetSlideElements.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for SlideElements', e);
            }
        }
    }
    if (hasValue(dotNetSlideElements.id)) {
        jsObjectRefs[dotNetSlideElements.id] ??= jsObject;
        arcGisObjectRefs[dotNetSlideElements.id] ??= jsObject;
    }

    return dotNetSlideElements;
}


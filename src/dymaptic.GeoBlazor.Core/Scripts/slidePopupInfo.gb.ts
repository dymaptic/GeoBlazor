// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import SlidePopupInfo from '@arcgis/core/webscene/support/SlidePopupInfo';
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './geoBlazorCore';

export async function buildJsSlidePopupInfoGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.features) && dotNetObject.features.length > 0) {
        let { buildJsFeatureReference } = await import('./featureReference');
        properties.features = await Promise.all(dotNetObject.features.map(async i => await buildJsFeatureReference(i, layerId, viewId))) as any;
    }
    if (hasValue(dotNetObject.location)) {
        let { buildJsPoint } = await import('./point');
        properties.location = buildJsPoint(dotNetObject.location) as any;
    }

    if (hasValue(dotNetObject.selectedFeatureIndex)) {
        properties.selectedFeatureIndex = dotNetObject.selectedFeatureIndex;
    }
    let jsSlidePopupInfo = new SlidePopupInfo(properties);
    
    jsObjectRefs[dotNetObject.id] = jsSlidePopupInfo;
    arcGisObjectRefs[dotNetObject.id] = jsSlidePopupInfo;
    
    return jsSlidePopupInfo;
}


export async function buildDotNetSlidePopupInfoGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetSlidePopupInfo: any = {};
    
    if (hasValue(jsObject.features)) {
        let { buildDotNetFeatureReference } = await import('./featureReference');
        dotNetSlidePopupInfo.features = await Promise.all(jsObject.features.map(async i => await buildDotNetFeatureReference(i, viewId)));
    }
    
    if (hasValue(jsObject.location)) {
        let { buildDotNetPoint } = await import('./point');
        dotNetSlidePopupInfo.location = buildDotNetPoint(jsObject.location);
    }
    
    if (hasValue(jsObject.selectedFeatureIndex)) {
        dotNetSlidePopupInfo.selectedFeatureIndex = jsObject.selectedFeatureIndex;
    }
    

    return dotNetSlidePopupInfo;
}


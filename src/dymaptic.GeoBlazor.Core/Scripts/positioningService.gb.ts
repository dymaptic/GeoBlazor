// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import PositioningService from '@arcgis/core/webdoc/ips/PositioningService';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';

export async function buildJsPositioningServiceGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};
    if (hasValue(dotNetObject.portalItem)) {
        let { buildJsPortalItem } = await import('./portalItem');
        properties.portalItem = await buildJsPortalItem(dotNetObject.portalItem, layerId, viewId) as any;
    }

    let jsPositioningService = new PositioningService(properties);
    
    jsObjectRefs[dotNetObject.id] = jsPositioningService;
    arcGisObjectRefs[dotNetObject.id] = jsPositioningService;
    
    return jsPositioningService;
}


export async function buildDotNetPositioningServiceGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetPositioningService: any = {};
    
    if (hasValue(jsObject.portalItem)) {
        let { buildDotNetPortalItem } = await import('./portalItem');
        dotNetPositioningService.portalItem = await buildDotNetPortalItem(jsObject.portalItem, layerId, viewId);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetPositioningService.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetPositioningService.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for PositioningService', e);
            }
        }
    }

    if (hasValue(dotNetPositioningService.id)) {
        jsObjectRefs[dotNetPositioningService.id] ??= jsObject;
        arcGisObjectRefs[dotNetPositioningService.id] ??= jsObject;
    }
    return dotNetPositioningService;
}


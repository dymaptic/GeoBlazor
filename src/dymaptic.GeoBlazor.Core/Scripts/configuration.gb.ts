// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import Configuration from '@arcgis/core/webdoc/ips/Configuration';
import { arcGisObjectRefs, jsObjectRefs, dotNetRefs, hasValue, lookupGeoBlazorId, removeCircularReferences } from './geoBlazorCore';

export async function buildJsConfigurationGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let properties: any = {};

    if (hasValue(dotNetObject.appleIPS)) {
        properties.appleIPS = dotNetObject.appleIPS;
    }
    if (hasValue(dotNetObject.gnss)) {
        properties.gnss = dotNetObject.gnss;
    }
    if (hasValue(dotNetObject.pathSnapping)) {
        properties.pathSnapping = dotNetObject.pathSnapping;
    }
    if (hasValue(dotNetObject.smoothing)) {
        properties.smoothing = dotNetObject.smoothing;
    }
    let jsConfiguration = new Configuration(properties);
    
    jsObjectRefs[dotNetObject.id] = jsConfiguration;
    arcGisObjectRefs[dotNetObject.id] = jsConfiguration;
    
    return jsConfiguration;
}


export async function buildDotNetConfigurationGenerated(jsObject: any, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetConfiguration: any = {};
    
    if (hasValue(jsObject.appleIPS)) {
        dotNetConfiguration.appleIPS = removeCircularReferences(jsObject.appleIPS);
    }
    
    if (hasValue(jsObject.gnss)) {
        dotNetConfiguration.gnss = removeCircularReferences(jsObject.gnss);
    }
    
    if (hasValue(jsObject.pathSnapping)) {
        dotNetConfiguration.pathSnapping = removeCircularReferences(jsObject.pathSnapping);
    }
    
    if (hasValue(jsObject.smoothing)) {
        dotNetConfiguration.smoothing = removeCircularReferences(jsObject.smoothing);
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetConfiguration.id = geoBlazorId;
    } else if (hasValue(viewId)) {
        let dotNetRef = dotNetRefs[viewId!];
        if (hasValue(dotNetRef)) {
            try {
                dotNetConfiguration.id = await dotNetRef.invokeMethodAsync('GetId');
            } catch (e) {
                console.error('Error invoking GetId for Configuration', e);
            }
        }
    }

    if (hasValue(dotNetConfiguration.id)) {
        jsObjectRefs[dotNetConfiguration.id] ??= jsObject;
        arcGisObjectRefs[dotNetConfiguration.id] ??= jsObject;
    }
    return dotNetConfiguration;
}


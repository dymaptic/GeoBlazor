// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './geoBlazorCore';
import { buildDotNetGeometry } from './geometry';
import { buildDotNetLOD } from './lOD';
import { buildJsGeometry } from './geometry';
import { buildJsLOD } from './lOD';

export function buildJsView2DConstraintsGenerated(dotNetObject: any): any {
    if (!hasValue(dotNetObject)) {
        return null;
    }

    let jsView2DConstraints: any = {};

    if (hasValue(dotNetObject.effectiveLODs) && dotNetObject.effectiveLODs!.length > 0) {
        jsView2DConstraints.effectiveLODs = dotNetObject.effectiveLODs?.map(i => buildJsLOD(i));
    }

    if (hasValue(dotNetObject.geometry)) {
        jsView2DConstraints.geometry = buildJsGeometry(dotNetObject.geometry);
    }

    if (hasValue(dotNetObject.lods) && dotNetObject.lods!.length > 0) {
        jsView2DConstraints.lods = dotNetObject.lods?.map(i => buildJsLOD(i));
    }


    if (hasValue(dotNetObject.effectiveMaxScale)) {
        jsView2DConstraints.effectiveMaxScale = dotNetObject.effectiveMaxScale;
    }

    if (hasValue(dotNetObject.effectiveMaxZoom)) {
        jsView2DConstraints.effectiveMaxZoom = dotNetObject.effectiveMaxZoom;
    }

    if (hasValue(dotNetObject.effectiveMinScale)) {
        jsView2DConstraints.effectiveMinScale = dotNetObject.effectiveMinScale;
    }

    if (hasValue(dotNetObject.effectiveMinZoom)) {
        jsView2DConstraints.effectiveMinZoom = dotNetObject.effectiveMinZoom;
    }

    if (hasValue(dotNetObject.maxScale)) {
        jsView2DConstraints.maxScale = dotNetObject.maxScale;
    }

    if (hasValue(dotNetObject.maxZoom)) {
        jsView2DConstraints.maxZoom = dotNetObject.maxZoom;
    }

    if (hasValue(dotNetObject.minScale)) {
        jsView2DConstraints.minScale = dotNetObject.minScale;
    }

    if (hasValue(dotNetObject.minZoom)) {
        jsView2DConstraints.minZoom = dotNetObject.minZoom;
    }

    if (hasValue(dotNetObject.rotationEnabled)) {
        jsView2DConstraints.rotationEnabled = dotNetObject.rotationEnabled;
    }

    if (hasValue(dotNetObject.snapToZoom)) {
        jsView2DConstraints.snapToZoom = dotNetObject.snapToZoom;
    }

    jsObjectRefs[dotNetObject.id] = jsView2DConstraints;
    arcGisObjectRefs[dotNetObject.id] = jsView2DConstraints;
    
    return jsView2DConstraints;
}


export function buildDotNetView2DConstraintsGenerated(jsObject: any, viewId: string | null): any {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetView2DConstraints: any = {};
    
        if (hasValue(jsObject.effectiveLODs) && jsObject.effectiveLODs!.length > 0) {
            dotNetView2DConstraints.effectiveLODs = jsObject.effectiveLODs?.map(i => buildDotNetLOD(i, viewId));
        }

        if (hasValue(jsObject.geometry)) {
            dotNetView2DConstraints.geometry = buildDotNetGeometry(jsObject.geometry);
        }

        if (hasValue(jsObject.lods) && jsObject.lods!.length > 0) {
            dotNetView2DConstraints.lods = jsObject.lods?.map(i => buildDotNetLOD(i, viewId));
        }

    if (hasValue(jsObject.effectiveMaxScale)) {
        dotNetView2DConstraints.effectiveMaxScale = jsObject.effectiveMaxScale;
    }
    
    if (hasValue(jsObject.effectiveMaxZoom)) {
        dotNetView2DConstraints.effectiveMaxZoom = jsObject.effectiveMaxZoom;
    }
    
    if (hasValue(jsObject.effectiveMinScale)) {
        dotNetView2DConstraints.effectiveMinScale = jsObject.effectiveMinScale;
    }
    
    if (hasValue(jsObject.effectiveMinZoom)) {
        dotNetView2DConstraints.effectiveMinZoom = jsObject.effectiveMinZoom;
    }
    
    if (hasValue(jsObject.maxScale)) {
        dotNetView2DConstraints.maxScale = jsObject.maxScale;
    }
    
    if (hasValue(jsObject.maxZoom)) {
        dotNetView2DConstraints.maxZoom = jsObject.maxZoom;
    }
    
    if (hasValue(jsObject.minScale)) {
        dotNetView2DConstraints.minScale = jsObject.minScale;
    }
    
    if (hasValue(jsObject.minZoom)) {
        dotNetView2DConstraints.minZoom = jsObject.minZoom;
    }
    
    if (hasValue(jsObject.rotationEnabled)) {
        dotNetView2DConstraints.rotationEnabled = jsObject.rotationEnabled;
    }
    
    if (hasValue(jsObject.snapToZoom)) {
        dotNetView2DConstraints.snapToZoom = jsObject.snapToZoom;
    }
    

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetView2DConstraints.id = geoBlazorId;
        jsObjectRefs[dotNetView2DConstraints.id] ??= jsObject;
        arcGisObjectRefs[dotNetView2DConstraints.id] ??= jsObject;
    }

    return dotNetView2DConstraints;
}


// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue } from './arcGisJsInterop';
import { buildDotNetVoxelColorStop } from './voxelColorStop';

export async function buildJsVoxelColorStopGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsVoxelColorStop: any = {};
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsVoxelColorStop.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.position)) {
        jsVoxelColorStop.position = dotNetObject.position;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsVoxelColorStop);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsVoxelColorStop;
    
    let dnInstantiatedObject = await buildDotNetVoxelColorStop(jsVoxelColorStop);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for VoxelColorStop', e);
    }
    
    return jsVoxelColorStop;
}

export async function buildDotNetVoxelColorStopGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetVoxelColorStop: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetVoxelColorStop.color = buildDotNetMapColor(jsObject.color);
        }
    if (hasValue(jsObject.position)) {
        dotNetVoxelColorStop.position = jsObject.position;
    }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetVoxelColorStop.id = k;
                break;
            }
        }
    }

    return dotNetVoxelColorStop;
}


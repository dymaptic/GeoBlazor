// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file.
import { arcGisObjectRefs, jsObjectRefs, hasValue, lookupGeoBlazorId } from './arcGisJsInterop';
import { buildDotNetCIMGeometricEffectWave } from './cIMGeometricEffectWave';

export async function buildJsCIMGeometricEffectWaveGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsCIMGeometricEffectWave: any = {};

    if (hasValue(dotNetObject.amplitude)) {
        jsCIMGeometricEffectWave.amplitude = dotNetObject.amplitude;
    }
    if (hasValue(dotNetObject.period)) {
        jsCIMGeometricEffectWave.period = dotNetObject.period;
    }
    if (hasValue(dotNetObject.primitiveName)) {
        jsCIMGeometricEffectWave.primitiveName = dotNetObject.primitiveName;
    }
    if (hasValue(dotNetObject.seed)) {
        jsCIMGeometricEffectWave.seed = dotNetObject.seed;
    }
    if (hasValue(dotNetObject.waveform)) {
        jsCIMGeometricEffectWave.waveform = dotNetObject.waveform;
    }
    
    let jsObjectRef = DotNet.createJSObjectReference(jsCIMGeometricEffectWave);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsCIMGeometricEffectWave;
    
    let { buildDotNetCIMGeometricEffectWave } = await import('./cIMGeometricEffectWave');
    let dnInstantiatedObject = await buildDotNetCIMGeometricEffectWave(jsCIMGeometricEffectWave);

    try {
        let seenObjects = new WeakMap();
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', 
            jsObjectRef, JSON.stringify(dnInstantiatedObject, function (key, value) {
                if (key.startsWith('_') || key === 'jsComponentReference') {
                    return undefined;
                }
                if (typeof value === 'object' && value !== null
                    && !(Array.isArray(value) && value.length === 0)) {
                    if (seenObjects.has(value)) {
                        console.warn(`Circular reference in serializing type CIMGeometricEffectWave detected at path: ${key}, value: ${value.declaredClass}`);
                        return undefined;
                    }
                    seenObjects.set(value, true);
                }
                return value;
            }));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for CIMGeometricEffectWave', e);
    }
    
    return jsCIMGeometricEffectWave;
}


export async function buildDotNetCIMGeometricEffectWaveGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetCIMGeometricEffectWave: any = {
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
    if (hasValue(jsObject.amplitude)) {
        dotNetCIMGeometricEffectWave.amplitude = jsObject.amplitude;
    }
    if (hasValue(jsObject.period)) {
        dotNetCIMGeometricEffectWave.period = jsObject.period;
    }
    if (hasValue(jsObject.primitiveName)) {
        dotNetCIMGeometricEffectWave.primitiveName = jsObject.primitiveName;
    }
    if (hasValue(jsObject.seed)) {
        dotNetCIMGeometricEffectWave.seed = jsObject.seed;
    }
    if (hasValue(jsObject.type)) {
        dotNetCIMGeometricEffectWave.type = jsObject.type;
    }
    if (hasValue(jsObject.waveform)) {
        dotNetCIMGeometricEffectWave.waveform = jsObject.waveform;
    }

    let geoBlazorId = lookupGeoBlazorId(jsObject);
    if (hasValue(geoBlazorId)) {
        dotNetCIMGeometricEffectWave.id = geoBlazorId;
    }

    return dotNetCIMGeometricEffectWave;
}


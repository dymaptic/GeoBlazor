import { buildDotNetElevationProfileLineQuery } from './elevationProfileLineQuery';
// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file
import ElevationProfileLineQuery from '@arcgis/core/widgets/ElevationProfile/ElevationProfileLineQuery';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
export async function buildJsElevationProfileLineQueryGenerated(dotNetObject: any): Promise<any> {
    let jsElevationProfileLineQuery = new ElevationProfileLineQuery();
    if (hasValue(dotNetObject.color)) {
        let { buildJsMapColor } = await import('./mapColor');
        jsElevationProfileLineQuery.color = buildJsMapColor(dotNetObject.color) as any;
    }

    if (hasValue(dotNetObject.elevationProfileLineId)) {
        jsElevationProfileLineQuery.id = dotNetObject.elevationProfileLineId;
    }
    if (hasValue(dotNetObject.source)) {
        const { id, dotNetComponentReference, layerId, viewId, ...sanitizedSource } = dotNetObject.source;
        jsElevationProfileLineQuery.source = sanitizedSource;
    }
    if (hasValue(dotNetObject.title)) {
        jsElevationProfileLineQuery.title = dotNetObject.title;
    }
    if (hasValue(dotNetObject.viewVisualizationEnabled)) {
        jsElevationProfileLineQuery.viewVisualizationEnabled = dotNetObject.viewVisualizationEnabled;
    }
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(jsElevationProfileLineQuery);
    jsObjectRefs[dotNetObject.id] = jsObjectRef;
    arcGisObjectRefs[dotNetObject.id] = jsElevationProfileLineQuery;
    
    let dnInstantiatedObject = await buildDotNetElevationProfileLineQuery(jsElevationProfileLineQuery);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for ElevationProfileLineQuery', e);
    }
    
    return jsElevationProfileLineQuery;
}
export async function buildDotNetElevationProfileLineQueryGenerated(jsObject: any): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetElevationProfileLineQuery: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.color)) {
            let { buildDotNetMapColor } = await import('./mapColor');
            dotNetElevationProfileLineQuery.color = buildDotNetMapColor(jsObject.color);
        }
        if (hasValue(jsObject.hoveredPoint)) {
            let { buildDotNetPoint } = await import('./point');
            dotNetElevationProfileLineQuery.hoveredPoint = buildDotNetPoint(jsObject.hoveredPoint);
        }
        if (hasValue(jsObject.id)) {
            dotNetElevationProfileLineQuery.elevationProfileLineId = jsObject.id;
        }
        if (hasValue(jsObject.progress)) {
            dotNetElevationProfileLineQuery.progress = jsObject.progress;
        }
        if (hasValue(jsObject.samples)) {
            dotNetElevationProfileLineQuery.samples = jsObject.samples;
        }
        if (hasValue(jsObject.source)) {
            dotNetElevationProfileLineQuery.source = jsObject.source;
        }
        if (hasValue(jsObject.statistics)) {
            dotNetElevationProfileLineQuery.statistics = jsObject.statistics;
        }
        if (hasValue(jsObject.title)) {
            dotNetElevationProfileLineQuery.title = jsObject.title;
        }
        if (hasValue(jsObject.type)) {
            dotNetElevationProfileLineQuery.type = jsObject.type;
        }
        if (hasValue(jsObject.viewVisualizationEnabled)) {
            dotNetElevationProfileLineQuery.viewVisualizationEnabled = jsObject.viewVisualizationEnabled;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetElevationProfileLineQuery.id = k;
                break;
            }
        }
    }

    return dotNetElevationProfileLineQuery;
}


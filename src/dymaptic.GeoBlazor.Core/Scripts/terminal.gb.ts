// File auto-generated by dymaptic tooling. Any changes made here will be lost on future generation. To override functionality, use the relevant root .ts file


import Terminal from '@arcgis/core/networks/support/Terminal';
import {arcGisObjectRefs, hasValue, jsObjectRefs} from './arcGisJsInterop';
import {IPropertyWrapper} from './definitions';

export default class TerminalGenerated implements IPropertyWrapper {
    public component: Terminal;
    public geoBlazorId: string | null = null;
    public viewId: string | null = null;
    public layerId: string | null = null;

    constructor(component: Terminal) {
        this.component = component;
    }
    
    // region methods
   
    unwrap() {
        return this.component;
    }
    
    // region properties
    
    getProperty(prop: string): any {
        return this.component[prop];
    }
    
    setProperty(prop: string, value: any): void {
        this.component[prop] = value;
    }
}

export async function buildJsTerminalGenerated(dotNetObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    let jsTerminal = new Terminal();

    if (hasValue(dotNetObject.name)) {
        jsTerminal.name = dotNetObject.name;
    }
    if (hasValue(dotNetObject.terminalId)) {
        jsTerminal.id = dotNetObject.terminalId;
    }
    let { default: TerminalWrapper } = await import('./terminal');
    let terminalWrapper = new TerminalWrapper(jsTerminal);
    terminalWrapper.geoBlazorId = dotNetObject.id;
    terminalWrapper.viewId = viewId;
    terminalWrapper.layerId = layerId;
    
    // @ts-ignore
    let jsObjectRef = DotNet.createJSObjectReference(terminalWrapper);
    jsObjectRefs[dotNetObject.id] = terminalWrapper;
    arcGisObjectRefs[dotNetObject.id] = jsTerminal;
    
    let dnInstantiatedObject = await buildDotNetTerminal(jsTerminal, layerId, viewId);
    
    try {
        await dotNetObject.dotNetComponentReference.invokeMethodAsync('OnJsComponentCreated', jsObjectRef, JSON.stringify(dnInstantiatedObject));
    } catch (e) {
        console.error('Error invoking OnJsComponentCreated for Terminal', e);
    }
    
    return jsTerminal;
}

export async function buildDotNetTerminalGenerated(jsObject: any, layerId: string | null, viewId: string | null): Promise<any> {
    if (!hasValue(jsObject)) {
        return null;
    }
    
    let dotNetTerminal: any = {
        // @ts-ignore
        jsComponentReference: DotNet.createJSObjectReference(jsObject)
    };
        if (hasValue(jsObject.name)) {
            dotNetTerminal.name = jsObject.name;
        }
        if (hasValue(jsObject.id)) {
            dotNetTerminal.terminalId = jsObject.id;
        }

    if (Object.values(arcGisObjectRefs).includes(jsObject)) {
        for (const k of Object.keys(arcGisObjectRefs)) {
            if (arcGisObjectRefs[k] === jsObject) {
                dotNetTerminal.id = k;
                break;
            }
        }
    }

    return dotNetTerminal;
}

